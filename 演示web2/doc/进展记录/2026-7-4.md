# 2026-7-4 进展记录

## 2026-07-04 01:00 ~ 2026-07-04 01:45

### 完成内容

为 lang-drill-agent 搭建第二版产品展示网站（演示web2），Framer 深色光晕风，面向 GitHub Pages 部署。

#### 新增文件清单

- `package.json`：项目配置，name=langdrill-agent-product-site-2，dev 端口 4273
- `vite.config.ts`：Vite 多入口配置，base 默认 `/lang-drill-agent/`，VITE_BASE_PATH 可覆盖
- `tsconfig.json` + `tsconfig.node.json`：标准 React + Vite + Node 类型配置
- `index.html`：landing 落地页入口，含前置主题脚本 + OG 社交分享元标签
- `app.html`：mock 演示前端入口，含前置主题脚本（与 frontend 一致）
- `.gitignore`：忽略 node_modules/dist/tsbuildinfo 等
- `AGENTS.md`：本地项目规则文档
- `src/main.tsx`：landing 入口，hash 路由 `#/app` 切换到 MockFrame（iframe 隔离）
- `src/App.tsx`：landing 主组件（约 800 行），含 Header/Hero/Workflow/PainPoint/Features/Showcase/DemoCTA/Install/Footer/CursorGlow + GSAP 动画
- `src/styles.css`：Framer 深色光晕风样式（约 1100 行），CSS 变量双主题
- `src/app-main.tsx`：mock 入口，导入 ./mock/App 和 ./mock/styles.css
- `src/mock/`：frontend/src/ 整包复制（App.tsx/api.ts/fileImport.ts/types.ts/styles.css/components/）
- `src/mock/api.ts`：替换为本地 mock，所有端点返回固定演示数据
- `public/assets/hero-bg-texture.jpg`：生成的英雄区质感纹理图（176KB，text_to_image API）
- `public/assets/screenshots/`：24 张脱敏截图（light-/dark- 前缀，12 张 × 2 主题）
- `public/favicon-light.png` + `favicon-dark.png`：页签图标
- `public/assets/logo-light.png` + `logo-dark.png` + `dark-theme-bg.jpg`：logo 和背景图

#### 关键决策

1. **Vite 多入口 + iframe 隔离**：landing 与 mock 前端样式差异巨大（Framer 风 vs 卡通果冻风），用独立 HTML 入口 + iframe 隔离避免 CSS 互相污染
2. **mock API 策略**：保留 frontend 整个 src/ 不动，仅替换 api.ts 为返回固定数据的本地实现，UI 渲染 100% 保真
3. **GitHub Pages base 路径**：base 设为 `/lang-drill-agent/`，可通过 `VITE_BASE_PATH=./` 覆盖用于本地预览
4. **素材虚构**：skills 路径虚构为 `~/LangDrill/skills/skill1`、`~/LangDrill/skills/skill2`，data_paths 虚构为 `~/LangDrill/user-data`，不暴露真实主机位置
5. **图片路径**：hero-bg-texture.jpg 使用 inline style + `import.meta.env.BASE_URL` 确保在 GitHub Pages 子路径下正确加载

### 验证结果

- `npm install`：成功，72 个依赖包
- `npm run build`：成功，TypeScript 编译零错，Vite 打包 4586 模块
  - dist/index.html：2.89 kB（gzip 1.17 kB）
  - dist/app.html：1.82 kB（gzip 0.83 kB）
  - dist/assets/main-*.css：28.69 kB（gzip 6.08 kB）
  - dist/assets/main-*.js：108.26 kB（gzip 36.59 kB）
  - dist/assets/app-*.css：51.32 kB（gzip 9.70 kB）
  - dist/assets/app-*.js：200.28 kB（gzip 58.59 kB）
- `npm run dev`：dev 服务器正常启动，HMR 热更新无错
- `npm run preview`：production build 预览正常，landing 页和 `#/app` mock iframe 均无浏览器错误

### 错误汇报

无错误。过程中遇到两个小问题已修复：
1. vite.config.ts 缺少 @types/node → 安装 @types/node 并在 tsconfig.node.json 添加 `types: ["node"]`
2. CSS 中 `background-image: url("/assets/...")` 在 GitHub Pages 子路径下会失效 → 改用 inline style + `import.meta.env.BASE_URL`

### 备注

- 演示web2 在根 `.gitignore` 中被忽略（第 17 行），提交前需手动 `git add -f 演示web2/` 或调整 `.gitignore`
- 父项目 AGENTS.md 位于工作区外无法编辑，已在 演示web2/AGENTS.md 中创建本地规则文档
- 父项目 doc/进展记录/ 位于工作区外无法写入，本进展记录保存在本地 演示web2/doc/进展记录/

## 2026-07-04 02:00 ~ 2026-07-04 11:30

### 完成内容

根据用户 11 项详细反馈对产品展示站进行第二轮迭代修改，覆盖视觉、文案、动画节奏和数据真实性。

#### 修改文件清单

- `src/App.tsx`（landing 主组件）
  - 单词银河扩充：英文 96 词 + 日语 25 词（英文 ~80%），分两条独立数组
  - WordGalaxy 组件：4 旋臂分布，字号 8-15px（原 50%），范围 0.1-0.95（+0.5 倍），引入 `--dist` 距离虚化变量
  - GSAP 滚动汇聚：银河随滚动汇聚到中央消失（被工作台界面遮挡），叠加 `galaxy-arms` 旋转
  - Workflow 动画重构：单词汇聚进入中央 Generate 图标消失，题目从中央渐变呈现发散到右侧，再飞入下方题卡；ScrollTrigger `end: "center 30%"` 确保工作台滑到网页中间时动画完成
  - ShowcaseSection：MutationObserver 监听 `data-theme`，浅色展示 light- 截图、深色展示 dark- 截图；SCREENSHOTS 扩充到 17 张；caption 移到图片下方不再覆盖
  - STATS 修改：`9 内置考试与考纲` / `100% 数据本地存储` / `6+ 题型自动组卷` / `1 本地学习闭环`（去掉 4+ 模型供应商 / 1:1 还原度 / 0 密钥）
  - FEATURE_ROWS 文案：全部改为面向客户的产品介绍（去掉技术说明性描述）
  - InstallSection：新增 install-warning 警告块（未签名安装包可能触发 SmartScreen）
  - DemoCtaSection：改为"在浏览器里直接体验"，不抄提示词
  - 移除未使用的 LockKey import

- `src/styles.css`
  - `.galaxy-arms` 旋臂容器 90s 旋转动画
  - `.galaxy-core` 放大到 460px，blur 50px
  - `.galaxy-word` 距中心越远越虚化（`--dist` 控制 color 和 blur）
  - `.galaxy-float` 增加 25%/75% 关键帧
  - `.word-galaxy::after` 中央密度减少 mask（radial-gradient 让中间文字后面更干净）
  - `.painpoint-grid` 改为 2 列布局，`.painpoint-bridge` 居中
  - `.showcase-caption-below` 替代 `.showcase-caption`（介绍语放图片下方，毛玻璃质感）
  - `.workflow-stage::before` 毛玻璃背景虚化单词
  - `.mock-frame-wrapper` 修复 iframe 全屏样式（解决 #/app 黑屏只剩左上角小窗口）
  - 移除 body `font-feature-settings: "cv02", "cv03", "cv04", "cv11"`（修复汉字"复"被压到左半边）
  - `.mock-back` top 从 16px 改为 64px（下移约 1.5 个按钮高度，不挡 mock 内左侧栏收起按钮）

- `src/mock/api.ts`（mock 数据添加用户痕迹）
  - DEMO_SESSIONS：3 → 10 条（涵盖截图词表练习、近义辨析错题回流、仔细阅读真题、CJT4 阅读语境、翻译练习、写作模板等）
  - DEMO_QUESTION：set_total 12 / set_done 2 / sequence 3（反映进行中状态）
  - DEFAULT_PANEL：12 题/8 完成、24 知识点/17 完成、accuracy 0.78、真实 plan 与 summary
  - DEFAULT_LEARNING_STATS：questions_done 168 / total 220、words_mastered 342 / 600、accuracy 0.78、attempts 220/172
  - DEFAULT_TOKEN_USAGE：1.67M tokens、248 calls、38 sessions、24 active_days、streak 3、含 model/provider/task breakdown + 7 天 daily_activity + 5 条 recent_calls
  - DEFAULT_DATA_PATHS：counts 全部填充（38 sessions / 412 messages / 220 questions / 248 attempts / 612 knowledge_items / 14 branches / 248 model_calls / 5 syllabus / 12 exam_assets）
  - `/api/chat`：daily_panel 与 active_question 返回真实进度（chat 文本仍为固定自介绍，符合 AGENTS.md 约束）

- `public/assets/screenshots/`：新增 16 张截图（8 张 × light/dark），总计 73 个文件

### 错误汇报

- **汉字"复"被压到左半边**：根因是 body 上的 `font-feature-settings: "cv02", "cv03", "cv04", "cv11"`，这些 Inter 字体的 OpenType 特性在 fallback 到 CJK 字体时让"复"字字形错位。解决方案：移除 body 的 font-feature-settings（这些特性对 CJK 无意义，移除后 Inter 拉丁字符渲染不受影响）
- **#/app 黑屏只剩左上角小窗口**：根因是 `.mock-frame-wrapper` 和 `.mock-frame` CSS 缺失，iframe 默认 150×150。解决方案：在 styles.css 添加 `position: fixed; inset: 0; flex: 1; width: 100%; height: 100%; border: none;`
- **mock-back 按钮挡住左侧栏收起**：top 从 16px 改为 64px，下移约 1.5 个按钮高度

### 验证结果

- `npm run build`：成功，TypeScript 编译零错，Vite 打包 4586 模块
  - dist/index.html：2.89 kB（gzip 1.17 kB）
  - dist/app.html：1.82 kB（gzip 0.83 kB）
  - dist/assets/main-*.css：30.67 kB（gzip 6.45 kB）
  - dist/assets/main-*.js：114.55 kB（gzip 38.49 kB）
  - dist/assets/app-*.css：51.32 kB（gzip 9.70 kB）
  - dist/assets/app-*.js：205.16 kB（gzip 59.94 kB）

### 待用户确认事项

1. 用户痕迹数据（DEMO_SESSIONS / PANEL / STATS / TOKEN_USAGE）的具体内容是否符合预期，后续按用户反馈删减
2. 银河动画节奏（scrub 1.0）是否合适
3. Workflow 动画在滑到中间时完成是否符合视觉预期
4. 截图主题切换是否正常（浅色主题展示 light- 截图、深色展示 dark-）

## 2026-07-04 12:00 ~ 2026-07-04 12:45

### 完成内容

根据用户第三轮 4 项反馈对产品展示站继续迭代，主要解决单词倒置、动画节奏、Showcase 选择栏位置和 Mock 真实数据填充。

#### 修改文件清单

- `src/App.tsx`（landing 主组件）
  - **需求 1（单词银河重构）**：
    - 词库扩充到 ~225 词（英文 96+80 / 日语 25+24，密度 +2 倍）
    - WordGalaxy 组件重写为两层结构：外层 `.galaxy-orbit` 用 CSS `offset-path: ellipse()` 沿独立椭圆轨道公转，`offset-rotate: 0deg` 保证文字方向始终正向（解决"单词倒过来"问题）；内层 `.galaxy-word` 做 `galaxy-drift` 独立小范围漂移（6-16s），双重运动通过 CSS 变量独立控制
    - GSAP 移除 `.galaxy-arms` 整体 360° 旋转（旧实现导致子元素文字跟着倒置）
  - **需求 2（Workflow 动画重构）**：
    - Generate 图标静态化：移除 GSAP 对 `.generator-core` 的 scale/filter/brightness 动画，仅保留 CSS `core-pulse` 持续脉冲
    - ScrollTrigger `end: "bottom 67%"`（trigger 底部到达视口 67% 时动画完成，即页面下方 1/3 处）
    - 单词卡：从左栏 Vocabulary 飞入 Generate 中央 → 向右滑动渐变消失到 Generate 的左 2/9 处（`x: 80, scale: 0.4, filter: blur(8px)`）
    - 题卡：从 Generate 的 7/9 处渐变出现（`x: -60 → 0`），移动到 Output 框的正确位置（`y: 0`，回到 CSS 布局原位，修复之前位置歪斜）
  - **需求 3（Showcase 选择栏位置）**：
    - 缩略图列从图片下方移到图片右侧（grid 1fr 240px 布局，列宽 280 → 240px）
    - 简介从右侧移到图片下方（新结构 `.showcase-main-col` 包含图片 + `.showcase-caption-below`）

- `src/styles.css`
  - `.galaxy-arms` 替换为 `.galaxy-orbit`：`offset-path: ellipse(var(--orbit-rx) var(--orbit-ry) at 0 0); offset-rotate: 0deg; animation: galaxy-orbit var(--orbit-duration) linear infinite;`
  - `.galaxy-word` 改为内层独立漂移：`animation: galaxy-drift var(--drift-duration) ease-in-out infinite;` 4 个关键帧（0/25/50/75%）控制 translate 和 scale
  - `.showcase-stage` 改为 `grid-template-columns: 1fr 240px`
  - 新增 `.showcase-main-col` 容器（flex column + gap 12px）
  - `.showcase-caption-below` 调整 padding 和 border-radius

- `src/mock/api.ts`（mock 数据填充真实数据）
  - **需求 4（Mock Agent 真实数据）**：
    - `MOCK_PROFILE.deadline`：`null` → `"2026-12-19T09:00"`（考试倒计时显示）
    - `DEFAULT_SYLLABUS_STATUS.sources`：1 条 → 8 条真实考纲占位（cet4/cet6/cft4/cjt4/cjt6/ielts/toefl/gaokao-english）
    - `DEFAULT_PAST_PAPER_STATUS.papers`：0 条 → 3 条真实真题占位（2023/2024/2025 年 CET-4 真题参考索引，含 metadata / parse_status / parser）
    - `DEMO_SESSIONS`：替换为 3 段真实会话（CET-4 截图词表练习 / CJT4 错题回流训练 / 截图导入 vehicle 词法点）
    - `SESSION_MESSAGES`：28 条完整脱敏消息记录（12 + 10 + 6 条），覆盖截图导入、答题判断、模型配置询问、复盘总结
    - 新增 `SESSION_PANELS`：3 个会话各自的 daily_panel（CET-4 12/8 题 / CJT4 12/4 题 / 0622 8/8 题已完成）
    - 新增 `SESSION_QUESTIONS`：3 个会话各自的 active_question（CET-4 collection / CJT4 しまう / 0622 null 已完成）
    - `/api/sessions/{id}` 路由：从空壳 `{ session: null }` 改为返回完整结构 `{ session, messages, daily_panel, active_question, token_usage, learning_stats }`，按 sessionId 读取 SESSION_MESSAGES / SESSION_PANELS / SESSION_QUESTIONS
    - `/api/chat` 路由：`session_id` 从 `"demo-1"` 改为 `"ses_demo_cet4_active"`（与 DEMO_SESSIONS 一致），daily_panel 和 active_question 同步指向 SESSION_PANELS / SESSION_QUESTIONS
    - 修复 TypeScript 类型问题：Message 类型无 `session_id` 字段，从所有 28 条消息中移除冗余的 `session_id: "..."` 字段

### 真实数据库查询（一次性）

通过 Python sqlite3 模块查询真实数据库 `d:\1Folder\语言学习-lang-drill\语言学习-lang-drill-agent\测试数据\演示数据库\产品网站演示-20260703\data\langdrill_agent.db`（446KB，原库被进程锁定，先复制到 try/ 再查询）：
- 表结构：study_sessions / questions / messages / attempts / knowledge_items / exam_assets / syllabus_sources 等
- 真实 study_sessions 列表 + 关联 messages（用于选择 3 段适合演示的会话）
- 真实 syllabus_sources / exam_assets（用于填充考纲和真题占位）

查询脚本和数据库副本均为临时调试用，查询完成后已删除。

### 验证结果

- `npm run build`：成功，TypeScript 编译零错，Vite 打包 4586 模块
  - dist/index.html：2.89 kB（gzip 1.18 kB）
  - dist/app.html：1.82 kB（gzip 0.83 kB）
  - dist/assets/main-*.css：30.99 kB（gzip 6.50 kB）
  - dist/assets/main-*.js：116.18 kB（gzip 39.03 kB）
  - dist/assets/app-*.css：51.32 kB（gzip 9.70 kB）
  - dist/assets/app-*.js：218.73 kB（gzip 63.25 kB）
  - dist/assets/Target.es-*.js：305.25 kB（gzip 99.76 kB）

### 错误汇报

- **sqlite3 CLI 不可用**：`sqlite3: The term 'sqlite3' is not recognized as a name of a cmdlet`，改用 Python 内置 `sqlite3` 模块
- **原数据库无法打开**：`sqlite3.OperationalError: unable to open database file`（原 DB 被进程锁定或权限问题），用 `Copy-Item` 复制到 `try/langdrill_agent_copy.db` 后再查询
- **Python 单行命令引号嵌套问题**：`SyntaxError: '(' was never closed`，改用独立 `.py` 脚本文件而非 `-c` 单行命令
- **CSS 残留 `}` 导致语法错误**：替换 `.galaxy-arms` 时遗留两个 `}` 在第 528-530 行，删除多余的 `}` 修复
- **db_summary.py SQL 字段错误**：`sqlite3.OperationalError: no such column: total_tokens`（model_calls 表用 input_tokens+output_tokens，无 total_tokens 列），脚本最后一段报错，但前面已获取到所需的所有会话和消息数据，不影响后续工作
- **Message 类型包含 session_id 字段**：types.ts 第 247-252 行 Message 类型无 `session_id`，但 SESSION_MESSAGES 中所有 28 条消息都加了 `session_id: "..."`，TypeScript 会报错。从所有消息中移除冗余的 `session_id` 字段（值与所在数组 key 一致，本身冗余）

### 清理操作

- 删除 `try/` 目录下所有临时调试文件：`db_dump.txt` / `db_inspect.py` / `db_sessions.py` / `db_summary.py` / `langdrill_agent_copy.db`
- 工作区无残留临时文件

### 待用户确认事项

1. 单词银河双重运动（公转 + 漂移）效果是否符合预期
2. Workflow 动画在页面下方 1/3 处完成的节奏是否合适
3. Showcase 缩略图移到右侧后选择是否更方便
4. Mock 中 3 段真实会话的展示效果是否符合预期
