# 2026-06-26 优化归档

本目录保存已从运行路径移除的旧功能模块，便于后续查阅或恢复。

- `composer.py`：旧右侧工作台组词器后端服务。前端已改为题目选项下方的额外提问输入框。
- `integrations_anki.py`：旧 Anki（记忆卡工具）导出集成。背词同步方向已改为手机映像与截图导入。

这些文件不再由 `backend/langdrill_agent/api.py` 导入，避免启动时加载已下线功能。
