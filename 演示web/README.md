# Lang Drill Agent Product Site

这是独立产品展示网站，不连接真实后端，不读取 `.env`，适合部署到 GitHub Pages 这类静态托管。

```powershell
npm install
npm run dev
npm run build
```

站点包含：

- 默认跟随系统的浅色/深色主题。
- 单词银河、滚动组卷演示和考试式题卡展示。
- 可探索的三栏工作台模拟器，模型回复为固定演示内容。
- GitHub 和 Windows 安装包入口。
- 脱敏截图素材和生成概念图资产。
