# 分支清理报告（2026-07-02）

## 检查范围

- 项目目录：`D:\1Folder\语言学习-lang-drill\语言学习-lang-drill-agent`
- 基准分支：`main`（主线）
- 远端基准：`origin/main`（远端主线）
- 检查方式：`git status`、`git branch --merged`、`git branch --no-merged`、`git rev-list`、`git diff --stat`、`git merge-base --is-ancestor` 和当前源码关键字核对。
- 本报告只做审计和建议，未删除任何分支，未合并任何未合并任务分支。

## 总体结论

- `main`（主线）工作区干净，且与 `origin/main`（远端主线）同步。
- 清理前本地分支共 46 个，远端跟踪分支共 33 个；数量已经偏多。
- 按 Git（版本控制）祖先关系判断，本地仍有 10 个分支未合并到 `main`，远端仍有 9 个分支未合并到 `origin/main`。
- 这些未合并分支不是 10 个独立任务，而是多条旧任务派生链，其中 7 个分支已经被其它未合并分支包含。
- 当前主线已经能看到多项对应能力：Markdown（标记语言）渲染组件、左右面板拖拽、已答题回顾卡片、Agent 设置权限、Skills（技能）单项开关，以及移除“当前：开启”重复思考状态标签。因此部分分支更像旧实现或旧检查点，不能直接按“未合并”等同于“功能缺失”处理。

## 未合并分支分组

### A. 需要重点判断的顶端分支

| 分支 | 远端状态 | 未进主线提交数 | 差异规模 | 判断 | 建议 |
| --- | --- | ---: | --- | --- | --- |
| `codex/fix-duplicate-thinking-status` | 本地和远端都有 | 8 | 19 个文件，2045 行新增，136 行删除 | 旧任务链顶端，包含截图词表回顾、设置草稿、上下文圆环、可拖拽面板、重复思考状态修复等历史提交；主线已有相似能力，但提交哈希不同。 | 不建议整分支合并。先按功能逐项确认主线已覆盖，确认后删除该分支及其包含的旧节点。 |
| `codex/feature-agent-settings-permissions` | 仅本地 | 3 | 8 个文件，216 行新增，45 行删除 | 本地检查点分支，包含 `codex/fix-duplicate-thinking-controls`；主线已有 Agent 设置权限和重复思考状态修复。 | 优先人工看一次 `RightWorkbench.tsx` 和样式差异，若无要保留的 UI（用户界面）状态变更，可只删本地分支。 |
| `codex/fix-markdown-message-rendering` | 本地和远端都有 | 1 | 11 个文件，1684 行新增，60 行删除 | 旧 Markdown（标记语言）渲染实现，新增 `MarkdownContent.tsx` 和依赖变更；主线当前采用 `MarkdownText.tsx`，且聊天和分支消息都已使用。 | 不建议合并，避免引入旧依赖和重复组件。确认 `MarkdownText.tsx` 满足验收后删除本地和远端分支。 |

### B. 被其它未合并分支包含的旧节点

| 分支 | 被包含于 | 建议 |
| --- | --- | --- |
| `codex/feat-resizable-panels` | `codex/fix-duplicate-thinking-status` | 主线已存在 `panel-resizer` 相关实现；确认拖拽验收通过后删除。 |
| `codex/feature-screenshot-word-review-cards` | `codex/feat-resizable-panels`、`codex/fix-context-ring-progress`、`codex/fix-duplicate-thinking-status` | 主线已有已答题回顾卡片和 `answered_question` 快照；确认验收记录已覆盖后删除。 |
| `codex/feature-settings-agent-autofill-current` | 多个旧链分支 | 主线已有 `settings_action`、`pendingPaperDraft` 和 `pendingCustomModelDraft`；确认设置草稿验收后删除。 |
| `codex/fix-context-ring-progress` | `codex/feat-resizable-panels`、`codex/fix-duplicate-thinking-status` | 主线已有上下文容量与圆环相关实现；确认显示进度无回归后删除。 |
| `codex/fix-duplicate-thinking-controls` | `codex/feature-agent-settings-permissions` | 主线已移除“当前：开启”重复标签；删除前无需单独合并。 |
| `codex/fix-screenshot-word-import` | 多个旧链分支 | 仅 1 个进展记录文件差异；若记录已被主线后续文档覆盖，可删除。 |
| `codex/test-browser-unaccepted-acceptance` | 多个旧链分支 | 主线已有 `try/browser_acceptance_check.py` 和 2026-07-02 浏览器验收记录；确认无独立测试资产后删除。 |

## 推荐清理顺序

1. 先保留 `main` 和当前报告提交，不动业务分支。
2. 对 3 个顶端分支逐个做人工确认：`codex/fix-duplicate-thinking-status`、`codex/feature-agent-settings-permissions`、`codex/fix-markdown-message-rendering`。
3. 人工确认后，先删被包含的旧节点，再删顶端分支，避免误删仍作为引用依据的分支。
4. 删除本地分支前优先用 `git branch -d`；只有确认无需保留且 Git（版本控制）仍因压缩合并或平行提交拒绝时，再使用 `git branch -D`。
5. 删除远端分支前先确认 GitHub（代码托管平台）上没有未关闭 PR（Pull Request，拉取请求）或正在使用的对比链接。

## 建议删除候选

确认后可优先清理这些旧节点：

```text
codex/feature-screenshot-word-review-cards
codex/feature-settings-agent-autofill-current
codex/fix-context-ring-progress
codex/fix-duplicate-thinking-controls
codex/fix-screenshot-word-import
codex/test-browser-unaccepted-acceptance
codex/feat-resizable-panels
```

需要最后确认的顶端候选：

```text
codex/fix-duplicate-thinking-status
codex/feature-agent-settings-permissions
codex/fix-markdown-message-rendering
```

## 暂不建议执行的操作

- 暂不建议把 `codex/fix-duplicate-thinking-status` 整分支合入主线，因为它基于旧基线，可能回带旧实现或覆盖当前 Skills（技能）拆分、自定义模型和联网能力。
- 暂不建议直接批量 `git branch -D`，因为 Git（版本控制）显示未合并的分支中有旧任务链顶端，仍需一次人工确认。
- 暂不建议删除远端分支前跳过 GitHub（代码托管平台）检查，避免破坏仍被引用的 PR（Pull Request，拉取请求）链接。

