# Lang Drill Agent 验收标准

## 测试环境

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10.0.26200 x64 或同等 Windows（视窗系统）环境 |
| 后端 | FastAPI（Web API 框架）开发服务 `http://127.0.0.1:8000` |
| 前端 | Vite（前端构建工具）开发服务 `http://127.0.0.1:5173` |
| 数据库 | SQLite（轻量数据库），正式学习状态以数据库为唯一来源 |
| 模型 | 普通 Web（网页）模式默认真实 provider（供应商）MiMo（小米米魔）；mock（本地模拟）provider 只用于自动测试或离线调试 |

## 1. 一键启动体验

| 项 | 内容 |
|---|---|
| 验收步骤 | 双击 `start.bat`。 |
| 预期结果 | `start.bat` 调用 `scripts/dev/start-dev.ps1`，后台启动后端和前端；只有 `http://127.0.0.1:8000/docs` 与 `http://127.0.0.1:5173` 都可访问后才打开浏览器；不额外留下后端/前端可见终端。 |
| 证明方式 | 自动测试 `py -m pytest try/test_startup_scripts.py -q`；运行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall` 返回 0；`Invoke-WebRequest http://127.0.0.1:5173` 与 `Invoke-WebRequest http://127.0.0.1:8000/docs` 返回 200；检查 `logs/langdrill-backend.out.log`、`logs/langdrill-backend.err.log`、`logs/langdrill-frontend.out.log`、`logs/langdrill-frontend.err.log`；运行 `stop.bat` 后 5173 / 8000 端口释放。 |
| 结果记录 | 2026-06-30 `start-dev.ps1 -NoBrowser -SkipInstall` 自动验证通过，5173 / 8000 返回 200；脚本已覆盖 API Key（接口密钥）前缀规范化；双击体验待人工确认。 |
| 反馈/问题 | 如服务失败，启动器必须输出错误并展示对应日志尾部，不能在未监听端口时直接打开浏览器；PowerShell（命令行脚本）内识别中文全角冒号必须使用编码安全的正则转义。 |
| 最终结论 | 自动验收通过，人工双击待确认。 |

## 2. 发送消息 thinking（思考）加载

| 项 | 内容 |
|---|---|
| 验收步骤 | 在 Web（网页）输入学习内容并发送，等待模型或后端响应。 |
| 预期结果 | 发送后聊天流末尾立即出现“模型正在思考”的 loading（加载）气泡和动效；后端返回后该气泡消失并显示正式回复。 |
| 证明方式 | 人工观察 UI（用户界面）；前端构建命令 `npm run build` 通过。 |
| 结果记录 | 待人工确认。 |
| 反馈/问题 | 若网络慢或模型慢，气泡必须持续可见，不能让页面无反馈。 |
| 最终结论 | 待定。 |

## 3. 正式题组先入库再展示

| 项 | 内容 |
|---|---|
| 验收步骤 | 输入一组词汇或学习请求，例如 `blood: n. 血液`，触发日常训练。 |
| 预期结果 | 后端先生成一整套题并写入 `questions` 表，再只返回当前第 1 题；回复不得固定为“已初始化今日学习面板，并准备好第一题”。 |
| 证明方式 | 自动测试 `py -m pytest try/test_formal_drill_flow.py -q`；数据库中同一 `session_id` 下存在多条 `questions` 记录。 |
| 结果记录 | 待自动验证。 |
| 反馈/问题 | 真实模型不可用时允许 fallback（兜底）题组，但仍必须多题入库。 |
| 最终结论 | 待定。 |

## 4. 答题后自动进入下一题

| 项 | 内容 |
|---|---|
| 验收步骤 | 对当前题选择 A/B/C/D 并提交。 |
| 预期结果 | 系统先判题和讲解，再自动返回下一道待答题；用户无需输入“下一题”。答题响应必须包含上一题 `answered_question`（已答题快照），助手消息 payload（附加数据）也必须保存该快照，供前端回顾。 |
| 证明方式 | 自动测试 `py -m pytest try/test_formal_drill_flow.py try/test_answer_submission.py -q`；`attempts` 表新增作答记录；上一题状态为 `answered`，下一题状态为 `ready`；历史会话详情能读回上一题 payload（附加数据）。 |
| 结果记录 | 2026-06-30 自动测试通过，答题响应和历史会话消息均包含已答题快照。 |
| 反馈/问题 | 最后一题答完时应提示本轮完成和总结入口，不应重新生成第一题。 |
| 最终结论 | 待定。 |

## 5. 显式“下一题”不重置

| 项 | 内容 |
|---|---|
| 验收步骤 | 当前题组进行中时，在聊天框输入“下一题”或“继续”。 |
| 预期结果 | 后端只读取当前题组待答题，不重新初始化今日学习面板，不重复生成新题组。 |
| 证明方式 | 自动测试 `py -m pytest try/test_formal_drill_flow.py -q`；回复包含“不重新开始”，且 `active_question.id` 保持为当前待答题。 |
| 结果记录 | 待自动验证。 |
| 反馈/问题 | 如果当前题组已完成，应提示输入新学习内容或查看总结。 |
| 最终结论 | 待定。 |

## 6. 题目显示位置

| 项 | 内容 |
|---|---|
| 验收步骤 | 发送学习内容、答题、等待下一题出现。 |
| 预期结果 | 当前待答题卡片跟随最新助手反馈显示在聊天流底部附近，并自动滚动到可见区域；提交答案后，上一题不直接消失，而是在助手讲解消息中显示为普通已答题回顾卡片，标出用户选择、正确答案和对错状态；回顾卡片不使用 sticky（吸附定位）。 |
| 证明方式 | 自动测试 `py -m pytest try/test_answer_submission.py try/test_formal_drill_flow.py -q`；前端构建命令 `cd frontend && npm run build` 通过；人工确认 UI（用户界面）中当前题吸附、已答题回顾卡片随聊天流滚动。 |
| 结果记录 | 2026-06-30 自动测试和前端构建通过；已答题回顾卡片的最终视觉仍待人工确认。 |
| 反馈/问题 | 不应出现题目卡片在顶部、用户以为没有下一题的情况；已答题卡片不能带提交控件，不能继续置顶遮挡下一题。 |
| 最终结论 | 待定。 |

## 7. 聊天栏模型与思考等级

| 项 | 内容 |
|---|---|
| 验收步骤 | 在设置中配置 OpenAI/GPT、Claude、DeepSeek（深度求索）或 MiMo（小米米魔）的 API Key（接口密钥），再在聊天栏上方切换模型供应商、模型名称和思考等级；点击模型供应商旁 `+`，填写供应商名称、Base URL（基础网址）和默认模型后点击“添加供应商”；对当前模型点击“添加思考等级”后保存。 |
| 预期结果 | 未配置 API Key（接口密钥）的真实供应商不出现在聊天栏快捷模型选择；自定义供应商未添加前不出现；点击 `+` 后必须在设置页内展开可填写表单，不依赖浏览器原生 prompt（提示框）；自定义供应商添加成功后刷新 provider（供应商）列表并自动切换到新供应商；供应商下拉标签显示短名称 `OpenAI GPT`、`Claude`、`DeepSeek`、`Xiaomi MiMo`，不追加括号说明。配置立即保存到后端模型配置；下次发送消息使用新的 provider（供应商）、model（模型）和当前模型的原生 thinking level（思考等级）。没有原生档位且未自定义添加档位的模型不显示思考等级选择。 |
| 证明方式 | 自动测试 `py -m pytest try/test_model_config.py try/test_provider_reasoning.py -q`；前端构建命令 `npm run build` 通过；Browser（浏览器）验证设置页 `+` 展开自定义供应商表单、字段可输入、取消可收起且无前端 error/warn（错误/警告）日志；Browser（浏览器）使用真实 DeepSeek（深度求索）API Key（接口密钥）验证模型配置保存、快捷栏出现和一次学习请求返回；人工确认聊天栏只显示已配置密钥供应商，设置页可新增当前模型思考等级。 |
| 结果记录 | 2026-06-30 模型配置、供应商调用和启动脚本自动测试通过；启动脚本健康检查通过；前端构建通过。2026-06-30 修复自定义供应商 `+` 点击无效：`try/test_model_config.py` 覆盖 API（接口）返回新 provider（供应商）和刷新列表；Browser（浏览器）验证表单展开、输入和取消通过；为避免写入用户真实配置，浏览器未提交测试供应商。2026-06-30 使用本地 DeepSeek（深度求索）API Key（接口密钥）浏览器联调通过：后端配置显示 `provider_id=deepseek`、`model=deepseek-chat`、`has_api_key=true`，聊天栏显示 DeepSeek（深度求索）/`deepseek-chat`，发送学习请求后返回并入库 12 道题。 |
| 反馈/问题 | 原生参数按模型配置写入：OpenAI-compatible（OpenAI 兼容）使用 `reasoning_effort`（推理强度），DeepSeek V4 使用 `thinking` + `reasoning_effort`，Claude 使用 Anthropic Messages（Anthropic 消息接口）的 `thinking` 和 `output_config.effort`，MiMo 使用 Anthropic Messages 的开关式 `thinking`。OpenAI/GPT 官方 Chat Completions（聊天补全）可发送 `developer` role（角色）；DeepSeek（深度求索）、本地模型和自定义 OpenAI-compatible（OpenAI 兼容）供应商只发送兼容的 `system`/`user` 消息，把上下文并入 user 内容，避免 400。API Key（接口密钥）保存/读取和启动脚本保留密钥时会清理常见 `apikey:` / `Bearer:` 粘贴前缀，非法请求头字符必须返回可读错误。禁止用提示词模拟思考等级。 |
| 最终结论 | 待定。 |

## 8. UI（用户界面）与会话交互回归

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开 Web（网页）首页，检查顶部模型栏、左下角设置按钮、长期学习总面板；点击“新建聊天”但不输入内容；重复点击“新建聊天”；发送第一条消息并模拟模型 API（接口）失败；右键会话和消息；开启分支并发送分支追问；在设置的“考试”页填写考试时间。 |
| 预期结果 | 顶部模型栏和设置/初始化供应商下拉不显示 Mock Provider（本地模拟供应商）；设置按钮为克制的侧栏工具按钮且带“设置”文字；长期面板显示题目完成/总数、单词掌握/总数、整体正确率、考试倒计时和 token（令牌）累计；未输入的新聊天会在当前本地日期文件夹下显示一个“新聊天”草稿，重复点击只保留一个草稿；发送首条消息后草稿替换为真实会话；模型 API（接口）失败时仍保留用户消息和助手错误提示，会话列表可见；会话右键可删除；消息右键可复制、重新编辑、开启分支；分支创建和分支追问等待期间显示“分支正在思考”loading（加载）动效；分支消息列表超出高度后可用滚轮在右侧消息区内滚动，底部输入框不挤出视口；考试时间保存后刷新仍存在并驱动倒计时。 |
| 证明方式 | 自动测试 `py -m pytest try/test_chat_error_persistence.py -q` 覆盖模型失败时会话保留；自动测试 `py -m pytest try -q` 覆盖会话按考试隔离；前端构建 `cd frontend && npm run build` 通过；Browser（浏览器）插件验收新聊天草稿、右键菜单、倒计时视觉、设置按钮可见性、分支 loading（加载）动效和分支消息滚轮滚动。 |
| 结果记录 | 2026-06-30 自动测试通过；Browser（浏览器）验收确认长期面板、草稿新聊、消息右键菜单、重新编辑、分支面板、设置考试时间保存与刷新持久化；桌面和移动折叠侧栏视口无前端错误与横向溢出。2026-06-30 修复分支会话等待反馈和滚动：前端构建通过；Browser（浏览器）插件真实页面验收通过：右键消息创建分支后消息区 `overflow-y=auto`、长内容 `scrollHeight > clientHeight`、发送分支追问时“分支正在思考”可见，发送后和回复完成后 `scrollTop` 不被自动改到底部，手动滚轮可滚动。 |
| 反馈/问题 | 自动化发现 `datetime-local`（日期时间输入）填入后 React（前端框架）状态未更新，已补 `onInput` 事件并复测通过。2026-06-30 分支面板原先没有分支级发送状态，且消息直接堆在工作台面板中，长内容时右侧无法稳定滚轮查看底部；已改为分支级 loading（加载）状态和独立可滚动消息区。 |
| 最终结论 | 自动构建与 Browser（浏览器）插件验收通过。 |

## 9. 截图词表自动练习与考试式题型

| 项 | 内容 |
|---|---|
| 验收步骤 | 在右侧工作台打开“截图导入”，粘贴手机词表 OCR（文字识别）文本，点击“导入并开始练习”。 |
| 预期结果 | 系统自动创建截图词表练习会话，写入本次词表，生成完整题组并直接显示第 1 题；不需要用户再输入“请出题”。题干必须是英文语境句、完形空格、阅读语境问题或同义改写；不得出现“选择中文释义 / 最合适理解”的词卡题；英语考试选项不得混入中文释义、日文字符、调试词或旧会话词汇。 |
| 证明方式 | 自动测试 `py -m pytest try/test_screenshot_import_workflow.py try/test_formal_drill_flow.py try/test_answer_submission.py -q`；Browser（浏览器）真实点击“导入并开始练习”后检查题卡、选项、控制台日志；数据库中当前截图会话存在多条 `questions` 记录。 |
| 结果记录 | 2026-06-30 自动测试通过；Browser（浏览器）实测导入 12 个词后自动生成 12 题，第 1 题为 `The police report said the ______ on the icy road...`，选项只来自本次词表，提交后自动进入第 2 题。 |
| 反馈/问题 | 当前仍依赖外部 OCR（文字识别）文本输入；后续如接入本地 OCR 或截图选择器，需要补充文件/图片输入验收。2026-06-30 修复主聊天 `真实联调：...` 这类中文冒号前缀被误当作英语词条并进入选项的问题。 |
| 最终结论 | 自动与浏览器验收通过。 |

## 10. 快速开始、主聊天截图导入与细分等待状态

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开 Web（网页）首页；当前查看的会话/日期面板没有导入或题组时点击长期学习总面板底部按钮；再把多行截图词表直接粘贴到主聊天栏发送；切到其他日期文件夹下已导入或已有题组的会话后再次查看长期学习总面板底部按钮。 |
| 预期结果 | 当前面板无内容时按钮显示“当日导入”，点击后打开右侧工作台并切到“截图导入”，长期总面板底部出现“当前题库为空，右侧已打开截图导入。”本地提示；按钮内部不得出现统计标签样式造成的“按钮套按钮”视觉；主聊天粘贴 3 个以上词条的截图词表时，后端复用截图导入流程创建独立截图练习会话、导入词表并生成完整考试式题组；等待过程能显示“截图解析中”和“题目生成中”；当前面板已有导入或题组时按钮显示“快速开始”，点击后发送“继续当前题组”。 |
| 证明方式 | 自动测试 `py -m pytest try/test_context_and_inline_screenshot.py -q` 覆盖主聊天截图词表复用导入流程；前端构建 `cd frontend && npm run build` 通过；Browser（浏览器）验证空状态打开截图导入、总面板提示、按钮单层样式、主聊天导入、细分等待状态、已有内容快速开始和移动视口。 |
| 结果记录 | 2026-07-01 前端构建通过；Browser（浏览器）临时空库验证通过：空状态只按当天 `daily_panel` 判断，不再被长期总统计误判为已有今日内容；按钮显示“当日导入”并打开右侧截图导入，同时在长期总面板显示本地提示。2026-07-01 修复跨日期会话判断并完成 Browser（浏览器）验证：切到 2026-06-30 下已有导入或题组的“截图词表练习：collision”会话时，左侧状态为 18/96 题、20/72 当日词汇，按钮按当前 `daily_panel` 显示“快速开始”，`aria-label` 为“继续当前题组”，不再因为日期不是今天而显示“当日导入”，且没有残留导入提示。2026-06-30 Browser（浏览器）隔离库验收通过：空状态按钮显示“导入截图”并打开右侧截图导入；主聊天粘贴 `word n. 释义` 格式导入 8 个词并生成 8 题；右侧粘贴 `word: 释义` 格式显示“截图解析中”“题目生成中”，导入 5 个词并生成 5 题；刷新后按钮保持“快速开始”，点击发送“继续今天的答题”并恢复题卡；移动 390x844 折叠侧栏后无横向溢出。 |
| 反馈/问题 | Browser（浏览器）验收发现主聊天和右侧导入原先漏识别 `word n. 释义` / `word: 释义` 格式，已扩展 `ScreenshotImportService` 与前端识别函数；单个词条仍不按截图导入处理，避免误判。 |
| 最终结论 | 自动与 Browser（浏览器）验收通过。 |

## 11. 答题模型讲解、上下文容量与 token（令牌）使用统计

| 项 | 内容 |
|---|---|
| 验收步骤 | 在题目卡选择答案并提交；观察聊天输入框发送按钮左侧上下文圆圈；悬浮圆圈查看上下文占用并点击“压缩上下文”；进入设置的“令牌”页修改上下文容量上限。 |
| 预期结果 | 答题后 Evaluator Tutor（判题讲解 Agent）调用模型生成讲解，讲解基于程序判定、用户背景和会话上下文；模型失败时保留基础判题和作答记录；讲解消息同时展示上一题回顾卡片；上下文圆圈按默认 1,000,000 或用户设置上限显示当前会话占用；悬浮弹窗展示当前会话上下文 token（令牌）、消息数、压缩摘要和压缩按钮；主聊天与分支消息中的基础 Markdown（标记语言）能显示为加粗、内联代码、列表、标题和代码块；设置页展示累计 token（令牌）、会话数、消息数、活跃天数、连续天数、最常用模型、近 30 天活动和模型分布。 |
| 证明方式 | 自动测试 `py -m pytest try/test_answer_submission.py try/test_context_and_inline_screenshot.py -q`；前端构建 `cd frontend && npm run build`；Browser（浏览器）插件确认上下文弹窗消息数和 Markdown（标记语言）DOM（文档对象模型）节点；人工确认已答题回顾卡片、圆圈悬浮弹窗和设置页统计视觉。 |
| 结果记录 | 2026-06-30 答题模型调用、上下文容量设置和主动压缩自动测试通过；Browser（浏览器）验收确认“模型正在判题”、答题后自动进入下一题、上下文圆圈弹窗、主动压缩、设置页使用统计、上下文容量改为 123456 后圆圈显示 `12.3万` 上限。2026-07-01 修复聊天响应上下文容量按全局统计返回导致当前会话显示 0 的问题；自动测试覆盖普通聊天响应 `context_messages=2` 且 `estimated_current_context>0`；Browser（浏览器）验证已有会话弹窗显示 `3,467 / 100.0万`、消息数 18，主聊天 Markdown（标记语言）生成 `strong` 和 `code` 节点，气泡文本不再残留原始 `**`。 |
| 反馈/问题 | Browser（浏览器）验收发现模型返回空 JSON（数据格式）时曾直接显示 `{}`，已改为提取 `message` / `feedback` / `explanation` 字段或回退基础讲解；LLMLingua（提示词压缩库）为可选增强，默认使用本地抽取式摘要。2026-07-01 发现 `chat`（聊天）响应未传当前 `session_id` 给 `token_totals()`，已改为当前会话口径。 |
| 最终结论 | 自动与 Browser（浏览器）验收通过；真实模型讲解质量仍建议按日常学习人工抽查。 |

## 12. 历年真题试卷参考与题型勾选

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开 Web（网页）设置 -> 考纲；查看“历年真题与题型”；勾选/取消真题试卷；填写试卷标题、年份、来源网站、本地路径或粘贴试卷文本、题型和摘要后点击“手动导入试卷”；点击“重新解析”；点击“大模型联网搜索导入”；勾选/取消题型；发送学习内容触发组卷。 |
| 预期结果 | 默认显示并选择当前考试近 3 年真题试卷，且按考试类型生成 `papers/<考试>/raw` 与 `papers/<考试>/parsed` 文件；用户选择会持久化；手动导入的试卷会复制或写入 raw（原始文件）目录并生成 parsed（解析结果）JSON（JSON 数据交换格式）；导入/解析出的题型进入可勾选列表；联网搜索导入创建近 3 年可核验来源和解析占位；组卷阶段 `past_paper_context` 包含当前选中试卷、来源网站、解析摘要、章节、短摘录和已勾选题型，生成题目的 `source_refs` 记录真题来源边界；未勾选题型不进入生成要求。 |
| 证明方式 | 自动测试 `py -m pytest try/test_past_papers.py -q`；前端构建 `cd frontend && npm run build`；人工确认设置页真题列表、导入表单、来源网站链接、重新解析按钮、原始/解析路径显示和题型勾选交互。 |
| 结果记录 | 2026-06-30 自动测试通过，前端构建通过，启动脚本和 HTTP（超文本传输协议）健康检查通过；设置页人工点击待确认。 |
| 反馈/问题 | 默认发布资产只包含目录骨架和可核验来源清单，不提交版权不明的完整真题；用户本地导入后可作为真实组卷参考。联网搜索导入按钮在本地版本创建待核验来源和解析占位，完整原文仍需用户按版权边界导入或粘贴已提取文本。 |
| 最终结论 | 自动验收通过，人工设置页操作待确认。 |

## 13. 普通聊天与组卷意图区分

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开 Web（网页）首页或新建聊天；发送“你好”；再发送“我应该怎么安排四级复习计划？”；最后发送明确练题请求，例如“请给我出 12 道四级词汇题”或 `collision: 碰撞；冲突`。 |
| 预期结果 | “你好”和学习建议咨询进入普通聊天，不显示“题目生成中”，不新增 `questions`（题目）记录，不把寒暄算入当日题目/词汇；若当前已有题卡，题卡保持但不被重置；明确练题请求仍进入正式组卷并先入库题组。 |
| 证明方式 | 自动测试 `py -m pytest try/test_chat_intent_routing.py try/test_chat_error_persistence.py try/test_formal_drill_flow.py -q`；前端构建 `cd frontend && npm run build` 通过；Browser（浏览器）人工验证“你好”loading（加载）文案为普通思考/回复状态且不生成题。 |
| 结果记录 | 2026-07-01 自动测试通过：`你好` 路由为 `general_chat`，API（接口）返回普通问候且 `questions` 与 `model_calls` 均为 0；“我应该怎么安排四级复习计划？”和“怎么练四级听力？”这类学习建议咨询不组卷；学习建议咨询在模型失败时仍保留会话和错误提示；显式出题、`今天练 CET-4 高频词汇` 和词表定义仍路由到 `daily_drill`。前端构建通过。Browser（浏览器）验证通过：发送“你好”后显示普通问候回复，不出现“题目生成中”，不显示题卡；长期统计发送前后均为 31/135 题、13/51 词；测试新增会话已删除。 |
| 反馈/问题 | 当前真实库里 2026-07-01 已存在此前误生成的 0/12 当日聚合数据，本轮修复不自动删除历史题组，避免误删用户数据；如需清理可后续单独做带确认的删除流程。 |
| 最终结论 | 自动与 Browser（浏览器）验收通过。 |

## 14. 回归命令

| 命令 | 预期结果 |
|---|---|
| `py -m pytest try -q` | 所有后端测试通过。 |
| `py -m ruff check backend try` | Python（编程语言）代码检查通过。 |
| `py -m pytest try/test_startup_scripts.py -q` | 启动脚本结构回归测试通过。 |
| `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall` | 后端和前端后台启动，HTTP（超文本传输协议）健康检查通过。 |
| `cd frontend && npm run build` | TypeScript（类型化 JavaScript）和 Vite（前端构建工具）构建通过。 |

## 最终结论

- 当前结论：2026-06-30 历年真题试卷参考已扩展为原始试卷与解析产物资产流；自动测试、前端构建和启动验收通过。真题设置页人工点击和真实模型按真题解析结果生成质量仍建议按日常学习人工抽查。
