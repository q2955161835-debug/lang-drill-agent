# Lang Drill Agent 验收标准

## 测试环境

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10.0.26200 x64 或同等 Windows（视窗系统）环境 |
| 后端 | FastAPI（Web API 框架）开发服务 `http://127.0.0.1:8000` |
| 前端 | Vite（前端构建工具）开发服务 `http://127.0.0.1:5173` |
| 数据库 | SQLite（轻量数据库），正式学习状态以数据库为唯一来源 |
| 模型 | 普通 Web（网页）模式默认真实 provider（供应商）MiMo（小米米魔）；mock（本地模拟）provider 只用于自动测试或离线调试 |

## 1. 一键启动体验

| 项 | 内容 |
|---|---|
| 验收步骤 | 双击 `start.bat`。 |
| 预期结果 | `start.bat` 调用 `scripts/dev/start-dev.ps1`，后台启动后端和前端；只有 `http://127.0.0.1:8000/docs` 与 `http://127.0.0.1:5173` 都可访问后才打开浏览器；不额外留下后端/前端可见终端。 |
| 证明方式 | 自动测试 `py -m pytest try/test_startup_scripts.py -q`；运行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall` 返回 0；`Invoke-WebRequest http://127.0.0.1:5173` 与 `Invoke-WebRequest http://127.0.0.1:8000/docs` 返回 200；检查 `logs/langdrill-backend.out.log`、`logs/langdrill-backend.err.log`、`logs/langdrill-frontend.out.log`、`logs/langdrill-frontend.err.log`；运行 `stop.bat` 后 5173 / 8000 端口释放。 |
| 结果记录 | 2026-06-30 `start-dev.ps1 -NoBrowser -SkipInstall` 自动验证通过，5173 / 8000 返回 200；脚本已覆盖 API Key（接口密钥）前缀规范化；双击体验待人工确认。 |
| 反馈/问题 | 如服务失败，启动器必须输出错误并展示对应日志尾部，不能在未监听端口时直接打开浏览器；PowerShell（命令行脚本）内识别中文全角冒号必须使用编码安全的正则转义。 |
| 最终结论 | 自动验收通过，人工双击待确认。 |

## 2. 发送消息 thinking（思考）加载

| 项 | 内容 |
|---|---|
| 验收步骤 | 在 Web（网页）输入学习内容并发送，等待模型或后端响应。 |
| 预期结果 | 发送后聊天流末尾立即出现“模型正在思考”的 loading（加载）气泡和动效；后端返回后该气泡消失并显示正式回复。 |
| 证明方式 | 人工观察 UI（用户界面）；前端构建命令 `npm run build` 通过。 |
| 结果记录 | 待人工确认。 |
| 反馈/问题 | 若网络慢或模型慢，气泡必须持续可见，不能让页面无反馈。 |
| 最终结论 | 待定。 |

## 3. 正式题组先入库再展示

| 项 | 内容 |
|---|---|
| 验收步骤 | 输入一组词汇或学习请求，例如 `blood: n. 血液`，触发日常训练。 |
| 预期结果 | 后端先生成一整套题并写入 `questions` 表，再只返回当前第 1 题；回复不得固定为“已初始化今日学习面板，并准备好第一题”。 |
| 证明方式 | 自动测试 `py -m pytest try/test_formal_drill_flow.py -q`；数据库中同一 `session_id` 下存在多条 `questions` 记录。 |
| 结果记录 | 待自动验证。 |
| 反馈/问题 | 真实模型不可用时允许 fallback（兜底）题组，但仍必须多题入库。 |
| 最终结论 | 待定。 |

## 4. 答题后自动进入下一题

| 项 | 内容 |
|---|---|
| 验收步骤 | 对当前题选择 A/B/C/D 并提交。 |
| 预期结果 | 系统先判题和讲解，再自动返回下一道待答题；用户无需输入“下一题”。答题响应必须包含上一题 `answered_question`（已答题快照），助手消息 payload（附加数据）也必须保存该快照，供前端回顾。 |
| 证明方式 | 自动测试 `py -m pytest try/test_formal_drill_flow.py try/test_answer_submission.py -q`；`attempts` 表新增作答记录；上一题状态为 `answered`，下一题状态为 `ready`；历史会话详情能读回上一题 payload（附加数据）。 |
| 结果记录 | 2026-06-30 自动测试通过，答题响应和历史会话消息均包含已答题快照。 |
| 反馈/问题 | 最后一题答完时应提示本轮完成和总结入口，不应重新生成第一题。 |
| 最终结论 | 待定。 |

## 5. 显式“下一题”不重置

| 项 | 内容 |
|---|---|
| 验收步骤 | 当前题组进行中时，在聊天框输入“下一题”或“继续”。 |
| 预期结果 | 后端只读取当前题组待答题，不重新初始化今日学习面板，不重复生成新题组。 |
| 证明方式 | 自动测试 `py -m pytest try/test_formal_drill_flow.py -q`；回复包含“不重新开始”，且 `active_question.id` 保持为当前待答题。 |
| 结果记录 | 待自动验证。 |
| 反馈/问题 | 如果当前题组已完成，应提示输入新学习内容或查看总结。 |
| 最终结论 | 待定。 |

## 6. 题目显示位置

| 项 | 内容 |
|---|---|
| 验收步骤 | 发送学习内容、答题、等待下一题出现。 |
| 预期结果 | 当前待答题卡片跟随最新助手反馈显示在聊天流底部附近，并自动滚动到可见区域；提交答案后，上一题不直接消失，而是在助手讲解消息中显示为普通已答题回顾卡片，标出用户选择、正确答案和对错状态；回顾卡片不使用 sticky（吸附定位）。 |
| 证明方式 | 自动测试 `py -m pytest try/test_answer_submission.py try/test_formal_drill_flow.py -q`；前端构建命令 `cd frontend && npm run build` 通过；人工确认 UI（用户界面）中当前题吸附、已答题回顾卡片随聊天流滚动。 |
| 结果记录 | 2026-06-30 自动测试和前端构建通过；已答题回顾卡片的最终视觉仍待人工确认。 |
| 反馈/问题 | 不应出现题目卡片在顶部、用户以为没有下一题的情况；已答题卡片不能带提交控件，不能继续置顶遮挡下一题。 |
| 最终结论 | 待定。 |

## 7. 聊天栏模型与思考等级

| 项 | 内容 |
|---|---|
| 验收步骤 | 在设置中配置 OpenAI/GPT、Claude、DeepSeek（深度求索）或 MiMo（小米米魔）的 Base URL（基础网址）、API 格式和 API Key（接口密钥），点击模型列表旁刷新按钮获取可调用模型，再在聊天栏上方切换模型供应商、模型名称和思考等级；点击模型供应商旁 `+`，填写供应商名称、Base URL（基础网址）和默认模型后点击“添加供应商”；在当前供应商下点击“添加自定义模型”，填写模型 ID、显示名称、上下文容量和视觉能力后保存，再删除该自定义模型；隐藏某个模型后查看聊天栏模型下拉，再重新显示；对当前模型点击“添加自定义思考等级”后保存，再删除该自定义档位；在模型页切换“当前模型支持图片输入”；填写或清除 MinerU token；在模型页修改上下文容量上限。 |
| 预期结果 | 未配置 API Key（接口密钥）的真实供应商不出现在聊天栏快捷模型选择；自定义供应商未添加前不出现；点击 `+` 后必须在设置页内展开可填写表单，不依赖浏览器原生 prompt（提示框）；自定义供应商添加成功后刷新 provider（供应商）列表并自动切换到新供应商；供应商下拉标签显示短名称 `OpenAI GPT`、`Claude`、`DeepSeek`、`Xiaomi MiMo`，不追加括号说明。模型列表刷新会调用供应商模型列表 API（接口），把返回的可调用模型加入模型下拉，默认全部显示；若供应商模型列表接口返回 404/405/501，应保留现有模型列表并提示“未开放模型列表接口”，不得显示 `<html>`、`openresty` 或其它原始 HTML（超文本标记语言）页面内容；设置页允许参考 opencode（开源代码 Agent）的 provider/models（供应商/模型）配置方式，在当前供应商下添加自定义模型，填写模型 ID、显示名称、上下文容量和是否支持图片；自定义模型默认显示在聊天栏，可隐藏/显示并允许删除；内置模型和 API 返回模型不可删除；隐藏模型后聊天栏快捷模型选择不再显示该模型，设置页仍可重新显示。配置立即保存到后端模型配置；下次发送消息使用新的 provider（供应商）、model（模型）、API 格式、当前模型的原生 thinking level（思考等级）和视觉能力声明。切换模型后思考等级必须按当前模型的 `reasoning.levels`（推理档位）刷新，例如 OpenAI/GPT 显示低/中/高/极高类档位，DeepSeek（深度求索）显示关闭/高/最高，MiMo（小米米魔）显示关闭/开启；没有原生档位且未自定义添加档位的模型不显示思考等级选择；有思考等级时只显示一个思考等级选择器，不额外显示“当前：开启”这类重复状态标签。设置页新增的自定义思考等级必须带删除入口；内置原生档位不得显示删除入口；删除当前选中的自定义档位后回退到当前模型默认档位。设置页必须提供“权限”页签，默认开启项必须实际勾选并包含截图导入、学习数据库、历年真题草稿、联网功能、考试目标和上下文容量；Skills（技能）在“默认关闭的扩展权限”中且默认不勾选；敏感设置权限集中在下方并默认关闭，包含模型配置、配置自定义模型、API Key（接口密钥）、MinerU token、数据迁移和试卷保存，相关保存动作仍必须由用户确认。设置页必须提供 Skills 页签，显示本地技能根目录、已安装技能和单个 Skill 开启/关闭开关。MinerU token 只写入 `.env` 的 `MINERU_TOKEN`，接口只返回是否已配置、脱敏预览、官方 token 获取地址和 API 文档地址，不返回明文 token。 |
| 证明方式 | 自动测试 `py -m pytest try/test_model_config.py try/test_provider_reasoning.py try/test_agent_settings_permissions.py -q` 覆盖模型刷新成功、接口不开放时保留现有模型、错误提示不泄露 HTML（超文本标记语言）、自定义模型增删和 Agent 自定义模型草稿权限；前端构建命令 `npm run build` 通过；Browser（浏览器）验证设置页 `+` 展开自定义供应商表单、字段可输入、取消可收起、API 格式下拉、模型刷新按钮、模型显示/隐藏、自定义模型添加/删除、自定义思考等级表单、删除自定义档位、切换模型刷新档位、视觉能力开关、上下文容量保存、权限分组、Skills 页签、MinerU token 保存/清除和无前端 error/warn（错误/警告）日志；Browser（浏览器）使用真实 DeepSeek（深度求索）API Key（接口密钥）验证模型列表刷新、模型配置保存、快捷栏出现和一次学习请求返回；人工确认聊天栏只显示已配置密钥供应商和未隐藏模型，设置页可新增和删除当前模型自定义思考等级。 |
| 结果记录 | 2026-06-30 模型配置、供应商调用和启动脚本自动测试通过；启动脚本健康检查通过；前端构建通过。2026-06-30 修复自定义供应商 `+` 点击无效：`try/test_model_config.py` 覆盖 API（接口）返回新 provider（供应商）和刷新列表；Browser（浏览器）验证表单展开、输入和取消通过；为避免写入用户真实配置，浏览器未提交测试供应商。2026-06-30 使用本地 DeepSeek（深度求索）API Key（接口密钥）浏览器联调通过：后端配置显示 `provider_id=deepseek`、`model=deepseek-chat`、`has_api_key=true`，聊天栏显示 DeepSeek（深度求索）/`deepseek-chat`，发送学习请求后返回并入库 12 道题。2026-07-01 删除聊天栏多余“当前：开启”重复思考状态标签，保留“思考：开启”选择器作为唯一思考等级入口；前端构建通过。2026-07-01 恢复 Agent 设置权限页签和后端权限账本；自动测试覆盖默认关闭、保存去重、未授权阻止试卷设置动作、授权后返回 `settings_action` 草稿；前端构建通过。2026-07-01 按模型能力表驱动思考档位，新增设置页自定义思考等级表单和自定义档位删除；自动测试覆盖自定义档位保存与删除后原生档位保留，前端构建通过。2026-07-02 新增供应商模型列表刷新、模型显示/隐藏、API 格式选项和移除手填自定义模型入口；自动测试覆盖 API 返回模型写入、原生思考档位保留和模型隐藏；前端构建通过。2026-07-02 将 Agent 权限调整为非敏感默认开启、敏感默认关闭并新增 Skills 页签；自动测试覆盖默认权限、Skills 状态、截图导入权限拦截和联网搜索权限拦截。2026-07-02 修正权限默认状态：联网功能默认开启且勾选，Skills 默认关闭且不勾选；新增旧权限记录迁移和单个 Skill 开关测试。2026-07-02 修复 MiMo 等供应商未开放模型列表接口时的刷新报错：自动测试覆盖 404 HTML（超文本标记语言）返回时保留 `mimo-v2.5` / `mimo-v2.5-pro`，以及 500 错误不泄露 HTML 正文。2026-07-02 恢复当前供应商下显式添加自定义模型能力：自动测试覆盖自定义模型添加、删除、禁止删除内置模型和 Agent 自定义模型草稿权限；前端构建通过。 |
| 反馈/问题 | 原生参数按模型配置写入：OpenAI-compatible（OpenAI 兼容）使用 `reasoning_effort`（推理强度），DeepSeek V4 使用 `thinking` + `reasoning_effort`，Claude 使用 Anthropic Messages（Anthropic 消息接口）的 `thinking` 和 `output_config.effort`，MiMo 使用 Anthropic Messages 的开关式 `thinking`。OpenAI/GPT 官方 Chat Completions（聊天补全）可发送 `developer` role（角色）；DeepSeek（深度求索）、本地模型和自定义 OpenAI-compatible（OpenAI 兼容）供应商只发送兼容的 `system`/`user` 消息，把上下文并入 user 内容，避免 400。API Key（接口密钥）和 MinerU token 保存/读取时会清理常见 `apikey:` / `Bearer:` 粘贴前缀，非法请求头字符必须返回可读错误。禁止用提示词模拟思考等级。 |
| 最终结论 | 待定。 |

2026-07-02 补充验收记录：Skills 页签必须分开展示内置联网检索和本地 Skills。内置联网检索默认开启、无需 API Key/token，且只受“联网功能”权限控制；启用或关闭 Multi Search Engine（多搜索引擎）本地 Skill 不得改变内置联网检索状态。本轮已通过 `py -m pytest try -q`、`py -m ruff check backend try` 和 `cd frontend && npm run build` 验证；人工仍需在浏览器中确认 Skills 页视觉展示。

## 8. UI（用户界面）与会话交互回归

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开 Web（网页）首页，检查顶部模型栏、左下角设置按钮、长期学习总面板；点击“新建聊天”但不输入内容；重复点击“新建聊天”；发送第一条消息并模拟模型 API（接口）失败；右键会话和消息；开启分支并发送分支追问；在设置的“考试”页填写考试时间。 |
| 预期结果 | 顶部模型栏和设置/初始化供应商下拉不显示 Mock Provider（本地模拟供应商）；设置按钮为克制的侧栏工具按钮且带“设置”文字；长期面板显示题目完成/总数、单词掌握/总数、整体正确率、考试倒计时和 token（令牌）累计；左右栏边界可用鼠标拖拽或键盘左右键调整宽度，刷新后保留宽度；未输入的新聊天会在当前本地日期文件夹下显示一个“新聊天”草稿，重复点击只保留一个草稿；发送首条消息后草稿替换为真实会话；模型 API（接口）失败时仍保留用户消息和助手错误提示，会话列表可见；会话右键可删除；消息右键可复制、重新编辑、开启分支；分支创建和分支追问等待期间显示“分支正在思考”loading（加载）动效；分支消息列表超出高度后可用滚轮在右侧消息区内滚动，底部输入框不挤出视口；考试时间保存后刷新仍存在并驱动倒计时。 |
| 证明方式 | 自动测试 `py -m pytest try/test_chat_error_persistence.py -q` 覆盖模型失败时会话保留；自动测试 `py -m pytest try -q` 覆盖会话按考试隔离；前端构建 `cd frontend && npm run build` 通过；Browser（浏览器）插件验收新聊天草稿、右键菜单、倒计时视觉、设置按钮可见性、分支 loading（加载）动效和分支消息滚轮滚动。 |
| 结果记录 | 2026-06-30 自动测试通过；Browser（浏览器）验收确认长期面板、草稿新聊、消息右键菜单、重新编辑、分支面板、设置考试时间保存与刷新持久化；桌面和移动折叠侧栏视口无前端错误与横向溢出。2026-06-30 修复分支会话等待反馈和滚动：前端构建通过；Browser（浏览器）插件真实页面验收通过：右键消息创建分支后消息区 `overflow-y=auto`、长内容 `scrollHeight > clientHeight`、发送分支追问时“分支正在思考”可见，发送后和回复完成后 `scrollTop` 不被自动改到底部，手动滚轮可滚动。2026-07-01 恢复三栏边界拖拽和本地宽度持久化；前端构建通过，真实拖拽视觉仍需 Browser（浏览器）人工确认。2026-07-01 修复右侧工作台收起状态内容露出：补充 `workbench-panel[hidden]` 显式隐藏规则，前端构建通过；Playwright（浏览器自动化工具）复用 Microsoft Edge（微软 Edge 浏览器）截图确认右侧收起后不再露出工作台标题、页签或正文，只保留展开按钮。2026-07-01 新增深色主题生成背景图：`frontend/public/assets/dark-theme-bg.jpg` 约 123KB，深色主题 `body` 使用该图和轻微压暗遮罩；前端构建通过。2026-07-01 发现前次视觉验收漏判：背景纹理只覆盖上半屏，下半屏仍出现纯黑断层；已把同一背景应用到实际占满中间视口的 `.chat-main`，并把 `.composer-wrap` 底部遮罩改为半透明渐变；Playwright（浏览器自动化工具）加载暗色偏好重新截图确认中间区域从顶部到输入框后方连续显示低对比纹理，无横向纯黑断层，面板文字和按钮仍可读。 |
| 反馈/问题 | 自动化发现 `datetime-local`（日期时间输入）填入后 React（前端框架）状态未更新，已补 `onInput` 事件并复测通过。2026-06-30 分支面板原先没有分支级发送状态，且消息直接堆在工作台面板中，长内容时右侧无法稳定滚轮查看底部；已改为分支级 loading（加载）状态和独立可滚动消息区。 |
| 最终结论 | 自动构建与 Browser（浏览器）插件验收通过。 |

## 9. 截图词表自动练习与考试式题型

| 项 | 内容 |
|---|---|
| 验收步骤 | 在右侧工作台打开“截图导入”；点击“选择文件”并在本机文件管理器中选择多张 TXT（纯文本格式）/Markdown（Markdown 文本格式）/PDF（Portable Document Format，便携式文档格式）/DOCX（Word 文档格式）/图片文件，或一次/多次拖入同类文件，确认它们进入待解析文件列表且未立即读取；折叠右侧栏、重新展开、切换到分支页签再切回截图导入；点击“解析文本”；也可直接粘贴手机词表 OCR（文字识别）文本；确认文本后点击“导入并开始练习”。 |
| 预期结果 | 选择或拖入文件后只显示待解析文件列表、文件名和移除/清空控件，不自动调用后端抽取接口，也不立即填入截图识别文本框；右侧栏折叠/展开和工作台页签切换不得丢失待解析文件、解析中状态、已识别文本或预览结果；点击“解析文本”后才统一抽取并把内容填入截图识别文本框。拖入图片时后端优先尝试 MinerU CLI（MinerU 命令行工具）OCR（文字识别），配置 `MINERU_TOKEN` 时使用精准解析，未配置时使用轻量解析，失败后自动回退 RapidOCR（本地文字识别），全部失败时返回可读错误而不是 `Request failed: 500`；手机背词截图 OCR 文本中的状态栏、词书标题、排序文案、底部导航等 UI（User Interface，用户界面）噪声不得导入为词条或混入释义；能唯一匹配的截断英文词可修复后导入，缺少释义的词条必须跳过并返回诊断信息；待解析文件未解析前“导入并开始练习”保持禁用，解析完成后系统自动创建截图词表练习会话，写入本次词表，生成完整题组并直接显示第 1 题；不需要用户再输入“请出题”。题干必须是英文语境句、完形空格、阅读语境问题或同义改写；不得出现“选择中文释义 / 最合适理解”的词卡题；英语考试选项不得混入中文释义、日文字符、调试词或旧会话词汇。 |
| 证明方式 | 自动测试 `py -m pytest try/test_screenshot_import_workflow.py try/test_past_papers.py try/test_formal_drill_flow.py try/test_answer_submission.py -q`；Browser（浏览器）真实点击“选择文件”和拖入多个文件，检查选取/拖入后无抽取请求、待解析列表展示正确、点击“解析文本”后才出现抽取请求并回填文本，再点击“导入并开始练习”后检查题卡、选项、控制台日志；数据库中当前截图会话存在多条 `questions` 记录。 |
| 结果记录 | 2026-06-30 自动测试通过；Browser（浏览器）实测导入 12 个词后自动生成 12 题，第 1 题为 `The police report said the ______ on the icy road...`，选项只来自本次词表，提交后自动进入第 2 题。2026-07-01 新增后端文件文本抽取接口自动测试，TXT（纯文本格式）上传可返回抽取文本。2026-07-01 修复图片拖拽 500：真实截图拖入右侧截图导入区后，MinerU CLI（MinerU 命令行工具）远程 EOF 失败自动回退 RapidOCR（本地文字识别），文本框回填 217 个字符并包含“全国大学法语四级考试大纲(2023版)”，页面无 `Request failed: 500`，控制台无 error/warn（错误/警告）。2026-07-01 修复右侧截图导入多文件队列：Browser（浏览器）验收模拟拖入 `words-a.txt` 和 `words-b.txt` 后，`/api/files/extract-text` 请求数为 0，待解析列表显示 2 个文件、截图识别文本框为空、“导入并开始练习”禁用；点击“解析文本”后才出现 2 次文件抽取和 1 次截图解析，文本框回填 4 个词，预览显示 `book/chair/skin/root`，导入按钮启用，控制台无 error/warn（错误/警告）。2026-07-01 修复手机词书截图噪声解析：自动测试覆盖含状态栏、`共4440词`、底部 `abc/速听/单词选义/速刷/拼写/听写` 和截断 `altoge.` 的 OCR（文字识别）文本，解析结果为 11 个词，`class` 释义不混入导航文案，`altoge.` 修复为 `altogether`，无释义 `forever` 被跳过并返回诊断信息。2026-07-01 右侧截图导入新增“选择文件”入口，选择后的文件复用待解析队列；前端构建通过，文件管理器弹出需人工确认。2026-07-01 恢复右侧工作台隐藏但不卸载，折叠和页签切换不会清空截图导入组件状态；前端构建通过，真实浏览器状态保持仍需人工确认。 |
| 反馈/问题 | 图片 OCR（文字识别）优先使用可选 MinerU CLI（MinerU 命令行工具），MinerU 远程结果下载失败时必须尝试 RapidOCR（本地文字识别）；两者均失败时提示具体原因而不是静默失败。2026-06-30 修复主聊天 `真实联调：...` 这类中文冒号前缀被误当作英语词条并进入选项的问题。 |
| 最终结论 | 自动与浏览器验收通过。 |

## 10. 快速开始、主聊天截图导入与细分等待状态

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开 Web（网页）首页；当前查看的会话/日期面板没有导入或题组时点击长期学习总面板底部按钮；把多行截图词表直接粘贴到主聊天栏发送；把包含词表文本的文件拖入主聊天文本框后发送；切换当前模型视觉能力后分别拖入图片并发送；切到其他日期文件夹下已导入或已有题组的会话后再次查看长期学习总面板底部按钮。 |
| 预期结果 | 当前面板无内容时按钮显示“当日导入”，点击后打开右侧工作台并切到“截图导入”，长期总面板底部出现“当前题库为空，右侧已打开截图导入。”本地提示；按钮内部不得出现统计标签样式造成的“按钮套按钮”视觉；主聊天粘贴或拖入 3 个以上词条的截图词表时，后端复用截图导入流程创建独立截图练习会话、导入词表并生成完整考试式题组；拖入文件的抽取文本必须追加到文本框；当前模型支持图片输入时，聊天栏拖入图片显示待发送附件并由模型视觉识别；当前模型不支持图片输入时，聊天栏拖入图片调用 MinerU/RapidOCR 抽取文本并追加到输入框；等待过程能显示“截图解析中”“题目生成中”或“模型正在识别图片”；当前面板已有导入或题组时按钮显示“快速开始”，点击后发送“继续当前题组”。 |
| 证明方式 | 自动测试 `py -m pytest try/test_context_and_inline_screenshot.py try/test_past_papers.py try/test_provider_reasoning.py -q` 覆盖主聊天截图词表复用导入流程、文件文本抽取接口、MinerU token 模式和多模态 payload；前端构建 `cd frontend && npm run build` 通过；Browser（浏览器）验证空状态打开截图导入、总面板提示、按钮单层样式、主聊天拖入文件、聊天图片直传/解析分流、主聊天导入、细分等待状态、已有内容快速开始和移动视口。 |
| 结果记录 | 2026-07-01 前端构建通过；Browser（浏览器）临时空库验证通过：空状态只按当天 `daily_panel` 判断，不再被长期总统计误判为已有今日内容；按钮显示“当日导入”并打开右侧截图导入，同时在长期总面板显示本地提示。2026-07-01 修复跨日期会话判断并完成 Browser（浏览器）验证：切到 2026-06-30 下已有导入或题组的“截图词表练习：collision”会话时，左侧状态为 18/96 题、20/72 当日词汇，按钮按当前 `daily_panel` 显示“快速开始”，`aria-label` 为“继续当前题组”，不再因为日期不是今天而显示“当日导入”，且没有残留导入提示。2026-06-30 Browser（浏览器）隔离库验收通过：空状态按钮显示“导入截图”并打开右侧截图导入；主聊天粘贴 `word n. 释义` 格式导入 8 个词并生成 8 题；右侧粘贴 `word: 释义` 格式显示“截图解析中”“题目生成中”，导入 5 个词并生成 5 题；刷新后按钮保持“快速开始”，点击发送“继续今天的答题”并恢复题卡；移动 390x844 折叠侧栏后无横向溢出。 |
| 反馈/问题 | Browser（浏览器）验收发现主聊天和右侧导入原先漏识别 `word n. 释义` / `word: 释义` 格式，已扩展 `ScreenshotImportService` 与前端识别函数；单个词条仍不按截图导入处理，避免误判。 |
| 最终结论 | 自动与 Browser（浏览器）验收通过。 |

## 11. 答题模型讲解、上下文容量与 token（令牌）使用统计

| 项 | 内容 |
|---|---|
| 验收步骤 | 在题目卡选择答案并提交；观察聊天输入框发送按钮左侧上下文圆圈；悬浮圆圈查看上下文占用并点击“压缩上下文”；进入设置的“模型”页修改上下文容量上限；进入设置的“令牌”页查看使用台账。 |
| 预期结果 | 答题后 Evaluator Tutor（判题讲解 Agent）调用模型生成讲解，讲解基于程序判定、用户背景和会话上下文；模型失败时保留基础判题和作答记录；讲解消息同时展示上一题回顾卡片；上下文圆圈按默认 1,000,000 或用户设置上限显示当前会话占用，白色占比圆环必须根据实时百分比更新，并兼容后端返回 0-1 或 0-100 两种 `context_percent` 口径；悬浮弹窗展示当前会话上下文 token（令牌）、消息数、压缩摘要和压缩按钮；主聊天与分支消息中的基础 Markdown（标记语言）能显示为加粗、内联代码、列表、标题和代码块；设置页“令牌”页不再提供容量输入，只展示今日 token（令牌）、昨日、近 7 天、近 30 天、本月、累计调用、平均 token（令牌）/调用、模型排行、provider（供应商）排行、任务类型排行、账户概览、近 30 天活动和最近 24 条调用明细。 |
| 证明方式 | 自动测试 `py -m pytest try/test_answer_submission.py try/test_context_and_inline_screenshot.py -q`；前端构建 `cd frontend && npm run build`；Browser（浏览器）插件确认上下文弹窗消息数和 Markdown（标记语言）DOM（文档对象模型）节点；人工确认已答题回顾卡片、圆圈悬浮弹窗和设置页统计视觉。 |
| 结果记录 | 2026-06-30 答题模型调用、上下文容量设置和主动压缩自动测试通过；Browser（浏览器）验收确认“模型正在判题”、答题后自动进入下一题、上下文圆圈弹窗、主动压缩、设置页使用统计、上下文容量改为 123456 后圆圈显示 `12.3万` 上限。2026-07-01 修复聊天响应上下文容量按全局统计返回导致当前会话显示 0 的问题；自动测试覆盖普通聊天响应 `context_messages=2` 且 `estimated_current_context>0`；Browser（浏览器）验证已有会话弹窗显示 `3,467 / 100.0万`、消息数 18，主聊天 Markdown（标记语言）生成 `strong` 和 `code` 节点，气泡文本不再残留原始 `**`。2026-07-01 恢复上下文白色占比圆环和百分比兼容计算；前端构建通过。2026-07-02 上下文容量设置迁移到模型页，令牌页改为使用台账；自动测试覆盖今日/近 7 天/近 30 天、provider（供应商）/任务分布和最近调用明细；前端构建通过。 |
| 反馈/问题 | Browser（浏览器）验收发现模型返回空 JSON（数据格式）时曾直接显示 `{}`，已改为提取 `message` / `feedback` / `explanation` 字段或回退基础讲解；LLMLingua（提示词压缩库）为可选增强，默认使用本地抽取式摘要。2026-07-01 发现 `chat`（聊天）响应未传当前 `session_id` 给 `token_totals()`，已改为当前会话口径。 |
| 最终结论 | 自动与 Browser（浏览器）验收通过；真实模型讲解质量仍建议按日常学习人工抽查。 |

## 12. 历年真题试卷参考与题型勾选

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开 Web（网页）设置 -> 考纲；查看“历年真题与题型”；确认只显示“打开来源网站”而没有“联网搜索导入”按钮；勾选/取消真题试卷；点击“加入试卷”后确认导入栏和详细字段展开；点击导入栏内“选择文件”从本机文件管理器选择试卷文件，或把试卷文件拖入导入区域，或填写试卷标题、年份、来源网站、本地路径/粘贴试卷文本、题型和摘要后点击“确认加入试卷”；点击“重新解析”；勾选/取消题型；发送学习内容触发组卷。 |
| 预期结果 | 默认显示并选择当前考试近 3 年真题试卷，且按考试类型生成 `papers/<考试>/raw` 与 `papers/<考试>/parsed` 文件；英语四/六级默认来源网站为 `https://www.guojiya.cn/#exams`；用户选择会持久化；手动加入试卷的导入栏默认收起，只有点击“加入试卷”后才展开文件选择、拖拽区域和详细字段；选择、拖入或手动导入的试卷会复制或写入 raw（原始文件）目录并生成 parsed（解析结果）JSON（JSON 数据交换格式）；配置 `MINERU_TOKEN` 时，PDF/图片/PPTX/XLSX 等复杂试卷解析优先使用 MinerU 精准解析，未配置时使用轻量解析；导入/解析出的题型进入可勾选列表；失效的联网搜索导入按钮不得出现在前端；组卷阶段 `past_paper_context` 包含当前选中试卷、来源网站、解析摘要、章节、短摘录和已勾选题型，生成题目的 `source_refs` 记录真题来源边界；未勾选题型不进入生成要求。 |
| 证明方式 | 自动测试 `py -m pytest try/test_past_papers.py try/test_agent_settings_permissions.py -q` 覆盖文件上传导入接口、解析产物、联网搜索权限和 Skills 状态；前端构建 `cd frontend && npm run build`；人工确认设置页真题列表、默认收起的“加入试卷”入口、文件选择按钮、拖拽导入区域、导入表单、来源网站链接、重新解析按钮、原始/解析路径显示、联网搜索权限禁用态和题型勾选交互。 |
| 结果记录 | 2026-06-30 自动测试通过，前端构建通过，启动脚本和 HTTP（超文本传输协议）健康检查通过；设置页人工点击待确认。2026-07-01 新增拖拽文件上传导入自动测试：Markdown（Markdown 文本格式）试卷上传后保存 raw（原始文件）、生成 parsed（解析结果）JSON（JSON 数据交换格式），题型包含 reading/writing（阅读/写作）。2026-07-01 设置页手动加入试卷改为点击“加入试卷”后展开，并在试卷导入栏新增“选择文件”；前端构建通过，文件管理器弹出需人工确认。2026-07-02 联网搜索导入改为受 `skills` 和 `web_search_import` 权限控制，并返回推荐的无密钥 `multi-search-engine` 技能信息；自动测试覆盖权限关闭返回 403 与开启后创建近三年索引。2026-07-02 删除前端“联网搜索导入”按钮，英语四/六级默认来源切换为 `https://www.guojiya.cn/#exams`，自动测试覆盖默认来源。 |
| 反馈/问题 | 默认发布资产只包含目录骨架和可核验来源清单，不提交版权不明的完整真题；用户本地导入后可作为真实组卷参考。完整原文仍需用户按版权边界导入或粘贴已提取文本。 |
| 最终结论 | 自动验收通过，人工设置页操作待确认。 |

## 13. 普通聊天与组卷意图区分

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开 Web（网页）首页或新建聊天；发送“你好”；再发送“我应该怎么安排四级复习计划？”；最后发送明确练题请求，例如“请给我出 12 道四级词汇题”或 `collision: 碰撞；冲突`。 |
| 预期结果 | “你好”和学习建议咨询进入普通聊天，不显示“题目生成中”，不新增 `questions`（题目）记录，不把寒暄算入当日题目/词汇；若当前已有题卡，题卡保持但不被重置；明确练题请求仍进入正式组卷并先入库题组。 |
| 证明方式 | 自动测试 `py -m pytest try/test_chat_intent_routing.py try/test_chat_error_persistence.py try/test_formal_drill_flow.py -q`；前端构建 `cd frontend && npm run build` 通过；Browser（浏览器）人工验证“你好”loading（加载）文案为普通思考/回复状态且不生成题。 |
| 结果记录 | 2026-07-01 自动测试通过：`你好` 路由为 `general_chat`，API（接口）返回普通问候且 `questions` 与 `model_calls` 均为 0；“我应该怎么安排四级复习计划？”和“怎么练四级听力？”这类学习建议咨询不组卷；学习建议咨询在模型失败时仍保留会话和错误提示；显式出题、`今天练 CET-4 高频词汇` 和词表定义仍路由到 `daily_drill`。前端构建通过。Browser（浏览器）验证通过：发送“你好”后显示普通问候回复，不出现“题目生成中”，不显示题卡；长期统计发送前后均为 31/135 题、13/51 词；测试新增会话已删除。 |
| 反馈/问题 | 当前真实库里 2026-07-01 已存在此前误生成的 0/12 当日聚合数据，本轮修复不自动删除历史题组，避免误删用户数据；如需清理可后续单独做带确认的删除流程。 |
| 最终结论 | 自动与 Browser（浏览器）验收通过。 |

## 14. 数据隔离、题目数据库迁移与默认模型

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开设置页“数据”；查看当前题目数据库路径、数据库大小和表计数；点击题目数据库文件夹右侧的文件夹按钮并在本机文件夹选择器中选择目录，或手动输入新的题目数据库文件夹；选择“迁移当前数据库”或关闭后初始化空库；在设置页“模型”选择供应商和模型后点击“设为默认模型”；也可运行 CLI（命令行接口）`py -m langdrill_agent.cli data-paths` 和 `py -m langdrill_agent.cli set-question-db-folder "D:\LangDrill\user-data" --migrate`。 |
| 预期结果 | 点击文件夹按钮会打开本机文件夹选择器；选择目录后路径写入输入框，取消时显示未选择提示；`测试数据/开发数据/<时间戳>/` 保存从正式运行路径迁出的污染开发数据；新正式库不包含学习会话、消息、题目、作答、知识项和模型调用记录；默认考纲、每个内置考试近三年真题索引、内置提示词模块和真实模型 `.env` 配置保留；项目内历史 `data/langdrill_agent.db` 不会默认复制污染新库，除非显式设置 `LANGDRILL_MIGRATE_LEGACY_DB=1`；自定义题目数据库文件夹会写入 `.env` 的 `LANGDRILL_USER_DATA_DIR` 和 `LANGDRILL_DB_PATH`；默认模型选择会写入 `LANGDRILL_DEFAULT_PROVIDER`、`LANGDRILL_DEFAULT_MODEL` 和对应 Base URL（基础网址）。 |
| 证明方式 | 自动测试 `py -m pytest try/test_data_paths.py try/test_user_data_and_logging.py try/test_model_config.py -q`；人工或命令检查 `C:\Users\29551\.langdrill-agent\data\langdrill_agent.db` 中 `study_sessions/messages/questions/attempts/knowledge_items/model_calls` 均为 0，`syllabus_sources=8`、`exam_assets=27`；检查 `测试数据/开发数据/污染数据-20260701-135716/manifest.json` 存在；前端构建 `cd frontend && npm run build` 通过；Browser（浏览器）人工确认“数据”页签、文件夹选择按钮和“设为默认模型”按钮可见；文件夹选择器需要人工确认。 |
| 结果记录 | 2026-07-01 已迁移污染数据到 `测试数据/开发数据/污染数据-20260701-135716`；新正式库创建成功，学习记录表为 0，默认参考数据保留；新增数据路径 API（接口）、CLI（命令行接口）、设置页“数据”页签和“设为默认模型”按钮；自动测试、构建、启动健康检查和 Browser（浏览器）设置页验证通过。2026-07-01 新增后端本机文件夹选择接口和前端文件夹按钮；选择器弹窗需人工确认。 |
| 反馈/问题 | 数据库迁移会复制当前 SQLite（轻量数据库）并切换 `.env`，正在运行的后端进程建议重启后继续使用新日志路径；覆盖目标库默认关闭，开启覆盖时会保留 `pre-migration` 备份。 |
| 最终结论 | 自动与 Browser（浏览器）验收通过。 |

## 15. 官方考纲识别与语种匹配

| 项 | 内容 |
|---|---|
| 验收步骤 | 打开设置页“考纲管理”；选择英语四级或英语六级并点击“手动检查并更新”；再切换到法语四级并点击“手动检查并更新”；查看考纲下拉列表和当前考纲卡片。 |
| 预期结果 | 官方 CET（大学外语考试）考纲页中的“全国大学法语四级考试大纲（2023版）”只匹配法语四级；英语四级/六级仍显示“全国大学英语四、六级考试大纲（2016年修订版）”；法语四级显示 2023 版并拥有近 3 年真题索引与法语题型勾选项。 |
| 证明方式 | 自动测试 `py -m pytest try/test_syllabus_service.py try/test_data_paths.py -q`；前端构建 `cd frontend && npm run build`；官方来源页面 `https://cet.neea.edu.cn/xhtml1/folder/16113/1588-1.htm`；Browser（浏览器）人工确认设置页下拉和手动检查结果。 |
| 结果记录 | 2026-07-01 新增官方考纲页面本地解析测试，覆盖英语 2016 不被法语 2023 污染、法语四级识别 2023；临时数据库真实官网抓取验证返回 `cet4=2016`、`cft4=2023`；`py -m pytest try/test_syllabus_service.py try/test_data_paths.py -q`、`py -m pytest try -q`、`py -m ruff check backend try` 和 `cd frontend && npm run build` 通过。 |
| 反馈/问题 | 官网同一页面包含多个语种考纲，必须按语种与级别过滤；不能只取页面最高年份。 |
| 最终结论 | 自动验收通过；设置页实际点击仍建议按日常人工抽查。 |

## 16. 回归命令

| 命令 | 预期结果 |
|---|---|
| `py -m pytest try -q` | 所有后端测试通过。 |
| `py -m ruff check backend try` | Python（编程语言）代码检查通过。 |
| `py -m pytest try/test_startup_scripts.py -q` | 启动脚本结构回归测试通过。 |
| `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall` | 后端和前端后台启动，HTTP（超文本传输协议）健康检查通过。 |
| `cd frontend && npm run build` | TypeScript（类型化 JavaScript）和 Vite（前端构建工具）构建通过。 |

## 最终结论

- 当前结论：2026-06-30 历年真题试卷参考已扩展为原始试卷与解析产物资产流；自动测试、前端构建和启动验收通过。真题设置页人工点击和真实模型按真题解析结果生成质量仍建议按日常学习人工抽查。
