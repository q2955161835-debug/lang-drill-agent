# 进展记录

## 2026-06-26 18:40 ~ 2026-06-26 19:48

### 本阶段完成内容
- 对当前功能进行了冒烟测试，覆盖 bootstrap（初始化加载）、chat（聊天学习）、答题判分、未作答解释、总结、分支对话、组词器、截图文本导入、Anki（记忆卡工具）未连接状态、档案保存、模型恢复默认、历史会话读取和前端构建。
- 使用用户提供的手机单词截图内容转写为 OCR（文字识别）文本，验证截图导入对普通单词列表会识别为 `text_only`（纯文本）且不崩溃。
- 修复启动脚本写入 API Key（接口密钥）、恢复默认清空 API Key（接口密钥）、进程环境变量旧值覆盖 `.env`、mock（本地模拟）继承真实模型配置、Git Bash（命令行）执行 `.bat` 误用非 Windows（视窗系统）命令等问题。
- 新增模型配置回归测试，防止恢复默认和 mock（本地模拟）隔离逻辑回退。
- 更新用户测试验收清单，区分自动冒烟已通过项和仍需人工视觉/点击确认项。

### 新增/修改/生成的文件清单与用途说明
- `backend/langdrill_agent/services.py`：修复模型配置服务，恢复默认保留 API Key（接口密钥），写 `.env` 后同步当前进程环境变量，并隔离 mock（本地模拟）配置。
- `start.bat`：不再写入或覆盖 API Key（接口密钥），只写入 MiMo（米魔）默认 provider/model/base_url（供应商/模型/基础网址）。
- `try/test_model_config.py`：新增模型配置回归测试，覆盖恢复默认保留 key（密钥）和 mock（本地模拟）隔离真实配置。
- `doc/manual-acceptance-checklist.md`：更新用户测试表，记录自动冒烟结果、待人工确认项和本轮问题状态。
- `doc/项目地图.md`：更新模型配置规则、功能入口、验证命令和右侧工作台说明。
- `doc/进展记录/2026-6-26.md`：记录本轮冒烟测试、修复和复测结果。

### 问题-原因-解决方案
- 问题：`start.bat` 明文写入 API Key（接口密钥）。原因：启动脚本将开发期模型配置和密钥一起覆盖写入 `.env`。解决方案：移除 key（密钥）写入逻辑，仅保留 MiMo（米魔）默认 provider/model/base_url（供应商/模型/基础网址）。
- 问题：恢复默认设置会清空已有 API Key（接口密钥）。原因：`reset_defaults` 使用空 key（密钥）和 `clear_empty=True` 写 `.env`。解决方案：恢复默认只写 provider/model/base_url（供应商/模型/基础网址），保留已有 key（密钥）。
- 问题：写 `.env` 后当前进程仍可能返回旧模型配置。原因：`python-dotenv`（环境变量加载库）加载过的进程环境变量优先级覆盖了新 `.env`。解决方案：`_write_env` 写文件后同步更新当前进程环境变量。
- 问题：mock（本地模拟）模式会继承真实 Base URL/API Key（基础网址/接口密钥）状态。原因：`mock` 不在正式 provider（供应商）查找表中，回落到了 DeepSeek（深度求索）并读取真实环境变量。解决方案：增加内部 mock（本地模拟）provider（供应商）处理，并让 `current_with_secret` 在 mock（本地模拟）模式不携带真实 key（密钥）。
- 问题：复测时 8000 端口存在热重载残留进程，导致新旧代码响应不一致。原因：旧 `--reload` 父子进程残留。解决方案：最终验证改用独立 8010 端口启动最新后端，并在测试表中提示人工测试前先运行 `stop.bat`。
- 问题：从 Git Bash（命令行）执行 `stop.bat` 时出现 `find` 和 `timeout` 命令冲突。原因：Git Bash（命令行）环境里的同名命令覆盖了 Windows（视窗系统）系统命令。解决方案：`start.bat` 和 `stop.bat` 显式调用 `%SystemRoot%\System32` 下的 `netstat/findstr/taskkill/ping`，并用 `ping` 替代 `timeout` 等待。

### 复测结果
- `py -m pytest try/test_model_config.py -q`：2 passed。
- `py -m ruff check backend try/test_model_config.py`：All checks passed。
- `npm run build`：构建成功。
- `stop.bat`：在 Git Bash（命令行）下通过 `MSYS_NO_PATHCONV=1 cmd.exe /c stop.bat` 复测，完成关闭服务和端口清理流程，无命令冲突报错。
- 8010 端口最终接口冒烟：bootstrap、chat、解释、答题、总结、分支、组词器、截图导入、Anki（记忆卡工具）未连接提示和历史会话读取均 PASS（通过）。

### 错误汇报
- 浏览器自动化命令 `browser` 不可用，因此纯视觉/点击手感项无法自动完成，已在用户测试表中标记为 ⚠️。
- 本轮自动测试曾向 `.env` 写入测试假 key（密钥）`smoke-keep-key`，已立即删除精确匹配行并恢复用户测试默认 MiMo（米魔）配置；当前未保留该测试假 key（密钥）。

## 2026-06-26 20:50 ~ 2026-06-26 21:24

### 本阶段完成内容
- 根据用户截图修复主聊天布局：外层应用固定为视口容器，输入框固定底部，文本框禁用横向滚动和手动拉伸，避免被浏览器误判为密码输入。
- 修复题目固定逻辑：题目卡片只在 `ready`（待答题）状态显示；答题提交后立即隐藏，并通过后端状态返回确认。
- 在题目选项下方新增额外提问输入框和提交按钮，提交内容包含 `question_id`（题目编号）、`selected_option`（选项）和可空的额外提问，避免会话漂移时误触发重新出题。
- 删除右侧工作台中误解的“组词器”和 Anki（记忆卡工具）背词同步入口，改为手机映像入口；后端新增 adb（安卓调试桥）/scrcpy（手机映像工具）检测和启动接口。
- 优化左侧栏：新增“新建聊天”入口；设置按钮缩为左下角图标；当日面板新增当日词汇/知识点进度。
- 修复当日统计口径割裂：`study_sessions` 新增 `exam_id`，会话、题量、答题数和知识点进度按“日期 + 考试”聚合；切换考试时会话列表隔离。
- 重构设置面板为左侧标签 + 右侧完整设置结构，新增考试选择、考纲管理、令牌统计、学习设置和外观分区。
- 考纲检查改为手动检查；内置英语四级、日语四级、雅思、托福、高考英语和自定义考试入口，并记录当前考纲年份。
- 响应式修复右侧工作台展开后主面板被压窄导致标题断字的问题。

### 新增/修改/生成的文件清单与用途说明
- `.gitignore`：忽略 `%TEMP%*.log` 和 `frontend/%TEMP%*.log` 临时日志。
- `backend/langdrill_agent/api.py`：新增考试/考纲、手机映像、新建会话接口；答题接口支持题目编号和选项提交。
- `backend/langdrill_agent/models.py`：新增答题、考纲、手机映像相关请求字段。
- `backend/langdrill_agent/services.py`：新增考试隔离、同日同考试聚合面板、知识点进度和考纲服务。
- `backend/langdrill_agent/db.py`、`backend/langdrill_agent/migrations/001_initial.sql`：为 `study_sessions` 增加 `exam_id` 并兼容旧库补列。
- `backend/langdrill_agent/agents.py`：支持带额外提问的答题反馈路径。
- `backend/langdrill_agent/phone_mirror.py`：新增 adb（安卓调试桥）/scrcpy（手机映像工具）检测和启动服务。
- `frontend/src/App.tsx`、`frontend/src/styles.css`、`frontend/src/types.ts`：修复主聊天布局、题目提交、侧栏、设置面板、考试/考纲和令牌统计 UI（用户界面）。
- `frontend/src/components/RightWorkbench.tsx`：改为分支、手机映像、截图导入和语音预留。
- `try/test_answer_submission.py`、`try/test_daily_scope.py`、`try/test_syllabus_service.py`：新增回归测试，覆盖答题误路由、同日同考试统计聚合、考试隔离和考纲状态。
- `doc/项目地图.md`：更新长期维护信息。
- `doc/进展记录/2026-6-26.md`：记录本阶段实现、验证和异常。

### 高风险操作与回退方案
- 高风险操作：本轮涉及前后端状态口径、数据库 schema（模式）补列、设置面板重构和右侧工作台功能替换。
- 回退方案：已在修改前提交 Git（版本控制）检查点 `258cb76 chore: pre-change checkpoint before UI fixes`，可通过该提交回退到修改前状态。

### 问题-原因-解决方案
- 问题：历史会话中输入框会“飞起来”。原因：外层 `.app-shell` 被浏览器自动滚动，三栏一起上移。解决方案：将 `.app-shell` 固定在视口并使用不可滚动裁剪，滚动只保留在消息流和侧栏列表。
- 问题：答题提交可能触发 401（接口未授权）模型错误。原因：结构化提交只带选项，后端在会话状态漂移时找不到 active question（当前题目），误路由到重新出题。解决方案：提交时携带 `question_id`（题目编号），后端优先按题目编号读取待答题记录。
- 问题：右侧手机映像点击检查时当前 8000 端口返回 404。原因：手动验证环境里残留多个旧 `--reload` 后端进程，页面代理到了旧路由集合。解决方案：代码层新增接口并在 8001 端口用当前源码直接验证通过；用户正式启动应使用 `start.bat`，该脚本会清理端口并使用 editable（可编辑）安装和 `PYTHONPATH`。

### 复测结果
- `py -m pytest try/test_answer_submission.py try/test_daily_scope.py try/test_syllabus_service.py try/test_model_config.py -q`：7 passed。
- `py -m ruff check backend try/test_answer_submission.py try/test_daily_scope.py try/test_syllabus_service.py try/test_model_config.py`：All checks passed。
- `npm run build`：构建成功。
- 浏览器验证：首屏非空、无框架错误、控制台无错误；输入框固定底部；题目卡片可显示选项与额外提问框；设置面板标签式布局正常；右侧手机映像 UI（用户界面）正常；8001 端口手机映像状态接口返回 adb/scrcpy 检测结果。

### 错误汇报
- 当前 8000 端口存在多个旧后端监听进程残留，无法在本轮权限环境中完全清理，因此右侧手机映像 UI（用户界面）的“检查环境”按钮在 5173 页面上仍会命中旧后端 404；新后端接口已在 8001 端口直接验证。

## 2026-06-26 21:43 ~ 2026-06-26 22:08

### 本阶段完成内容
- 将默认用户状态从项目内 `data/` 分离到当前系统用户主目录点目录 `~/.langdrill-agent`，数据库写入 `~/.langdrill-agent/data/langdrill_agent.db`，日志写入 `~/.langdrill-agent/logs/langdrill-agent.log`。
- 增加旧项目内默认数据库复制迁移：若点目录库不存在且 `data/langdrill_agent.db` 存在，启动时只复制不删除。
- 增加统一日志配置和 API（接口）请求日志，模型、数据库、截图导入等模块可写入统一日志，便于定位问题。
- 将运行时已下线的旧组词器和 Anki（记忆卡工具）导出模块移动到 `archive/optimized-out/2026-06-26/`，并从 API（接口）路由中移除 `/api/composer/next`、`/api/anki/status`、`/api/anki/export`。
- 重做截图导入链路：支持源图片路径记录、OCR（文字识别）文本解析、单词列表识别、知识项落库、当日词汇进度刷新。
- 导入词汇可驱动兜底出题；答题后会更新相关 `knowledge_items.mastery_score`（掌握度）和 `due_at`（下次复习时间）。
- 前端右侧截图导入支持“解析文本”和“导入当前会话”，导入后刷新左侧当日词汇进度。
- 新增 `try/full_chain_smoke.py`，按设置 MiMo（米魔）`mimo-v2.5-pro`、检查考纲、导入真实截图词表、查库、出题、答题、查掌握度的顺序进行全链路冒烟。

### 高风险操作与回退方案
- 高风险操作：将默认数据库/日志从项目目录迁移到用户主目录点目录，移除运行时旧功能导入，新增截图单词导入落库和端到端验证脚本。
- 回退方案：先提交 Git（版本控制）检查点；如数据迁移不符合预期，可从 Git（版本控制）回退代码，并使用项目内旧 `data/langdrill_agent.db` 或备份目录恢复测试状态。

### 新增/修改/生成的文件清单与用途说明
- `.env.example`：新增 `LANGDRILL_USER_DATA_DIR`、点目录数据库示例和 `LANGDRILL_LOG_LEVEL`。
- `.gitignore`：忽略 `data_backups/`，避免用户数据备份提交。
- `backend/langdrill_agent/config.py`：新增点目录默认路径和旧默认数据库路径识别。
- `backend/langdrill_agent/db.py`：新增旧项目数据库复制迁移和初始化日志。
- `backend/langdrill_agent/logging_config.py`：新增统一日志配置。
- `backend/langdrill_agent/api.py`：新增请求日志，移除旧 Anki（记忆卡工具）/组词器接口，截图导入支持落库和返回当日面板。
- `backend/langdrill_agent/models.py`：截图导入请求新增 `source_image_path`，移除旧运行时请求模型。
- `backend/langdrill_agent/screenshot_import.py`：新增单词列表解析、知识项导入和会话计划更新。
- `backend/langdrill_agent/agents.py`：兜底出题使用导入词汇，答题更新知识项掌握度和复习时间。
- `backend/langdrill_agent/services.py`：当日词汇进度纳入当天导入的知识项；`.env` 写入顺序保留新增变量。
- `backend/langdrill_agent/cli.py`：新增 `data-paths` 和 `backup-user-data` 命令，并接入日志配置。
- `frontend/src/App.tsx`、`frontend/src/components/RightWorkbench.tsx`：截图导入接入当前会话 ID（编号）和当日面板刷新。
- `archive/optimized-out/2026-06-26/`：归档旧组词器和 Anki（记忆卡工具）模块。
- `try/test_user_data_and_logging.py`、`try/test_screenshot_import_workflow.py`、`try/test_runtime_surface.py`：新增回归测试。
- `try/full_chain_smoke.py`：新增全链路冒烟脚本。
- `doc/项目地图.md`、`doc/manual-acceptance-checklist.md`、`README.md`、`try/debug.py`：同步当前数据目录、运行入口和验收说明。

### 问题-原因-解决方案
- 问题：默认数据库仍可能读取项目内 `./data/langdrill_agent.db`。原因：`.env.example` 和历史 `.env` 使用旧相对路径。解决方案：配置层将旧默认路径视为迁移来源，默认正式路径改为用户点目录；显式非旧默认 `LANGDRILL_DB_PATH` 仍可覆盖。
- 问题：截图导入只把文本写消息，不写知识库。原因：原服务只解析 A-D 题目文本，没有单词列表语义和落库流程。解决方案：新增 vocabulary_list（单词列表）解析、`knowledge_items` 写入、当日计划合并和进度刷新。
- 问题：导入单词无法参与出题和复习算法。原因：兜底题固定使用 affect/effect 示例，答题后不更新知识项。解决方案：兜底题优先选择低掌握度导入词，答题后写回掌握度和 `due_at`。
- 问题：浏览器复测时截图导入仍返回 `text_only`（纯文本）。原因：8000 端口残留旧热重载父子进程，前端命中了旧代码。解决方案：按父子进程链清理 8000，启动当前源码单进程后复测，解析结果变为 `vocabulary_list`（单词列表）。
- 问题：旧组词器和 Anki（记忆卡工具）功能虽然前端移除，但后端仍导入并暴露接口。原因：API（接口）未同步拆除旧路由。解决方案：移除运行时导入和路由，旧代码移入归档目录。

### 复测结果
- `py -m pytest try/test_answer_submission.py try/test_daily_scope.py try/test_syllabus_service.py try/test_model_config.py try/test_user_data_and_logging.py try/test_screenshot_import_workflow.py try/test_runtime_surface.py -q`：15 passed。
- `py try/full_chain_smoke.py`：通过；真实截图路径存在，导入 12 个单词，题目使用低掌握度导入词，答题正确，`attempts` 写入 1 条，对应知识项掌握度更新到 0.844，`due_at` 已写入。
- `py -m ruff check backend try/test_answer_submission.py try/test_daily_scope.py try/test_syllabus_service.py try/test_model_config.py try/test_user_data_and_logging.py try/test_screenshot_import_workflow.py try/test_runtime_surface.py try/full_chain_smoke.py`：All checks passed。
- `npm run build`：构建成功。
- Browser（浏览器）验证：页面标题和 URL 正确，首屏非空，输入框底部固定，设置按钮为 42x42；右侧截图导入真实交互显示“已导入 12 个单词”，预览包含 `cultivate`，左侧当日词汇进度刷新；复测期间近 5 分钟无新增控制台错误或警告。

### 错误汇报
- 本机未安装 adb（安卓调试桥）/scrcpy（手机映像工具），手机映像只能验证检测接口和提示，无法启动真实手机窗口。
- 当前环境没有可用 `LANGDRILL_PROVIDER_API_KEY`（模型接口密钥）进程变量；冒烟脚本已把模型配置设为 MiMo（米魔）`mimo-v2.5-pro`，但真实在线模型调用未执行，避免伪造成功或消耗未知密钥。
- 8000 端口曾残留旧热重载父子进程，导致前端命中旧截图解析逻辑；已清理并重启当前源码后完成复测。
