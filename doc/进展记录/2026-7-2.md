# 2026-7-2 进展记录

## 2026-07-02 14:26 ~ 2026-07-02 14:41

本阶段完成内容：
- 进入修复-验收循环，修复上轮 Browser（浏览器）全量验证中能落到项目代码的问题。
- 修复设置页“数据”页计数过期：打开数据页时刷新 `/api/data-paths`，并用规范化合并避免 `counts` 被浅合并覆盖；Browser 插件复验显示 UI（用户界面）题目 27、作答 1、会话 6、知识项 23，与后端接口一致。
- 修复分支入口依赖右键/文本选择导致 Browser 插件和普通点击路径难以覆盖的问题：右侧分支页在当前会话有待答题题卡或最新消息时，提供“基于当前题/最新消息创建分支”按钮；Browser 插件复验基于当前题创建分支并发送追问成功。
- 扩展浏览器验收脚本，覆盖数据页真实计数刷新、当前题分支创建和分支追问。
- 同步 `AGENTS.md`、`doc/项目地图.md`、`doc/验收标准.md` 的长期维护口径和验收项。

新增/修改/生成的文件清单与用途：
- `frontend/src/App.tsx`：新增数据路径规范化、数据页进入时刷新真实计数、当前题/最新消息分支来源生成和分支创建回调。
- `frontend/src/components/RightWorkbench.tsx`：新增分支页普通点击创建分支入口。
- `frontend/src/styles.css`：调整分支面板 grid（网格）行数，容纳新增创建按钮。
- `try/browser_acceptance_check.py`：新增数据页计数与 `/api/data-paths` 对齐检查、当前题创建分支和分支追问检查。
- `AGENTS.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步分支入口和数据页计数刷新规则。
- `doc/进展记录/2026-7-2.md`：记录本阶段修复与验收。

验证结果：
- `py -m ruff check backend try`：All checks passed。
- `cd frontend && npm run build`：通过。
- `git diff --check`：通过，仅有 Git（版本控制）换行提示。
- `py -m pytest try -q`：91 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py try/browser_acceptance_check.py`：通过。
- Browser（浏览器）插件复验：页面标题 `Lang Drill Agent`，无 Vite/Webpack/Next.js 错误覆盖，控制台 error/warn 为空；数据页计数与 `/api/data-paths` 一致；分支页基于当前题创建分支并发送追问成功。

错误汇报：
- 问题：首次扩展 `try/browser_acceptance_check.py` 时用 `text_content()` 判断数据页计数，脚本误判失败。
  原因：`text_content()` 不保留用户可见换行，导致 `题目\n22` 这类断言不成立。
  解决方案：改为 `inner_text()` 并轮询等待数据页刷新完成，复跑通过。
- 剩余限制：Browser 插件仍不能直接设置本地文件选择器文件，真实“选择文件/拖拽文件”上传路径需通过 Playwright（浏览器自动化工具）验收脚本或人工确认；真实 MiMo（小米米魔）服务超时属于外部模型稳定性问题，本轮未改供应商调用逻辑。

## 2026-07-02 13:20 ~ 2026-07-02 14:25

本阶段完成内容：
- 按用户要求使用 Browser（浏览器）插件对当前 Web（网页）版 Lang Drill Agent 进行端到端验证；测试数据保留在 `try/.cache/browser-acceptance/`，未清空。
- 创建并使用任务分支 `codex/test-full-web-validation`；启动隔离后端和前端，前端 `http://127.0.0.1:5173`、后端 `http://127.0.0.1:8000/docs` 均可访问。
- 验证右侧截图导入文本路径：用用户给定截图中的词表文本解析出 23 个单词，导入后自动生成考试式题组；回答第 1 题后出现已答题回顾卡片并自动推进下一题。
- 验证主聊天 3 个以上词条粘贴路径：`meal/steal/save/emerge/dish` 触发截图词表后台流程，生成 5 道题并保持当前待答题吸附题卡。
- 验证设置页模型、权限、Skills（技能）、考纲、数据、令牌页：自定义模型新增/删除 UI（用户界面）可用；权限默认分组正确；`Multi Search Engine` Skill 可开启并恢复关闭；真题近三年索引、加入试卷上传区、数据迁移入口、令牌台账均可见。
- 验证上下文容量弹层：显示 `319 / 100.0万`、消息数、压缩方式和“压缩上下文”按钮；未执行压缩。
- 验证右侧工作台状态保持：切换到手机映像并检查环境后，截图导入页仍保留 23 个词条和导入状态；本机未检测到 adb（安卓调试桥）和 scrcpy（手机映像工具），页面给出安装提示。

新增/修改/生成的文件清单与用途：
- `doc/进展记录/2026-7-2.md`：记录本阶段 Browser（浏览器）插件验证、异常和验收结果。
- `try/.cache/browser-acceptance/langdrill-agent.db`：本轮隔离测试数据库，保留测试生成的会话、题目、作答、知识项和 token（令牌）台账。
- `try/.cache/browser-acceptance/`：本轮隔离用户数据目录和测试 Skills（技能）目录，按用户要求未清空。
- 工作区外读取的用户截图来源：`D:\29551\QQ_Files\Tencent Files\2955161835\nt_qq\nt_data\Pic\2026-06\Ori\65a03fb7c59367f9d718b38c3a590827.png`、`D:\29551\QQ_Files\Tencent Files\2955161835\nt_qq\nt_data\Pic\2026-06\Ori\edb688058b034b1561b9ee7159d48d38.png`。

验证结果：
- `py -m pytest try -q`：91 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m ruff check backend try`：All checks passed。
- `npm run build`（frontend）：通过。
- `py -m pytest try/test_startup_scripts.py -q`：4 passed。
- Browser（浏览器）插件检查：页面标题为 `Lang Drill Agent`；浏览器 error/warn 日志为空；截图导入解析 23 个词条；截图导入题组 12 题、主聊天词表题组 5 题；数据库只读核对为 4 个学习会话、17 道题、1 条作答、23 个知识项、3 次模型调用。
- `/api/data-paths` 只读核对返回正确计数：`study_sessions=4`、`questions=17`、`attempts=1`、`knowledge_items=23`。

错误汇报：
- 问题：Browser（浏览器）插件当前无法直接设置本地文件选择器文件，无法用该插件完成真实文件上传路径。
  原因：Browser 插件暴露的 in-app browser（内置浏览器）能力没有 `set_input_files` 等本地文件选择 API（接口）。
  解决方案：本轮使用用户截图中可见词表文本完成 Browser 插件路径验证；真实“选择文件/拖拽文件”上传路径列为未覆盖项。
- 问题：Browser（浏览器）插件 viewport（视口）能力尝试曾导致连接超时并重置。
  原因：当前插件会话对 viewport 能力不稳定。
  解决方案：停止使用该能力，改用当前桌面视口进行验证；移动视口未覆盖。
- 问题：隔离环境中真实 MiMo（小米米魔）模型调用超时。
  原因：外部模型服务响应不稳定，不适合做确定性 UI（用户界面）验收。
  解决方案：页面加载后将测试配置切到 Mock Provider（本地模拟供应商），用于确定性生成题组和判题。
- 问题：设置页“数据”页显示题目 0、作答 0、会话 2，与左侧学习面板和后端计数不一致。
  原因：只读核对显示数据库和 `/api/data-paths` 计数正确，前端数据页使用的 `dataPathDraft` 没有跟随后续学习动作刷新。
  解决方案：记录为前端数据页计数刷新问题，未在本轮修改代码。
- 问题：右侧分支对话发送按钮在未创建分支时保持禁用，Browser 插件无法完成右键消息菜单或文本拖选创建分支。
  原因：分支创建入口依赖选中文本或右键上下文菜单，当前 Browser CUA（电脑使用自动化）接口不支持右键，文本拖选未触发浏览器选择。
  解决方案：已验证分支页禁用态和前置条件；完整分支创建/追问列为未覆盖项。

## 2026-07-02 13:12 ~ 2026-07-02 13:17

本阶段完成内容：
- 按用户要求调整项目定位：正式使用方式改为 Web（网页）三栏学习工作台，CLI（命令行接口）保留为开发、调试、自动化和数据维护辅助入口，不删除既有功能。
- 已创建任务分支 `codex/docs-web-primary-positioning`，并提交修改前 Git（版本控制）空检查点，方便回退。
- 同步新增验收项“Web（网页）核心入口定位”，要求普通学习、截图导入、模型配置、题目数据库迁移和人工验收以 Web（网页）为主。

新增/修改/生成的文件清单与用途：
- `AGENTS.md`：更新项目目标、核心优化方向、目录职责和核心数据流中的 Web（网页）优先定位。
- `README.md`：更新公开项目定位，删除 CLI（命令行接口）终端聊天作为示例主路径的展示，保留维护命令。
- `doc/项目地图.md`：更新最后更新时间、项目目标、系统分层、核心入口和数据迁移口径。
- `doc/验收标准.md`：新增 Web（网页）核心入口定位验收项，并调整数据迁移验收步骤为 Web（网页）优先。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- 旧定位短语扫描：无把 CLI（命令行接口）与 Web（网页）并列为普通用户入口、或把终端聊天写成核心学习路径的旧定位命中。
- `git diff --check`：通过，仅出现 Git（版本控制）自动换行提示。
- `npm run build`（frontend）：通过。

错误汇报：
- 问题：旧定位短语扫描曾命中 `doc/验收标准.md` 的证明命令文本本身。
- 原因：验收项把要查找的旧短语原样写入了文档，造成自匹配。
- 解决方案：将证明方式改为描述性检查目标，不再把旧短语原样保存在验收标准中。

## 2026-07-02 13:05 ~ 2026-07-02 13:07

本阶段完成内容：
- 高风险操作前记录：按用户要求准备清理旧任务分支，目标是保留 `main` / `origin/main`，删除本地和远端旧任务分支，降低仓库分支噪声。
- 已检查 `main` 与 `origin/main` 同步且工作区干净；清理前统计本地非 `main` 分支 45 个，远端非 `main` 分支 31 个。
- 已在工作区外生成分支回退备份，路径为 `D:\0文件夹\备份\lang-drill-agent-branch-cleanup-20260702-1305`。
- 高风险标记：本阶段后续将批量删除 Git（版本控制）本地分支和 GitHub（代码托管平台）远端分支引用；不会修改 `main` 文件内容，但会改变本地和远端分支列表。
- 回退方案：优先使用备份目录中的 `local-branches.tsv`、`remote-branches.tsv` 和 `all-refs-before-cleanup.bundle` 恢复；本地分支可用 `git branch <分支名> <SHA>` 重建，远端分支可用 `git push origin <SHA>:refs/heads/<分支名>` 重建。
- 已完成旧分支清理：远端删除 31 个非 `main` 分支，本地删除 46 个非 `main` 分支（包含本轮临时清理分支）。
- 已执行 `git fetch --prune origin` 清理远端跟踪引用；最终只保留 `main`、`origin/main` 和 `origin/HEAD`。

新增/修改/生成的文件清单与用途：
- `doc/进展记录/2026-7-2.md`：记录分支批量删除前的高风险说明、备份路径和回退方案。
- `D:\0文件夹\备份\lang-drill-agent-branch-cleanup-20260702-1305\local-branches.tsv`：清理前本地分支名称、SHA 和提交说明。
- `D:\0文件夹\备份\lang-drill-agent-branch-cleanup-20260702-1305\remote-branches.tsv`：清理前远端跟踪分支名称、SHA 和提交说明。
- `D:\0文件夹\备份\lang-drill-agent-branch-cleanup-20260702-1305\all-refs-before-cleanup.bundle`：清理前 Git 全引用打包备份。
- `D:\0文件夹\备份\lang-drill-agent-branch-cleanup-20260702-1305\restore-commands.ps1`：恢复命令示例说明。

验证结果：
- `git status --short --branch`：`main` 与 `origin/main` 同步且工作区干净。
- `git branch --format=...`：已导出本地分支备份。
- `git branch -r --format=...`：已导出远端跟踪分支备份。
- `git bundle create ... --all`：已生成清理前完整引用 bundle（Git 打包备份）。
- `git push origin --delete ...`：成功删除 31 个远端旧分支。
- `git branch -D ...`：成功删除 46 个本地旧分支。
- `git branch --all --format=...`：最终仅剩 `main`、`origin/main` 和 `origin/HEAD`。

错误汇报：
- 未出现阻塞性错误。

## 2026-07-02 12:38 ~ 2026-07-02 12:47

本阶段完成内容：
- 按用户要求完成分支清理审计，核对 `main`、`origin/main`、本地已合并/未合并分支、远端已合并/未合并分支、未合并分支提交数量、差异规模和分支间祖先关系。
- 识别当前分支混乱的主要原因：未合并列表中大部分是旧任务派生链节点，不是彼此独立的新任务；真正需要重点判断的是 3 个顶端分支。
- 生成 `doc/分支清理报告-2026-7-2.md`，记录清理分组、删除候选、需要人工确认的顶端分支和不建议直接批量删除的原因。
- 本阶段未删除分支、未合并未合并任务分支、未重写 Git（版本控制）历史。

新增/修改/生成的文件清单与用途：
- `doc/分支清理报告-2026-7-2.md`：分支清理审计报告，包含未合并分支分组、判断依据和建议清理顺序。
- `doc/进展记录/2026-7-2.md`：记录本阶段只读审计和报告生成情况。

验证结果：
- `git status --short --branch`：开始审计时 `main` 与 `origin/main` 同步且工作区干净。
- `git branch --no-merged main`：确认本地未合并分支 10 个。
- `git branch -r --no-merged origin/main`：确认远端未合并分支 9 个。
- `git merge-base --is-ancestor`：确认 7 个未合并分支被其它未合并分支包含。
- 本轮为 Git（版本控制）和文档审计，未运行业务自动测试。

错误汇报：
- 未出现阻塞性错误。

## 2026-07-02 01:43 ~ 2026-07-02 02:08

本阶段完成内容：
- 将本地 Skills（技能）从 Agent 权限页的全局“Skills 功能”开关中拆出；本地 Skills 不再有总权限，统一由 Skills 页每个 Skill 的单项开关独立启用/关闭。
- 后端权限服务移除 `skills` 权限定义，并过滤旧设置中残留的 `skills` ID，防止历史数据库继续显示或启用幽灵总开关。
- 前端权限页移除“默认关闭的扩展权限”空组和 Skills 总权限，Skills 页文案改为“每个本地 Skill 都有独立开关”。
- 新增 Playwright（浏览器自动化工具）验收脚本，自动生成临时 Skill fixture（测试夹具）并校验服务端使用临时 Skills 根目录，覆盖权限页、Skills 单项开关互不影响、自定义模型添加/删除、真题设置页导入栏、题型勾选、截图导入待解析队列在页签切换和右栏折叠后保持。
- 对此前标记缺少浏览器确认且可自动化覆盖的内容进行浏览器验收；系统级双击、真实文件夹/文件选择器弹窗和主观视觉审美仍属于需要真人观察的边界。
- 验收后重新用默认配置启动服务，避免本轮临时数据库继续占用本地页面。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/services.py`：移除 Skills 总权限，过滤旧权限记录中的 `skills`。
- `frontend/src/App.tsx`：移除前端默认 Skills 总权限，更新 Skills 页文案，补齐自定义模型权限默认项。
- `try/test_agent_settings_permissions.py`：覆盖权限列表不再包含 `skills`，旧版和当前版本权限记录会丢弃残留 `skills`。
- `try/browser_acceptance_check.py`：新增浏览器验收脚本，使用 `try/.cache/` 临时数据和临时 Skill fixture（测试夹具）。
- `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步 Skills 单项开关、测试命令和浏览器验收结果。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_agent_settings_permissions.py -q`：11 passed，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m pytest try -q`：91 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m ruff check backend try`：All checks passed。
- `cd frontend; npm run build`：通过。
- `py try/browser_acceptance_check.py`：通过。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall`：退出码 0。
- `http://127.0.0.1:8000/docs`：HTTP 200。
- `http://127.0.0.1:5173`：HTTP 200。

错误汇报：
- 初次 Node 版浏览器验收脚本失败，原因是 `npx --package=playwright` 没有把 `playwright` 包暴露给 ESM import（ES 模块导入）；解决方案是改为项目已有的 Python Playwright。
- 浏览器脚本首次切换 Skill 时使用 `check()`，但受控 checkbox 需要等待接口返回后才更新状态；解决方案是改为点击后等待 `/api/skills/enabled` 响应再断言状态。
- 浏览器脚本定位自定义模型删除结果时使用固定显示名，多次失败重跑会被旧临时模型干扰；解决方案是每轮使用唯一模型 ID 和显示名，并重建临时数据库。

## 2026-07-02 01:28 ~ 2026-07-02 01:42

本阶段完成内容：
- 阅读当天进展记录并联合验收“联网功能 / Skills（技能）拆分”和“自定义模型”两组改动。
- 补齐前端 Skills 状态默认值中的 `builtin_web_search`，并新增 `normalizeSkillsStatus`（Skills 状态归一化）以兼容旧响应或缺字段响应。
- 设置页 Skills 页签分开展示内置联网检索和 Multi Search Engine（多搜索引擎）本地 Skill，明确内置联网只受“联网功能”权限控制，不依赖 Skills 开关。
- 前端默认权限分组补入“配置自定义模型”，与后端敏感权限口径一致。
- 自动测试补充断言：内置联网默认开启、无需 API Key/token，启用或关闭本地 Multi Search Engine Skill 不影响内置联网状态。
- 同步 AGENTS、README、项目地图和验收标准中的联网/Skills 拆分、自定义模型和联合验收记录。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/web_search.py`：内置无密钥联网检索服务，由普通聊天显式联网请求触发。
- `backend/langdrill_agent/api.py`：普通聊天接入内置联网检索上下文，并把来源写入助手消息 payload。
- `backend/langdrill_agent/services.py`：Skills 状态返回内置联网检索与可选本地 Skill 两类状态。
- `frontend/src/App.tsx`：修复 `builtin_web_search` 缺省状态，Skills 页分开展示内置联网和本地 Skill，补齐默认敏感权限分组。
- `frontend/src/types.ts`：Skills 状态类型包含 `builtin_web_search`。
- `try/test_agent_settings_permissions.py`：覆盖内置联网与本地 Skill 开关互不影响。
- `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步长期规则、用户说明、项目地图和验收记录。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try -q`：90 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m ruff check backend try`：All checks passed。
- `cd frontend; npm run build`：通过。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall`：退出码 0。
- `http://127.0.0.1:8000/docs`：HTTP 200。
- `http://127.0.0.1:5173`：HTTP 200。

错误汇报：
- 初次 `npm run build` 失败，错误为 `src/App.tsx(578,7): Property 'builtin_web_search' is missing in type ... but required in type 'SkillsStatus'`。原因是后端/类型新增了内置联网状态字段，但前端默认 `DEFAULT_SKILLS_STATUS` 未同步；解决方案是补齐默认内置联网状态并增加 `normalizeSkillsStatus` 统一归一化 API 响应。

## 2026-07-01 23:34 ~ 2026-07-02 00:09

本阶段完成内容：
- 优化模型设置：新增从供应商 API（接口）自动刷新可调用模型，支持 OpenAI-compatible（OpenAI 兼容）`/models` 与 Anthropic Messages（Anthropic 消息接口）`/v1/models` 常见返回格式；模型默认全部显示，可在设置页逐个隐藏/显示，聊天栏只列出未隐藏模型。
- 删除设置页和初始化弹窗的手填自定义模型入口；模型下拉只来自内置模型、供应商 API（接口）刷新结果或自定义供应商默认模型。
- 模型页新增 API 格式选择；上下文容量上限从“令牌”页迁移到“模型”页。
- 优化内置系统提示词，让模型知道 Lang Drill Agent 的截图导入、主聊天词表/文件导入、题组生成、答题讲解、分支、设置草稿和权限边界；普通聊天不再错误声称无法访问或导入词库流程。
- 令牌页参考 sub2api（中转服务项目）的使用台账思路，新增今日、昨日、近 7 天、近 30 天、本月、累计调用、平均 token（令牌）/延迟、模型/provider（供应商）/任务分布、近 30 天活动和最近调用明细。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/models.py`：新增模型列表刷新和模型显示状态请求模型。
- `backend/langdrill_agent/services.py`：新增模型列表刷新、模型可见性持久化、模型列表解析与模型元数据合并。
- `backend/langdrill_agent/api.py`：新增模型刷新/显示状态 API；普通聊天系统提示词补充程序能力与权限边界。
- `backend/langdrill_agent/context.py`：扩展 token（令牌）统计台账字段。
- `backend/langdrill_agent/db.py`：内置提示词 seed 改为可升级写回，并新增程序能力提示模块。
- `frontend/src/App.tsx`、`frontend/src/types.ts`、`frontend/src/styles.css`：模型页、令牌页、模型显示/隐藏和类型/样式更新。
- `try/test_model_config.py`：覆盖模型列表刷新、原生思考档位保留和模型隐藏。
- `try/test_context_and_inline_screenshot.py`：覆盖 token（令牌）窗口统计和最近调用明细。
- `AGENTS.md`、`doc/项目地图.md`、`doc/验收标准.md`、`README.md`：同步长期规则、项目地图、验收项和用户说明。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_model_config.py try/test_context_and_inline_screenshot.py -q`：22 passed，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `npm run build`（frontend）：通过。
- `py -m pytest try -q`：78 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m ruff check backend try`：All checks passed。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall`：退出码 0；`http://127.0.0.1:8000/docs` 与 `http://127.0.0.1:5173` 健康检查均返回 200。

错误汇报：
- 未出现阻塞性错误。

## 2026-07-02 01:10 ~ 2026-07-02 01:27

本阶段完成内容：
- 参考 opencode 的 provider/models（供应商/模型）配置思路，恢复当前供应商下显式添加自定义模型功能；自定义模型记录模型 ID、显示名称、上下文容量和视觉能力。
- 自定义模型带 `custom` 标记并允许删除；内置模型和 API（接口）返回模型只允许显示/隐藏，不能删除。
- 新增 Agent 权限“配置自定义模型”，属于敏感设置、默认关闭；会话 Agent 未授权时提示开启权限，授权后只生成可确认的自定义模型草稿，最终添加/删除仍由用户在设置页确认。
- 设置页模型页新增“添加自定义模型”表单和自定义模型删除按钮；模型列表刷新失败时仍可通过该入口补齐供应商未开放自动发现的模型。
- 更新内置系统提示词，让模型知道自定义模型只能生成草稿，不能自行保存或删除。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/models.py`：新增自定义模型请求模型。
- `backend/langdrill_agent/services.py`：新增 Agent 自定义模型权限、自定义模型添加/删除和模型 `custom` 标记规范化。
- `backend/langdrill_agent/api.py`：新增自定义模型添加/删除 API，并支持会话设置请求生成自定义模型草稿。
- `backend/langdrill_agent/db.py`：更新内置程序能力提示词。
- `frontend/src/App.tsx`、`frontend/src/types.ts`、`frontend/src/styles.css`：新增设置页自定义模型表单、删除按钮、设置动作卡片和类型/样式。
- `try/test_model_config.py`：覆盖自定义模型添加、删除、禁止删除内置模型和 API 往返。
- `try/test_agent_settings_permissions.py`：覆盖“配置自定义模型”权限默认关闭、未授权拦截和授权后返回草稿。
- `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步自定义模型、权限和验收口径。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_model_config.py -q`：23 passed，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m pytest try/test_agent_settings_permissions.py -q`：10 passed，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m ruff check backend try`：All checks passed。
- `py -m pytest try/test_model_config.py try/test_agent_settings_permissions.py -q`：33 passed，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m pytest try -q`：94 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `npm run build`（frontend）：通过。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall`：退出码 0。
- `http://127.0.0.1:8000/docs`：HTTP 200。
- `http://127.0.0.1:5173`：HTTP 200。

错误汇报：
- 发现当前工作区出现无关的内置联网检索 WIP 和未跟踪文件；为避免混入本次提交，已用 Git stash 保存为 `stash@{0}`（`preserve unrelated web search WIP before custom model final`），本次分支只保留自定义模型相关改动。

## 2026-07-02 00:55 ~ 2026-07-02 01:09

本阶段完成内容：
- 修复模型列表刷新遇到供应商不开放模型列表接口时的误报：MiMo（小米米魔）这类 Anthropic-compatible（Anthropic 兼容）Base URL（基础网址）如果 `/v1/models` 与 `/models` 都返回 404/405/501，将保留内置或已保存模型列表并返回短提示。
- 净化模型列表刷新错误信息：真正的鉴权、网络或服务端错误继续报错，但不再把 404/500 的 HTML（超文本标记语言）页面正文、`openresty` 等原始内容展示给前端。
- 补充自动测试覆盖 404 HTML 返回时保留 `mimo-v2.5` / `mimo-v2.5-pro`，以及 500 HTML 返回时错误提示不泄露正文。
- 同步模型配置规则、项目地图、README（项目说明）和验收标准。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/services.py`：新增模型列表接口不支持的降级分支和 HTTP（超文本传输协议）错误净化逻辑。
- `try/test_model_config.py`：新增模型刷新 404 降级和 500 错误净化回归测试。
- `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步模型列表刷新失败处理规则与验收口径。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_model_config.py -q`：20 passed，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m ruff check backend try`：All checks passed。
- `py -m pytest try -q`：86 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `npm run build`（frontend）：通过。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall`：退出码 0。
- `http://127.0.0.1:8000/docs`：HTTP 200。
- `http://127.0.0.1:5173`：HTTP 200。

错误汇报：
- 未出现阻塞性错误。

## 2026-07-02 00:33 ~ 2026-07-02 00:52

本阶段完成内容：
- 修正权限默认值：联网功能默认开启，Skills（技能）默认关闭；新增权限版本迁移，旧权限记录会补齐默认开启项但不会自动开启 Skills，避免“默认开启但未勾选”的误导。
- 删除设置页“历年真题与题型”中的“联网搜索导入”按钮，只保留“打开来源网站”和手动导入试卷入口。
- 按用户指定来源核验并切换英语四/六级真题来源到 `https://www.guojiya.cn/#exams`；该页面显示 2025 年最新真题、140+ 套历年真题和 2013–2025 完整覆盖。
- 新增单个 Skill 开启/关闭状态，Skills 页签可逐个启用或关闭本地技能，默认均未启用。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/services.py`：更新权限默认值与旧设置迁移；新增 Skills 启用状态持久化；更新英语四/六级真题来源和默认索引刷新逻辑。
- `backend/langdrill_agent/api.py`、`backend/langdrill_agent/models.py`：新增 `/api/skills/enabled` 和请求模型；联网隐藏接口改为只依赖联网权限。
- `frontend/src/App.tsx`、`frontend/src/types.ts`、`frontend/src/styles.css`：删除联网搜索导入按钮；新增 Skills 单项开关；同步默认权限显示。
- `try/test_agent_settings_permissions.py`、`try/test_past_papers.py`：覆盖旧权限迁移、Skills 开关、联网权限和国级呀真题来源。
- `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步权限、Skills 和真题来源口径。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_agent_settings_permissions.py try/test_past_papers.py -q`：20 passed，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `npm run build`（frontend）：通过。
- `py -m pytest try -q`：84 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m ruff check backend try`：All checks passed。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall`：退出码 0。
- `http://127.0.0.1:8000/docs`：HTTP 200。
- `http://127.0.0.1:5173`：HTTP 200。

错误汇报：
- 未出现阻塞性错误。

## 2026-07-02 00:10 ~ 2026-07-02 00:32

本阶段完成内容：
- 调整 Agent 权限模型：截图导入、学习数据库、历年真题草稿、联网搜索导入、考试目标、上下文容量和 Skills（技能）等非敏感权限默认开启；模型配置、题目数据库目录、MinerU token 等敏感设置默认关闭并集中显示。
- 新增截图导入和学习数据库权限闸门：右侧截图导入写库、主聊天截图词表写库会检查权限；关闭后主聊天返回可读提示，不写入题目。
- 将“联网搜索导入”改为受 `skills` 与 `web_search_import` 权限控制；关闭时接口返回 403（Forbidden，禁止访问），开启时返回推荐技能信息。
- 新增 Skills 状态功能：后端发现本地 Skills，前端新增 Skills 页签展示技能根目录、已安装技能、无密钥技能数量和推荐的 `multi-search-engine`；该技能不需要个人 API Key（接口密钥）或 token（令牌）。
- 更新普通聊天内置提示词，让模型知道截图导入、学习数据库、联网搜索导入和 Skills 能力边界。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/services.py`：扩展 `AgentSettingsPermissionService` 默认值和分组；新增 `SkillRegistryService`。
- `backend/langdrill_agent/api.py`：bootstrap（初始化）返回 Skills 状态；新增 `/api/skills`；为截图导入和联网搜索导入添加权限检查；更新普通聊天系统提示词。
- `frontend/src/App.tsx`、`frontend/src/types.ts`、`frontend/src/styles.css`：新增 Skills 状态类型、设置页 Skills 页签、权限分组 UI（用户界面）和联网搜索导入权限禁用态。
- `try/test_agent_settings_permissions.py`：覆盖默认权限、Skills 状态、截图导入权限拦截和联网搜索导入权限拦截。
- `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步权限默认值、Skills 功能、联网搜索导入口径和验收项。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_agent_settings_permissions.py -q`：7 passed，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m pytest try/test_agent_settings_permissions.py try/test_past_papers.py try/test_context_and_inline_screenshot.py -q`：22 passed，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `npm run build`（frontend）：通过。
- `py -m pytest try -q`：82 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 废弃警告）。
- `py -m ruff check backend try`：All checks passed。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall`：退出码 0。
- `http://127.0.0.1:8000/docs`：HTTP 200。
- `http://127.0.0.1:5173`：HTTP 200。

错误汇报：
- 未出现阻塞性错误。

## 2026-07-02 14:43 ~ 2026-07-02 14:59

本阶段完成内容：
- 按用户要求复核网页版 GPT（聊天模型）提出的三个短板：公开版 README（项目说明文档）过长、测试覆盖偏薄、`api.py`（API，应用程序接口）过大。
- 读取项目规则、项目地图、验收标准、README（项目说明文档）、测试目录和关键后端入口；量化发现 `README.md` 256 行、`backend/langdrill_agent/api.py` 1781 行、`backend/langdrill_agent/services.py` 3041 行、`frontend/src/App.tsx` 4239 行。
- 核验测试现状：当前并非只有启动脚本测试，已有正式刷题、答题提交、截图词表导入、数据路径迁移、模型配置、权限、真题和上下文统计等 91 个自动测试；但仍缺少独立 Validator（校验器）边界测试、结构化出题成功路径测试、CI（持续集成）流水线和公开展示截图。
- 使用隔离数据库运行 `try/full_chain_smoke.py`，验证截图导入、mock（本地模拟）生成题、答题推进和掌握度更新链路。

新增/修改/生成的文件清单与用途：
- `doc/进展记录/2026-7-2.md`：记录本轮只读审查、验证结果、问题和后续建议。
- `try/.cache/smoke-review/`：生成隔离冒烟测试数据库与日志，属于调试缓存，可清理，不影响项目运行。

验证结果：
- `py -m pytest try -q`：91 passed，1 skipped，2 warnings（FastAPI `on_event` 启动钩子弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- `npm run build`（frontend）：通过。
- `LANGDRILL_DB_PATH=try\.cache\smoke-review\langdrill-agent.db`、`LANGDRILL_USER_DATA_DIR=try\.cache\smoke-review` 下运行 `py try\full_chain_smoke.py`：通过，导入 12 个截图词，生成题并完成一次答题推进；烟测报告中出现 `fallback_after_validation_failure`，说明需补充模型结构化输出成功路径测试。

错误汇报：
- 问题：隔离运行 `try/full_chain_smoke.py` 后，项目 `.env`（真实环境变量账本）修改时间变为 2026-07-02 14:59:04。
- 原因：烟测虽然隔离了数据库和用户数据目录，但内部模型配置保存逻辑仍会触碰项目根目录 `.env`。
- 解决方案：本轮只做审查不修改业务代码；后续应让 smoke（冒烟脚本）隔离 `PROJECT_ROOT` 或增加 no-write（禁止写入）模式，确保测试不触碰真实 `.env`。已用脱敏方式检查变量名，未在记录或对话中写入真实密钥。

## 2026-07-02 15:00 ~ 2026-07-02 15:11

本阶段完成内容：
- 将根目录 `README.md` 改为面向公开展示/招聘快速阅读的短版，保留项目问题、核心功能、架构图、本地运行、截图、本人职责、完成度和下一步计划。
- 将原长版 README（项目说明文档）复制到 `doc/README-full.md`，并加入长版维护说明提示。
- 复制脱敏工作台截图到 `doc/screenshots/workbench-overview.jpg`，用于短版 README（项目说明文档）展示。
- 收紧 `QuestionValidator`（题目校验器）选择题答案边界：答案字母必须对应当前选项数量，答案字母和答案文本同时存在时必须一致。
- 新增 Validator（校验器）边界测试、Question Author（出题 Agent）结构化模型输出成功路径测试、full-chain smoke（全链路冒烟）隔离测试。
- 修复 `try/full_chain_smoke.py` 默认写真实 `.env` 的问题：默认隔离项目根、用户数据、数据库、试卷资产和敏感环境变量到 `try/.cache/full-chain-smoke`，只有显式 `LANGDRILL_SMOKE_ALLOW_REAL_ENV=1` 才保留真实密钥环境变量。
- 新增 `.github/workflows/ci.yml`，在 push（推送）和 pull_request（拉取请求）时运行 ruff（Python 代码检查）、pytest（Python 测试）和前端 build（构建）。
- 同步 `AGENTS.md`、`doc/项目地图.md` 和 `doc/验收标准.md` 的文档职责、截图目录、CI（持续集成）和验收项。

新增/修改/生成的文件清单与用途：
- `README.md`：公开短版项目说明。
- `doc/README-full.md`：长版完整维护说明。
- `doc/screenshots/workbench-overview.jpg`：公开 README（项目说明文档）使用的脱敏工作台截图。
- `.github/workflows/ci.yml`：GitHub Actions CI（持续集成）流水线。
- `backend/langdrill_agent/validator.py`：收紧选择题答案边界校验。
- `try/full_chain_smoke.py`：隔离冒烟测试运行时与敏感环境变量。
- `try/test_validator.py`：Validator（校验器）边界测试。
- `try/test_question_author_structured_output.py`：结构化出题成功路径测试。
- `try/test_smoke_isolation.py`：smoke（冒烟）隔离测试。
- `AGENTS.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步长期规则和验收项。
- `try/.cache/smoke-verify/`：本轮隔离冒烟验证缓存，可清理，不影响项目运行。

验证结果：
- `py -m pytest try/test_validator.py try/test_question_author_structured_output.py try/test_smoke_isolation.py -q`：7 passed，2 warnings（FastAPI `on_event` 启动钩子弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- 对比 `.env` 修改时间后运行 `LANGDRILL_SMOKE_RUNTIME=try\.cache\smoke-verify py try\full_chain_smoke.py`：通过，导入 12 个截图词并完成答题推进；真实 `.env` 修改时间保持不变。
- `py -m pytest try -q`：98 passed，1 skipped，2 warnings（FastAPI `on_event` 启动钩子弃用警告）。
- `npm run build`（frontend）：通过。

错误汇报：
- 未出现阻塞性错误。FastAPI `on_event` 弃用警告仍存在，建议后续拆 `api.py`（API，应用程序接口）时一并迁移到 lifespan（生命周期）写法。

## 2026-07-02 15:12 ~ 2026-07-02 15:39

本阶段完成内容：
- 按用户授权读取 Gmail（邮件）中的 GitHub（代码托管平台）通知，定位到 `lang-drill-agent` 的 CI（持续集成）失败通知：`main` 分支提交 `9d0beaa` 的 GitHub Actions run `28572389890` 失败。
- 使用 `gh`（GitHub CLI，GitHub 命令行工具）查看失败日志，确认根因是 CI 在 Windows runner（Windows 运行器）安装依赖到 setup-python（Python 设置动作）的 Python 3.13 后，lint/test（检查/测试）步骤使用 `py -m ...` 调到了另一个 Python 3.14 环境，导致 `ruff` 模块不可见。
- 修复 CI 命令，改为使用当前 workflow（工作流）配置的 `python -m ruff` 和 `python -m pytest`。
- 按用户反馈将用户可见“Skills”口径调整为“拓展 Skills（拓展技能）”，并把内置联网检索改为始终开启的内置工具：`enabled=true`、`always_enabled=true`、`locked=true`；实际联网调用仍通过 `permission_enabled` 反映“联网功能”权限状态。
- 更新前端设置页、后端状态接口、浏览器验收脚本、项目规则、项目地图、长版 README（项目说明文档）和验收标准，避免把内置必备工具与可选拓展 Skill 混为一类。

新增/修改/生成的文件清单与用途：
- `.github/workflows/ci.yml`：修复 Windows CI 使用错误 Python launcher（Python 启动器）的问题。
- `backend/langdrill_agent/services.py`：内置联网检索标记为始终开启并单独返回 `permission_enabled`。
- `backend/langdrill_agent/api.py`：更新联网权限和拓展 Skills 文案。
- `frontend/src/App.tsx`、`frontend/src/types.ts`：设置页改为“拓展 Skills”，展示内置工具始终开启和联网权限状态，补齐类型字段。
- `try/test_agent_settings_permissions.py`：覆盖内置工具始终开启、权限关闭只改变 `permission_enabled` 的边界。
- `try/browser_acceptance_check.py`：同步新版“拓展 Skills”页签和内置工具状态断言。
- `AGENTS.md`、`doc/README-full.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步长期规则、架构说明和验收口径。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_agent_settings_permissions.py -q`：11 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- `py -m pytest try -q`：98 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `npm run build`（frontend）：通过。
- GitHub Actions CI run `28573588628`：通过；唯一 annotation（注解）是 GitHub Actions 关于 Node.js 20（Node 运行时）弃用的提示，不影响本次任务。

错误汇报：
- 问题：GitHub Actions CI 在 Windows runner 上使用 `py -m ruff` 时调用了未安装依赖的 Python 3.14，导致 `No module named ruff`。
- 原因：`actions/setup-python` 已安装并缓存依赖到 Python 3.13，但 `py` launcher（Python 启动器）不保证使用该解释器。
- 解决方案：CI 命令统一改用 `python -m ...`，绑定到 workflow 当前 PATH（路径）中的 setup-python 解释器。

## 2026-07-02 15:40 ~ 2026-07-02 15:47

本阶段完成内容：
- 按用户反馈将所有考试中的听力 / Listening 题型改为预留项：默认未勾选、不可勾选，并显示“暂未接入听力题和语音模型”的原因。
- 后端 `PastPaperService` 为听力类题型返回 `available=false`、`disabled=true`、`locked=true` 和 `disabled_reason`，保存题型时过滤听力，确保直接调用 API（接口）也不能把听力加入 `enabled_question_type_ids`。
- 前端“历年真题与题型”复选框读取禁用字段，听力题型展示为禁用状态；浏览器验收脚本改为先断言听力禁用，再点击第一个可用题型。
- 同步长期规则、项目地图、长版 README（项目说明文档）和验收标准，明确未接入听力题和语音模型前不得生成听力题。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/services.py`：题型可用性标记、默认启用过滤和保存过滤。
- `frontend/src/App.tsx`、`frontend/src/types.ts`、`frontend/src/styles.css`：听力题型禁用展示、默认状态和禁用样式。
- `try/test_past_papers.py`：覆盖全部内置考试听力题型预留禁用和 API 保存过滤。
- `try/browser_acceptance_check.py`：更新浏览器验收脚本的题型勾选断言。
- `AGENTS.md`、`doc/README-full.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步听力题型预留规则和验收项。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_past_papers.py -q`：12 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- `npm run build`（frontend）：通过。
- `py -m pytest try -q`：99 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 弃用警告）。

错误汇报：
- 未出现阻塞性错误。浏览器验收脚本已更新，但本轮未启动本地前后端服务执行 `py try/browser_acceptance_check.py`。

## 2026-07-02 15:50 ~ 2026-07-02 15:58

本阶段完成内容：
- 按用户反馈重设计外观背景：移除浅色主题高方差小纹理和深色主题背景图片，改为低方差蓝紫色背景。
- 浅色主题背景统一为 `#f1f4ff`，深色主题背景统一为 `#11162d`；`body` 和 `.chat-main` 继续共用同一背景变量，避免中间工作区断层。
- 删除不再使用的 `frontend/public/assets/dark-theme-bg.jpg`，同步 AGENTS（项目规则）、项目地图、长版 README（项目说明文档）和验收标准。
- 用 Vite（前端开发服务器）临时启动 `http://127.0.0.1:5174`，通过 Playwright（浏览器自动化工具）+ Microsoft Edge（微软 Edge 浏览器）分别截取浅色和深色页面；截图确认浅色为平滑浅蓝紫，深色为平滑深蓝紫，无旧纹理噪声。验证后已停止临时服务并清理 `try/appearance-check`。

新增/修改/生成的文件清单与用途：
- `frontend/src/styles.css`：改为低方差浅蓝紫 / 深蓝紫主题背景。
- `frontend/public/assets/dark-theme-bg.jpg`：删除已停用的深色纹理背景图。
- `AGENTS.md`、`doc/README-full.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步外观背景口径和验收记录。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `npm run build`（frontend）：通过。
- `npx playwright screenshot --channel=msedge --color-scheme=light ...`：通过；截图确认浅色背景为低方差浅蓝紫。
- `npx playwright screenshot --channel=msedge --color-scheme=dark ...`：通过；截图确认深色背景为低方差深蓝紫。

错误汇报：
- 问题：首次 `npx playwright screenshot` 未给 `--viewport-size` 参数加引号，PowerShell（Windows 命令壳）把尺寸拆开，导致命令报错。
- 原因：Playwright CLI（浏览器自动化命令行）要求 `--viewport-size="width,height"` 作为单个参数。
- 解决方案：重跑时使用 `--viewport-size="1365,768"`，截图成功。
- 问题：Playwright 默认 Chromium（浏览器引擎）未安装，截图命令提示需要 `npx playwright install`。
- 原因：本机没有对应 Playwright Chromium 缓存。
- 解决方案：改用已安装的 `--channel=msedge`，不额外下载浏览器。

## 2026-07-02 16:04 ~ 2026-07-02 16:10

本阶段完成内容：
- 按用户反馈将 Multi Search Engine（多搜索引擎）拓展 Skill 改为首次发现时默认启用；用户手动关闭后仍保存为空启用列表，避免默认值覆盖用户选择。
- 设置页“拓展 Skills”文案改为说明内置联网检索和 Multi Search Engine 的边界：内置联网检索负责真实网页检索与摘要来源，Multi Search Engine 只生成可审计搜索入口。
- 后端联网失败/权限提示同步去掉“去启用 Multi Search Engine”的旧口径，避免默认启用后误导。
- 自动测试和浏览器验收脚本补充默认启用断言，并保留“关闭一个 Skill 不影响其它 Skill 或内置联网检索”的边界测试。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/services.py`：新增默认启用 Skill 列表，调整 Skills 状态与保存逻辑。
- `backend/langdrill_agent/api.py`：更新联网权限/失败提示中的拓展 Skills 边界文案。
- `frontend/src/App.tsx`、`frontend/src/types.ts`：更新 Skills 面板说明、默认启用标记和类型字段。
- `try/test_agent_settings_permissions.py`：覆盖 Multi Search Engine 默认启用、手动关闭和权限关闭边界。
- `try/browser_acceptance_check.py`：补充 Multi Search Engine 默认勾选、其它拓展 Skill 默认关闭的浏览器断言。
- `AGENTS.md`、`doc/README-full.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步长期规则、项目地图和验收口径。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_agent_settings_permissions.py -q`：11 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- `py -m pytest try -q`：99 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `npm run build`（frontend）：通过。

错误汇报：
- 问题：第一次运行 `py -m pytest try/test_agent_settings_permissions.py -q` 时，`test_skills_status_api_reports_recommended_no_key_skill` 仍按旧逻辑断言 `web_search_skill.enabled is False`，失败 1 项。
- 原因：本机默认拓展 Skills 目录已安装 `multi-search-engine`，新需求要求首次发现即默认启用，旧断言与新行为冲突。
- 解决方案：将断言改为跟随安装状态：已安装时必须出现在 `enabled_skill_ids`，未安装时仍保持未启用占位。

## 2026-07-02 16:12 ~ 2026-07-02 16:24

本阶段完成内容：
- 修复主会话普通聊天未调用大模型的问题：移除 `你好/hi` 等简单寒暄的程序硬编码短路，普通聊天统一调用当前模型并写入 `model_calls`，但仍不自动生成题组。
- 将普通聊天接入 PromptAssembler（提示词组装器）：PromptPack（提示词包）包含 core capability（核心能力）、task.general_chat（普通聊天任务）、persona（表达风格）、学习目标、学习背景、权限状态、拓展 Skills 状态、当前题和联网状态。
- 修复分支创建接口只返回程序文案的问题：创建分支和后续分支消息均调用当前模型，写入 `branch_chat` 模型调用记录，并携带选中文本和分支历史上下文。
- 恢复上轮误改的页面背景：恢复浅色渐变纹理和深色背景图 `frontend/public/assets/dark-theme-bg.jpg`；真正调整主聊天/分支消息气泡为浅蓝紫/深蓝紫专用颜色，避免纯白或纯黑气泡。
- 更新 AGENTS（项目规则）、项目地图、长版 README（项目说明文档）和验收标准，明确普通聊天/分支对话不能用程序硬编码回复替代模型。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/api.py`：普通聊天和分支对话统一模型调用、运行上下文组装、权限/拓展 Skills 状态注入。
- `backend/langdrill_agent/db.py`：新增 `task.general_chat` 和 `task.branch_chat` 内置提示词模块。
- `backend/langdrill_agent/prompt_engine.py`：让 persona（表达风格）模块作用于普通聊天和分支对话。
- `frontend/src/styles.css`：恢复页面背景变量并新增聊天气泡颜色变量；主聊天和分支消息使用蓝紫气泡。
- `frontend/public/assets/dark-theme-bg.jpg`：恢复深色主题背景图。
- `try/test_chat_intent_routing.py`：覆盖寒暄调用模型但不组卷、PromptPack 携带运行上下文、分支创建/消息调用模型。
- `AGENTS.md`、`doc/README-full.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步长期规则和验收口径。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_chat_intent_routing.py -q`：6 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- `py -m pytest try -q`：101 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `npm run build`（frontend）：通过。

错误汇报：
- 问题：用户发送 `hi/你好` 时主会话返回固定“你好，boss。我在...”，没有调用模型。
- 原因：`_general_chat_response` 中存在简单寒暄正则短路，早于 PromptPack 构造和 provider 调用直接 return。
- 解决方案：删除寒暄短路；联网权限关闭和联网失败也作为上下文交给模型回复，只有模型请求失败时才返回明确错误提示。
- 问题：右侧分支创建时返回固定“已开启分支对话...”，未调用模型。
- 原因：`/api/branch` 创建接口只写入分支记录和硬编码助手消息，只有后续 `/api/branch/{id}/messages` 才调用模型。
- 解决方案：抽出分支模型回复函数，创建分支和后续分支消息都调用当前模型并记录 `branch_chat`。
- 问题：上一轮误把整体页面背景改成低方差蓝紫，而用户实际指的是聊天气泡纯白/纯黑。
- 原因：只根据截图判断“外观背景”，没有把目标锁定到 `.bubble` / `.message.user .bubble` 选择器。
- 解决方案：恢复页面背景和深色背景图，只改消息气泡专用变量；验收标准改为明确检查气泡而不是页面背景。

## 2026-07-02 16:24 ~ 2026-07-02 16:52

本阶段完成内容：
- 实际使用隔离数据库捕获主会话和分支会话传给模型前的 PromptPack（提示词包），确认用户学习目标、学习背景、考试时间、权限状态和拓展 Skills 状态已进入上下文，问题不是硬性组装异常。
- 优化运行上下文：主会话、当前题追问和分支会话新增用户画像使用规则，要求模型在用户询问目标、基础、考试和考试时间时优先读取 `context_pack.profile`，字段为空才反问。
- 优化权限和工具说明：`context_pack.agent_permissions.enabled_tool_guidance` 现在只包含当前已开启权限对应的工具触发场景、使用方式和边界；`context_pack.skills` 增加内置联网检索与 Multi Search Engine（多搜索引擎）的使用说明。
- 修复当前题追问路径绕过运行上下文的问题：主会话中的题目提示/讲解现在也会携带用户画像、权限工具说明、当前题选项和自定义偏好。
- 分支会话继承主会话当前题：创建分支和后续分支消息会携带用户画像、权限工具说明、拓展 Skills 状态、选中文本、分支历史和当前待答题。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/api.py`：新增已开启权限工具说明、Skills 使用说明、运行时用户画像/工具/分支规则模块；修复题目追问和分支上下文组装。
- `backend/langdrill_agent/db.py`：更新 `task.general_chat`、`task.branch_chat` 和 `task.evaluator` 内置提示词，强化用户画像和权限工具说明使用规则。
- `try/test_chat_intent_routing.py`：补充主会话、题目追问和分支会话 PromptPack 上下文断言。
- `AGENTS.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步主/分支会话上下文和工具说明长期规则。
- `try/.cache/prompt-context-inspect`、`try/.cache/prompt-context-inspect-after`：本轮隔离实测数据库目录，仅用于调试验证，可清理。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- 隔离实测：优化前主/分支会话已有 `profile`，但权限和 Skills 只有状态；优化后主/分支会话包含 `runtime.profile_context_contract`、`runtime.tool_usage_contract`，分支会话额外包含 `runtime.branch_context_contract`，并带有开启权限的工具说明。
- `py -m pytest try/test_chat_intent_routing.py -q`：7 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend/langdrill_agent/api.py backend/langdrill_agent/db.py try/test_chat_intent_routing.py`：All checks passed。
- `py -m pytest try -q`：102 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- `npm run build`（frontend）：通过。

错误汇报：
- 问题：用户设置了学习目标和学习背景后，主会话询问“我的目标是什么”仍得到泛泛回答。
- 原因：实测 PromptPack 已包含 `context_pack.profile`，但提示词模块没有强制模型优先读取用户画像；权限和 Skills 上下文也缺少工具使用说明，模型只看到状态列表。
- 解决方案：新增运行时用户画像规则和工具使用规则；把已开启权限对应的工具说明加入上下文；题目追问和分支会话统一复用运行上下文。

## 2026-07-02 17:00 ~ 2026-07-02 17:15

本阶段完成内容：
- 使用 Browser（浏览器）插件连接真实页面并发送“请直接依据我的学习设置回答...”验收主会话，发现请求被误路由到设置入口硬编码文案。
- 修复设置路由规则：只有明确打开、修改、配置、添加、保存或导入设置的动作进入 `settings`；读取学习设置、当前模型或已开启权限状态的问题保留在 `general_chat` 模型路径。
- 补充自动测试覆盖已保存设置查询、设置修改动作、接口层模型调用和运行上下文。
- 重启前后端后完成真实浏览器复测：主会话真实 MiMo（小米米魔）回复引用四级 600 分、考试截止日期、每日计划、学习背景和已开启权限工具说明；右侧分支基于最新消息创建成功，模型调用记录显示 `branch_chat` 且包含运行时用户画像/工具/分支规则模块。

新增/修改/生成的文件清单与用途：
- `backend/langdrill_agent/task_router.py`：收紧设置路由为动作意图匹配，避免“学习设置是什么”类查询被误判。
- `try/test_chat_intent_routing.py`：新增设置查询走普通聊天、设置修改仍走设置流程、接口层携带运行上下文的回归测试。
- `AGENTS.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步已保存设置查询与设置修改动作的路由边界和验收记录。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `py -m pytest try/test_chat_intent_routing.py -q`：10 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m pytest try -q`：105 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- `npm run build`（frontend）：通过。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall`：重启后 8000 / 5173 健康检查返回 200。
- Browser（浏览器）真实验收：主会话 `general_chat` 和右侧分支 `branch_chat` 均真实调用当前 MiMo（小米米魔）模型；`model_calls` 最近记录包含 `runtime.profile_context_contract`、`runtime.tool_usage_contract`，分支额外包含 `runtime.branch_context_contract`。

错误汇报：
- 问题：真实浏览器发送“请直接依据我的学习设置回答...”时，主会话返回“请点击左侧栏底部的「设置」按钮...”，没有回答目标、背景和权限工具说明。
- 原因：`TaskRouter` 旧逻辑只要命中“设置 / 模型 / 配置”等关键词就进入 `settings`，并且 `设置...目标/背景` 正则窗口过宽，把“学习设置回答：我的目标...”误判成修改设置。
- 解决方案：删除宽泛关键词路由，改成设置动作正则；将“设置/设定”作为动词时的目标匹配窗口收紧到 3 个字符，并新增回归测试防止复发。

## 2026-07-02 17:18 ~ 2026-07-02 17:23

本阶段完成内容：
- 根据 Gmail（谷歌邮箱）收到的 GitHub Actions（GitHub 动作）CI（持续集成）失败邮件，读取 run `28579183093` 的失败日志。
- 定位 CI 失败为测试环境假设问题：`test_general_chat_prompt_includes_runtime_context` 依赖本机已安装的 `multi-search-engine` 拓展 Skill，GitHub runner（云端运行器）无该本地目录，导致 `skills.enabled_skill_guidance` 为空。
- 修复测试夹具：在测试临时目录中显式创建 `multi-search-engine/SKILL.md`，并通过 `LANGDRILL_SKILLS_ROOTS` 指向该临时根目录，使本机和 CI 输入一致。

新增/修改/生成的文件清单与用途：
- `try/test_chat_intent_routing.py`：为 PromptPack（提示词包）拓展 Skill 上下文测试补充临时技能根夹具，消除对个人机器安装状态的依赖。
- `doc/进展记录/2026-7-2.md`：记录本阶段 CI 邮件定位与修复。

验证结果：
- `py -m pytest try/test_chat_intent_routing.py::test_general_chat_prompt_includes_runtime_context -q`：1 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m pytest try -q`：105 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- `npm run build`（frontend）：通过。

错误汇报：
- 问题：GitHub Actions（GitHub 动作）CI 在提交 `640c807` 上失败，邮件主题为 `Run failed: CI - main (640c807)`，失败项为 `CI / test`。
- 原因：测试直接断言 `enabled_skill_guidance` 非空，但没有为 CI 显式提供任何已安装拓展 Skill；本机通过是因为存在个人目录 `D:/2Folder/skills/multi-search-engine`。
- 解决方案：测试内部创建最小 `multi-search-engine` 技能夹具并设置环境变量，保留对“已启用拓展 Skill 会进入 PromptPack”的验证，同时不依赖个人环境。

## 2026-07-02 18:20 ~ 2026-07-02 18:50

本阶段完成内容：
- 优化右侧截图导入拖拽区 UI（用户界面）：将拖拽入口改为图标、短文案和固定操作按钮的紧凑布局，减少窄栏内文字换行和按钮挤压。
- 优化待解析文件队列：新增文件图标、数量徽标和图标化移除按钮，文件名保持省略显示，队列支持最大高度内滚动，避免压住下方“源图片路径 / 文件名”输入项。
- 保持截图导入行为不变：选择或拖入文件仍只加入待解析队列，不触发后端抽取；点击“解析文本”后才读取文件。

新增/修改/生成的文件清单与用途：
- `frontend/src/components/RightWorkbench.tsx`：调整截图导入拖拽区和待解析文件行结构，压缩文案并增加文件/移除图标。
- `frontend/src/styles.css`：新增截图导入专用拖拽区布局和队列样式，处理窄栏换行、按钮位置、文件行省略和滚动。
- `doc/验收标准.md`：补充本轮截图导入拖拽区 UI 验收记录。
- `try/.cache/screenshot-drop-ui-open.png`：浏览器检查生成的截图，仅用于本轮 UI 验证，可清理。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。

验证结果：
- `npm run build`（frontend）：通过。
- Playwright（浏览器自动化工具）真实页面检查：展开右侧工作台并切到“截图导入”，模拟选择 `b305d-vocabulary-screenshot.png` 后，拖拽区内容完整位于边框内，待解析队列位于拖拽区下方，拖拽区和队列均无横向溢出，控制台无 error/warning（错误/警告）。

错误汇报：
- 问题：第一次 UI 调整后，“选择文件”按钮和拖拽区说明在窄栏中出现挤压，按钮视觉上接近队列区域。
- 原因：拖拽区原先基于 grid（栅格）布局，长格式说明在右栏窄宽度下换行过多，导致内容高度和边框视觉不一致。
- 解决方案：改为截图导入专用 flex（弹性布局）横排，压缩说明文案，固定操作按钮宽度，并用浏览器坐标断言确认内容在边框内、队列在下方。

## 2026-07-02 18:52 ~ 2026-07-02 19:15

本阶段完成内容：
- 恢复右侧截图导入解析结果的可编辑单词卡：点击“解析文本”后把识别词条展示为可修改的单词/释义输入卡，支持删除误识别词条和手动补充词条。
- 修正导入链路：“导入并开始练习”在存在已解析单词卡时，优先把用户确认后的词条序列化为 `word: meaning` 文本提交给现有截图解析接口，避免继续使用未修改的 OCR（文字识别）原文。
- 补充浏览器验收脚本断言：解析文件后必须出现可编辑单词卡，修改首个词条后导入，接口返回词条必须使用修改后的内容。
- 修正浏览器验收脚本中两个既有脆弱断言：权限页只检查是否存在标题为“拓展 Skills”的权限行，不再误伤联网功能说明；拓展 Skills 页单项开关文案允许当前“都可单独开关”表述。

新增/修改/生成的文件清单与用途：
- `frontend/src/components/RightWorkbench.tsx`：新增截图词条编辑状态、添加/删除/修改操作和导入前序列化逻辑。
- `frontend/src/styles.css`：新增截图导入可编辑单词卡、卡片列表、添加按钮和诊断摘要样式。
- `try/browser_acceptance_check.py`：新增可编辑单词卡验收断言，并修正权限/拓展 Skills 文案匹配。
- `AGENTS.md`：同步右侧截图导入解析后必须保留并使用可编辑词条卡的长期规则。
- `doc/项目地图.md`：同步 `RightWorkbench.tsx` 和截图导入数据流职责。
- `doc/验收标准.md`：补充截图导入可编辑单词卡验收步骤、预期和本轮验证记录。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。
- 超出工作区范围：使用用户临时截图 `C:\Users\29551\AppData\Local\Temp\codex-clipboard-8bad9034-a743-45f7-834d-0dc19108d695.png` 作为问题定位参考，未修改该文件。

验证结果：
- `npm run build`（frontend）：通过。
- `py -m pytest try/test_screenshot_import_workflow.py -q`：5 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check try/browser_acceptance_check.py`：All checks passed。
- `py -m pytest try -q`：105 passed，1 skipped，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- Browser（浏览器）真实页面目标流验证通过：打开 `http://127.0.0.1:5173`，右侧截图导入粘贴 `meal/steal/save/emerge` 词表后解析出 4 张可编辑单词卡；将第一张改为 `mealx` 并修改释义后导入，主聊天显示 `mealx`，隔离数据库 `knowledge_items` 最新截图词条为 `mealx`；页面无 console error/warning（控制台错误/警告）。
- `py try/browser_acceptance_check.py`：未通过整脚本验收；运行到既有分支创建步骤时等待 `/api/branch` 响应超时，后端日志未出现对应请求。本轮新增的截图导入卡片断言位于该步骤之后，因此未由整脚本执行到。

错误汇报：
- 问题：用户反馈截图导入解析结果不再是可修改单词卡，只剩只读预览文本。
- 原因：当前 `RightWorkbench.tsx` 只把 `parsed.words` 渲染为只读 `span`，且导入按钮会重新用原始 `text` 调用 `/api/screenshot/parse`，即使前端显示可编辑控件也不会自动影响后端导入。
- 解决方案：新增 `editableWords` 状态和可编辑卡片 UI（用户界面）；导入时若存在已解析词条卡，则把确认后的完整词条转换为 `word: meaning` 文本提交，复用现有后端解析与入库流程。

- 问题：`py try/browser_acceptance_check.py` 首次运行误报“权限页仍显示拓展 Skills 总权限”和“拓展 Skills 页未说明单个拓展 Skill 独立开关”。
- 原因：脚本用整段权限文本匹配“拓展 Skills”，误伤联网功能说明里的“独立于拓展 Skills”；另一个断言绑定了旧文案，没有兼容当前“都可单独开关”表述。
- 解决方案：把权限页断言收窄到权限行标题；拓展 Skills 页断言允许等价文案。

- 问题：完整浏览器验收脚本最终在既有分支创建步骤超时，未执行到新增截图卡片断言。
- 原因：脚本点击“基于当前题创建分支”后没有发出 `/api/branch` 请求，后端日志中也没有对应请求记录；该分支步骤与本轮截图导入卡片修复无直接关系。
- 解决方案：本轮未扩大范围修复分支验收脚本；已用 Browser（浏览器）对目标截图导入流程做独立页面验收，并在最终报告中标记整脚本未通过原因。

## 2026-07-02 19:16 ~ 2026-07-02 19:42

本阶段完成内容：
- 修复右侧分支输入区按钮和占位文字重叠：把“发送分支消息”全宽文字按钮改为右侧 40px 图标按钮，和 textarea（文本区域）组成固定双列布局。
- 调整分支创建交互：右键消息、选中文本悬浮入口、基于当前题/最新消息入口只先创建“分支引用卡”，不再把选中文本直接作为用户消息发送。
- 新增引用提交流程：用户可在引用状态下输入提示词后提交，也可留空用默认提示词提交；提交成功后才创建分支编号，后续同一小按钮用于发送分支追问。
- 更新浏览器验收脚本：分支步骤改为先断言引用卡，再点击“提交分支引用”；对真实模型参与的分支和截图导入慢接口放宽等待时间。

新增/修改/生成的文件清单与用途：
- `frontend/src/App.tsx`：新增 `BranchQuoteDraft` 状态、引用暂存、引用提交和清除逻辑，替换原先选中文本立即发分支的行为。
- `frontend/src/components/RightWorkbench.tsx`：新增分支引用卡、引用提交按钮、清除引用按钮和右侧图标发送按钮。
- `frontend/src/styles.css`：新增分支引用卡和分支输入区双列布局样式，避免按钮与输入文字重叠。
- `try/browser_acceptance_check.py`：更新分支引用卡验收步骤，并放宽真实模型慢接口等待。
- `AGENTS.md`：同步右侧分支引用必须先展示、再由用户提交的长期规则。
- `doc/项目地图.md`：同步右侧工作台分支引用卡数据流。
- `doc/验收标准.md`：补充分支引用提交验收项、验证结果和未完成整脚本原因。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。
- 超出工作区范围：使用用户临时截图 `C:\Users\29551\AppData\Local\Temp\codex-clipboard-17f86cea-7d0e-48d3-a8a4-9854317ab721.png` 作为问题定位参考，未修改该文件。

验证结果：
- `npm run build`（frontend）：通过。
- `py -m pytest try/test_chat_intent_routing.py -q`：10 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。
- `py -m ruff check backend try`：All checks passed。
- `git diff --check`：通过，仅输出 Windows（Windows 操作系统）换行提示。
- Browser（浏览器）插件目标流验证：点击“基于当前题创建分支”后出现分支引用卡；右侧图标按钮处于启用状态，textarea（文本区域）与按钮矩形不重叠；点击按钮后显示“分支正在思考”，请求完成后出现 `当前分支：br_...`，引用卡消失，按钮语义切换为“发送分支消息”。
- `py try/browser_acceptance_check.py`：未通过完整脚本；已通过本轮分支引用卡、引用提交和分支追问步骤，后续“导入并开始练习”生成题组时真实 MiMo（小米米魔）模型调用触发 `httpx.ReadTimeout` 并返回 400。

错误汇报：
- 问题：用户反馈分支消息发送按钮与输入框占位文字重叠，且选中文本开启分支时直接把选中内容发送出去。
- 原因：`RightWorkbench.tsx` 原先把 textarea（文本区域）和全宽文字按钮作为连续 grid（栅格）行渲染，右栏高度受限时容易视觉挤压；`App.tsx` 的 `createBranchFromText` 同时承担“选择引用”和“创建分支”，点击后立即调用 `/api/branch`，并把选中文本作为本地 user message（用户消息）展示。
- 解决方案：拆分为 `stageBranchQuote` 和 `submitBranchQuote`；先展示引用卡，再由分支输入区提交提示词；右侧发送改为固定 40px 图标按钮。

- 问题：首次运行完整浏览器验收脚本时卡在分支创建等待。
- 原因：本轮改造后“基于当前题创建分支”按钮只应进入引用状态，脚本仍期待点击后立即发出 `/api/branch` 请求。
- 解决方案：更新脚本为先断言 `.branch-quote-card`，再点击“提交分支引用”等待 `/api/branch`。

- 问题：完整浏览器验收脚本后续卡在截图导入题组生成。
- 原因：开发启动脚本会把默认模型切到真实 MiMo（小米米魔），浏览器验收中的截图导入题组生成调用真实模型，最终触发 `httpx.ReadTimeout` 并返回 400。
- 解决方案：本轮只放宽慢接口等待并记录未完成原因；该失败点属于既有验收环境/真实模型稳定性问题，与本轮分支 UI（用户界面）修改无直接关系。

## 2026-07-02 22:15 ~ 2026-07-02 22:17

本阶段完成内容：
- 修复右侧截图导入解析后单词卡挤压上方导入区的问题：把截图导入面板拆为导入控件区和结果区，结果区回到正常文档流，在导入控件下方自然向下延伸。
- 移除单词卡列表的内部固定高度滚动，让截图导入面板整体负责纵向滚动，保证拖拽区、文件列表、路径、识别文本框和操作按钮不被生成后的单词卡压缩。
- 同步长期规则、项目地图和验收标准，明确截图导入单词卡不得用内部滚动或高度挤压上方导入控件。

新增/修改/生成的文件清单与用途：
- `frontend/src/components/RightWorkbench.tsx`：为截图导入面板增加导入控件容器和解析结果容器，保持结果卡片位于控件之后。
- `frontend/src/styles.css`：调整截图导入面板为正常流式布局，取消 `.screenshot-word-list` 的固定最大高度和内部滚动。
- `AGENTS.md`：补充右侧截图导入结果区布局长期规则。
- `doc/项目地图.md`：同步 `RightWorkbench.tsx` 的截图导入布局职责和状态保持边界。
- `doc/验收标准.md`：补充截图导入单词卡向下延伸、面板整体滚动的验收项和验证结果。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。
- 超出工作区范围：使用用户临时截图 `C:\Users\29551\AppData\Local\Temp\codex-clipboard-8ccd49d7-e8a9-424d-9dd3-47f3911ef2cf.png` 作为问题定位参考，未修改该文件。

验证结果：
- `npm run build`（frontend）：通过。
- `py -m ruff check backend try`：All checks passed。
- `git diff --check`：通过，仅输出 Windows（Windows 操作系统）换行提示。
- HTTP（HyperText Transfer Protocol，超文本传输协议）健康检查：`http://127.0.0.1:8000/docs` 返回 200，`http://127.0.0.1:5173` 返回 200。
- Browser（浏览器）真实页面目标流验证通过：右侧截图导入粘贴 11 个词条并点击“解析文本”后，DOM（Document Object Model，文档对象模型）测得结果区位于导入控件之后，截图导入面板整体可滚动，单词列表 `max-height=none`、`overflow-y=visible`，向下滚动后可继续查看后续可编辑单词卡。
- 未运行完整 `py try/browser_acceptance_check.py`；该整脚本在前一阶段已记录会受到真实 MiMo（小米米魔）模型超时影响，本轮使用目标 Browser（浏览器）页面验证覆盖用户反馈路径。

错误汇报：
- 问题：用户反馈截图导入解析生成单词卡后会挤压上方导入区。
- 原因：截图导入面板沿用通用 `.workbench-form` grid（栅格）布局，解析结果和上方控件在同一滚动/高度约束内竞争空间；单词卡列表还有内部 `max-height` 和滚动，视觉上会压缩导入控件区。
- 解决方案：把截图导入面板改为 block（块级）正常流，导入控件先渲染，结果区随后向下延伸，由面板整体滚动；单词卡列表取消固定最大高度和内部滚动。

- 问题：首次布局尝试后 Browser（浏览器）测得结果区高度为 0。
- 原因：仍受原 grid（栅格）行高分配影响，上方导入控件高度较大时第二个隐式行塌缩。
- 解决方案：取消截图导入面板对通用 grid（栅格）布局的继承，改成专用 block（块级）布局后复测通过。

## 2026-07-02 22:50 ~ 2026-07-02 22:53

本阶段完成内容：
- 将右侧截图导入的“导入并开始练习”从识别文本框下方移到可编辑单词卡结果区最底部右侧，使用户先解析、检查/编辑词卡，再在底部执行最终导入。
- 收窄导入入口启用条件：只有已存在解析结果且至少有一个完整词条时，底部导入按钮才可用；手动新增的完整词条也会被序列化后参与导入。
- 同步长期规则、项目地图和验收标准，明确导入按钮属于词卡确认后的最终动作。

新增/修改/生成的文件清单与用途：
- `frontend/src/components/RightWorkbench.tsx`：移除上方导入按钮，在结果区底部新增右对齐导入按钮，并调整导入条件。
- `frontend/src/styles.css`：新增 `.screenshot-result-actions` 底部右对齐操作栏样式。
- `AGENTS.md`：补充截图导入最终导入按钮应位于结果区底部右侧。
- `doc/项目地图.md`：同步 `RightWorkbench.tsx` 截图导入按钮位置与数据流。
- `doc/验收标准.md`：补充底部右侧导入按钮的验收步骤、预期和验证记录。
- `doc/进展记录/2026-7-2.md`：记录本阶段工作。
- 超出工作区范围：使用用户临时截图 `C:\Users\29551\AppData\Local\Temp\codex-clipboard-30a02871-4fee-4af1-bdc7-3f1b420fe363.png` 和 `C:\Users\29551\AppData\Local\Temp\codex-clipboard-6dbaba0a-66e5-4044-a4f3-39696d76a439.png` 作为问题定位参考，未修改这些文件。

验证结果：
- `npm run build`（frontend）：通过。
- `py -m ruff check backend try`：All checks passed。
- `git diff --check`：通过，仅输出 Windows（Windows 操作系统）换行提示。
- HTTP（HyperText Transfer Protocol，超文本传输协议）健康检查：`http://127.0.0.1:8000/docs` 返回 200，`http://127.0.0.1:5173` 返回 200。
- Browser（浏览器）真实页面目标流验证通过：右侧截图导入粘贴 11 个词条并点击“解析文本”后，识别文本框下方操作区不再存在导入按钮；滚到单词卡结果区底部后，“导入并开始练习”按钮位于“添加词条”和诊断信息之后，操作栏右对齐，按钮可见；页面无 console error/warning（控制台错误/警告）。
- 未运行完整 `py try/browser_acceptance_check.py`；该整脚本在前一阶段已记录会受到真实 MiMo（小米米魔）模型超时影响，本轮使用目标 Browser（浏览器）页面验证覆盖用户反馈路径。

错误汇报：
- 问题：用户反馈“导入并开始练习”按钮放在识别文本框下方不符合习惯，应该在右侧最下面。
- 原因：上一轮布局仍沿用旧动作区，把“解析文本”和“导入并开始练习”并列放在上方，视觉上早于可编辑词卡确认步骤。
- 解决方案：上方动作区只保留“解析文本”；将导入按钮移动到结果区底部右侧，并要求已有解析结果和完整词条后才可用。
