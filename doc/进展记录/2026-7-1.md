# 2026-7-1 进展记录

## 2026-07-01 10:50 ~ 2026-07-01 11:39

本阶段完成内容：
- 修复新一天当日空题库时长期总面板按钮状态：只按当天 `daily_panel` 判断是否已有导入/题组，不再被长期总统计误判为已有今日内容。
- 空状态按钮文案从“导入截图”调整为“当日导入”，点击后打开右侧工作台并切到“截图导入”。
- 点击“当日导入”后在长期总面板标题下方显示本地提示：“今天空题库，右侧已打开截图导入。”
- 修复快速开始按钮内部文字被统计条 `span` 样式套用的问题，避免出现“按钮套按钮”视觉。

新增/修改/生成的文件清单与用途说明：
- 修改 `frontend/src/App.tsx`：新增当天内容判断函数、当日导入提示状态、空状态打开截图导入逻辑和按钮文案。
- 修改 `frontend/src/styles.css`：收窄统计条样式选择器，重做快速开始/当日导入按钮样式，新增当日导入提示样式。
- 修改 `doc/验收标准.md`：更新第 10 项验收标准，记录“当日导入”、总面板提示和按钮单层样式要求。
- 修改 `README.md`：同步快速开始入口文案和空状态行为说明。
- 临时创建 `try/quick-start-empty-state` 用于隔离空库浏览器验证，验证后已删除。

错误汇报：
- Browser（浏览器）验证中发现当前自动化 API（接口）不支持 `networkidle` 等待状态；改用 `load` 后继续验证。
- 初版提示放在聊天流和统计条底部时会被底部输入区遮挡；改为放在长期总面板标题下方，最终截图确认可见。

验证结果：
- `cd frontend && npm run build` 通过；仅保留 Vite（前端构建工具）chunk 超过 500 kB 的既有警告。
- `py -m pytest try/test_context_and_inline_screenshot.py -q` 通过，2 passed，存在 FastAPI（Web API 框架）`on_event` deprecation（弃用）警告。
- Browser（浏览器）桌面验证通过：按钮显示“当日导入”，右侧切到“截图导入”，提示可见，按钮内部无内嵌标签样式，控制台无 error/warn（错误/警告）。
- Browser（浏览器）移动 390x844 验证通过：收起左栏后点击“当日导入”可打开右侧截图导入，页面无横向溢出，控制台无 error/warn（错误/警告）。
- 默认服务已恢复，`http://127.0.0.1:5173` 与 `http://127.0.0.1:8000/docs` 均返回 200。

风险与回退：
- 本阶段为局部前端交互与文档修改，无批量删除、结构性重构或数据迁移。
- 回退方式：使用 Git（版本控制）回退本次分支提交即可恢复原按钮判断、文案和样式。

## 2026-07-01 11:45 ~ 2026-07-01 11:57

本阶段完成内容：
- 修复切换到其他日期文件夹下已导入/已有题组会话时，长期总面板仍显示“当日导入”的问题。
- 快速开始按钮改为按当前 `dailyPanel` 是否有内容判断，不再要求 `dailyPanel.date` 必须等于本地今天。
- 点击已有内容的按钮时发送“继续当前题组”，避免旧日期会话仍发送“继续今天的答题”。
- 新建聊天和切换会话时清理上一轮“当日导入”本地提示，避免提示串到其他会话。
- 空状态提示改为“当前题库为空，右侧已打开截图导入。”，避免在非今天会话里继续显示“今天空题库”。

新增/修改/生成的文件清单与用途说明：
- 修改 `frontend/src/App.tsx`：移除按钮状态中的今天日期限制，改为当前面板内容判断；补充切换会话/新建聊天时清理提示。
- 修改 `AGENTS.md`：同步长期维护规则，记录快速开始按钮必须按当前面板内容判断。
- 修改 `README.md`：同步快速开始按当前面板判断和“继续当前题组”行为。
- 修改 `doc/验收标准.md`：补充跨日期已导入会话应显示“快速开始”的验收要求。
- 修改 `doc/进展记录/2026-7-1.md`：记录本阶段问题、原因和修复结果。

错误汇报：
- 问题：切到 2026-06-30 等非今天日期的已导入会话后，长期总面板仍显示“当日导入”。
- 原因：前端将按钮状态写成 `dailyPanel.date === localDateString() && hasDailyImportedContent(dailyPanel)`，非今天会话被误判为空。
- 解决方案：按钮状态只使用当前 `dailyPanel` 的题目/词汇内容判断；长期总统计不参与空状态判断，日期也不再作为已有题组的否定条件。

验证结果：
- `npm run build` 通过，确认前端 TypeScript（类型化 JavaScript）与 Vite（前端构建工具）生产构建正常。
- `py -m pytest try/test_context_and_inline_screenshot.py -q` 通过，2 个用例通过；保留 FastAPI（Web API 框架）`on_event` 既有弃用警告。
- `git diff --check` 通过，仅提示 Windows 换行签出提醒。
- Browser（浏览器）验证通过：打开 `http://127.0.0.1:5173`，切到 2026-06-30 下“截图词表练习：collision”会话，左侧状态为 18/96 题、20/72 当日词汇；长期总面板按钮显示“快速开始”，`aria-label` 为“继续当前题组”，未显示导入提示。控制台仅见此前热更新遗留的 Vite（前端开发服务器）日志，页面无错误覆盖。

风险与回退：
- 本阶段为局部前端判断和文档更新，无数据迁移或结构性改动。
- 回退方式：使用 Git（版本控制）回退本阶段提交即可恢复上一版当日导入判断。

## 2026-07-01 11:58 ~ 2026-07-01 12:14

本阶段完成内容：
- 修复发送“你好”等普通寒暄却被当成学习输入并自动生成 12 道题的问题。
- 新增 `general_chat` 普通聊天任务类型；路由器改为“明确学习/练题意图才进入组卷”，普通寒暄、学习建议咨询和澄清问题不再落入 `daily_drill`。
- API（接口）增加普通聊天分支：不调用 Orchestrator（调度器）和 Question Author（出题 Agent），不更新 daily_plan（当日计划），不新增 questions（题目）；简单问候直接轻量回复，学习建议类可走模型普通聊天。
- 前端发送等待文案改为按意图显示：截图词表显示“截图解析中/题目生成中”，答题显示“模型正在判题”，明确练题显示“题目生成中”，普通聊天显示“模型正在思考”。

新增/修改/生成的文件清单与用途说明：
- 修改 `backend/langdrill_agent/models.py`：新增 `TaskType.general_chat`。
- 修改 `backend/langdrill_agent/task_router.py`：加入显式组卷意图判断，取消默认兜底组卷。
- 修改 `backend/langdrill_agent/api.py`：增加普通聊天响应分支，避免寒暄触发题库写入。
- 修改 `frontend/src/App.tsx`：同步前端 loading（加载）文案意图判断。
- 新增 `try/test_chat_intent_routing.py`：覆盖“你好”不生成题、学习建议不组卷、显式练题仍组卷。
- 修改 `try/test_chat_error_persistence.py`：将模型失败保留会话测试改为普通学习建议咨询，避免与问候短路回复冲突。
- 修改 `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步普通聊天与组卷意图区分规则和验收项。
- 修改 `doc/进展记录/2026-7-1.md`：记录本阶段问题、原因、验证和回退方案。

错误汇报：
- 问题：用户只发送“你好”，前端显示“题目生成中”，后端创建 12 道题并把寒暄算进当日题目/词汇。
- 原因：`TaskRouter.route()` 末尾把所有未识别输入默认返回 `daily_drill`；API 默认分支对 `daily_drill` 调用 Orchestrator（调度器）与 Question Author（出题 Agent）生成完整题组。前端也把所有非截图、非答题输入的等待文案统一写成“题目生成中”。
- 解决方案：新增 `general_chat`，只有明确词表或显式“出题 / 练习 / 刷题 / 考我”等学习动作才进入组卷；普通聊天不新增题目，前端按同类意图显示普通思考文案。

验证结果：
- `py -m pytest try/test_chat_intent_routing.py try/test_chat_error_persistence.py try/test_formal_drill_flow.py -q` 通过，8 个用例通过；覆盖“你好”、学习建议咨询、“怎么练四级听力？”和显式出题边界；保留 FastAPI（Web API 框架）`on_event` 既有弃用警告。
- `py -m pytest try/test_context_and_inline_screenshot.py try/test_screenshot_import_workflow.py try/test_answer_submission.py try/test_chat_intent_routing.py try/test_chat_error_persistence.py try/test_formal_drill_flow.py -q` 通过，19 个用例通过，确认截图导入、答题、正式组卷和普通聊天路由未互相破坏。
- `py -m ruff check backend try` 通过。
- `npm run build` 通过，确认前端 TypeScript（类型化 JavaScript）与 Vite（前端构建工具）生产构建正常。
- Browser（浏览器）验证通过：重启开发服务后打开 `http://127.0.0.1:5173`，新建聊天发送“你好”，页面显示普通问候回复，不出现“题目生成中”，不显示题卡；长期统计发送前后保持 31/135 题、13/51 词不变；测试新增会话 `ses_a2318b38a34d` 已删除。控制台仅见此前热更新遗留的 Vite（前端开发服务器）日志，页面无错误覆盖。

风险与回退：
- 本阶段为路由判断、API 分支和前端等待文案的局部修改，无数据库迁移或数据删除。
- 回退方式：使用 Git（版本控制）回退本阶段提交即可恢复旧的默认组卷逻辑。

## 2026-07-01 12:15 ~ 2026-07-01 12:29

本阶段完成内容：
- 修复聊天响应返回全局 `token_usage` 导致当前会话上下文容量显示为 0 的问题。
- 新增安全 Markdown（标记语言）渲染组件，主聊天和右侧分支消息支持加粗、内联代码、列表、标题和代码块。
- 主聊天气泡和分支消息改为使用 `MarkdownText` 渲染，不再把模型返回的 `**` 和反引号原样显示。

新增/修改/生成的文件清单与用途说明：
- 修改 `backend/langdrill_agent/api.py`：`/api/chat` 最终响应改为 `token_totals(conn, session_id)`，按当前会话返回上下文容量。
- 新增 `frontend/src/components/MarkdownText.tsx`：安全解析基础 Markdown（标记语言）为 React（前端框架）节点，不直接注入 HTML（超文本标记语言）。
- 修改 `frontend/src/App.tsx`：主聊天消息气泡接入 `MarkdownText`。
- 修改 `frontend/src/components/RightWorkbench.tsx`：分支消息接入 `MarkdownText`。
- 修改 `frontend/src/styles.css`：新增 Markdown（标记语言）段落、列表、代码和用户气泡内代码样式。
- 修改 `try/test_context_and_inline_screenshot.py`：新增聊天响应必须返回当前会话上下文消息数和估算 token（令牌）的回归测试。
- 修改 `AGENTS.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步 Markdown（标记语言）渲染组件、当前会话上下文口径和验收记录。

错误汇报：
- 问题 1：上下文容量弹窗在已有消息和题卡时仍显示 0。
- 原因 1：`/api/chat` 最终响应调用 `token_totals(conn)` 未传 `session_id`，上下文快照为空，只剩全局 token（令牌）累计字段。
- 解决方案 1：改为 `token_totals(conn, session_id)`，并新增自动测试覆盖 `context_messages` 与 `estimated_current_context`。
- 问题 2：模型讲解中的 `**加粗**` 和反引号内联代码按纯文本显示。
- 原因 2：前端气泡直接渲染 `message.content` 字符串，没有 Markdown（标记语言）解析层。
- 解决方案 2：新增 `MarkdownText` 组件，安全解析基础 Markdown（标记语言）并在主聊天与分支消息复用。

验证结果：
- `py -m pytest try/test_context_and_inline_screenshot.py -q` 通过，3 个用例通过；保留 FastAPI（Web API 框架）`on_event` 既有弃用警告。
- `py -m pytest try/test_chat_intent_routing.py try/test_formal_drill_flow.py try/test_answer_submission.py -q` 通过，12 个用例通过。
- `py -m ruff check backend try` 通过。
- `cd frontend && npm run build` 通过，确认 TypeScript（类型化 JavaScript）与 Vite（前端构建工具）生产构建正常。
- Browser（浏览器）验证通过：打开 `http://127.0.0.1:5173`，加载已有“截图词表练习：collision”会话；上下文弹窗显示 `3,467 / 100.0万`、消息数 18；Markdown（标记语言）DOM（文档对象模型）中存在 20 个 `strong` 节点和 3 个 `code` 节点，气泡文本不含原始 `**`；控制台无 error/warn（错误/警告）。

风险与回退：
- 本阶段为局部 API（接口）返回口径和前端渲染组件修改，无数据库迁移、批量删除或依赖新增。
- 回退方式：使用 Git（版本控制）回退本阶段提交即可恢复旧的纯文本消息渲染和全局 `token_usage` 返回口径。

## 2026-07-01 13:53 ~ 2026-07-01 13:56

本阶段完成内容：
- 开始处理用户数据污染隔离、无污染测试库、题目数据库自定义目录/迁移和默认模型选择功能。
- 已确认当前 Git（版本控制）工作区干净，从 `main` 创建任务分支 `codex/feature-data-isolation-user-db`，并提交变更前检查点 `chore: checkpoint before data isolation work`。
- 高风险标记：后续将迁移当前正式用户点目录 `C:\Users\29551\.langdrill-agent` 和项目内历史数据库 `data/langdrill_agent.db*`，并把当前污染学习数据分类保存到工作区内 `测试数据`。

新增/修改/生成的文件清单与用途说明：
- 暂无源码改动；本阶段仅完成只读确认、任务分支和变更前 Git 检查点。

错误汇报：
- 暂无。

验证结果：
- `git status --short --branch` 显示原 `main` 工作区干净。
- `git remote -v` 确认远端为 `https://github.com/q2955161835-debug/lang-drill-agent.git`。

风险与回退：
- 数据迁移前将把 `C:\Users\29551\.langdrill-agent`、项目内 `data/langdrill_agent.db*`、`.env` 和现有 `papers/` 运行资产备份到 `D:\0文件夹\备份` 下带时间戳的目录。
- 如新库或迁移结果异常，停止服务后从该备份目录恢复点目录、项目旧库和 `.env`；代码变更可通过 Git（版本控制）回退本任务分支。

## 2026-07-01 13:56 ~ 2026-07-01 14:17

本阶段完成内容：
- 已停止运行服务，把污染开发数据迁移到 `测试数据/开发数据/污染数据-20260701-135716`，并额外备份到工作区外 `D:\0文件夹\备份\langdrill-data-isolation-20260701-135518`。
- 已创建无污染正式用户数据库 `C:\Users\29551\.langdrill-agent\data\langdrill_agent.db`，学习会话、消息、题目、作答、知识项、分支和模型调用记录均为 0；默认考纲、每个考试 3 份真题索引、内置提示词和真实模型 `.env` 配置保留。
- 新增题目数据库自定义文件夹与迁移能力，支持 API（接口）、CLI（命令行接口）和设置页“数据”页签；新增“设为默认模型”功能。
- 修复污染根因：项目内历史 `data/langdrill_agent.db` 不再默认自动复制到正式用户点目录，只有显式设置 `LANGDRILL_MIGRATE_LEGACY_DB=1` 才迁移旧库。

新增/修改/生成的文件清单与用途说明：
- 新增 `backend/langdrill_agent/data_paths.py`：统一查询数据路径、初始化空库、迁移 SQLite（轻量数据库）题目数据库并写入 `.env`。
- 修改 `backend/langdrill_agent/db.py`：关闭旧项目数据库默认自动迁移，改为显式环境变量控制。
- 修改 `backend/langdrill_agent/api.py`、`backend/langdrill_agent/cli.py`、`backend/langdrill_agent/models.py`：新增数据路径 API（接口）、CLI（命令行接口）命令和请求模型；新增默认模型保存接口。
- 修改 `frontend/src/App.tsx`、`frontend/src/styles.css`、`frontend/src/types.ts`：新增设置页“数据”页签、题目数据库文件夹输入、迁移/覆盖开关、路径统计展示和“设为默认模型”按钮。
- 新增 `try/test_data_paths.py`，修改 `try/test_user_data_and_logging.py`、`try/test_model_config.py`：覆盖空库初始化、数据库迁移、旧库显式迁移和默认模型接口。
- 修改 `.gitignore`、`.env.example`、`AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：记录 `测试数据/` 排除、旧库迁移开关、数据路径功能和验收结果。
- 生成 `测试数据/开发数据/污染数据-20260701-135716/manifest.json`：记录污染数据迁移清单；该目录已加入 `.gitignore`，不提交真实数据。
- 工作区外生成 `D:\0文件夹\备份\langdrill-data-isolation-20260701-135518`：保存迁移前点目录、项目旧库、`.env` 和 `papers/` 的回退备份。

错误汇报：
- 问题：历史项目数据库会在新用户点目录空库时被自动复制，导致正式测试库被开发数据污染。
- 原因：`db.py` 的旧库兼容逻辑默认从项目内 `data/langdrill_agent.db` 复制到 `C:\Users\29551\.langdrill-agent\data\langdrill_agent.db`。
- 解决方案：默认初始化全新正式库，只保留默认参考数据；旧库复制改为 `LANGDRILL_MIGRATE_LEGACY_DB=1` 显式启用，并通过自动测试覆盖。

验证结果：
- `py -m pytest try/test_data_paths.py try/test_user_data_and_logging.py try/test_model_config.py -q` 通过，18 个用例通过。
- `py -m pytest try -q` 通过，56 个用例通过，1 个跳过。
- `py -m ruff check backend try` 通过。
- `cd frontend && npm run build` 通过。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall` 返回 0；`http://127.0.0.1:8000/docs` 和 `http://127.0.0.1:5173` 均返回 200。
- `GET /api/data-paths` 返回干净正式库：学习记录表均为 0，`syllabus_sources=7`、`exam_assets=24`、`prompt_modules=7`、`app_settings=10`。
- Browser（浏览器）验证通过：设置页“数据”页签显示题目数据库文件夹、当前数据库、迁移按钮、测试数据路径和 0 题计数；设置页“模型”页签显示“保存模型配置”和“设为默认模型”；控制台无 error/warn（错误/警告）。

风险与回退：
- 本阶段包含数据迁移和 `.env` 路径切换，属于高风险操作；已在修改前完成 Git（版本控制）检查点和工作区外备份。
- 回退方式：停止服务后从 `D:\0文件夹\备份\langdrill-data-isolation-20260701-135518` 恢复 `C:\Users\29551\.langdrill-agent`、项目旧 `data/`、`.env` 和 `papers/`；代码可通过 Git（版本控制）回退本任务分支提交。

## 2026-07-01 15:34 ~ 2026-07-01 16:00

本阶段完成内容：
- 为主聊天文本框新增拖拽文件文本导入：拖入 TXT（纯文本格式）/Markdown（Markdown 文本格式）/PDF（Portable Document Format，便携式文档格式）/DOCX（Word 文档格式）/图片等文件后，前端调用后端抽取接口并把文本追加到输入框。
- 为右侧“截图导入”新增拖拽区域：拖入截图、文档或文本文件后抽取 OCR（文字识别）/文本并填入截图识别文本框，继续复用原“解析文本 / 导入并开始练习”流程。
- 为设置页“历年真题与题型”的“手动导入试卷”新增拖拽上传：拖入试卷文件后点击导入，后端复制到 `papers/<考试>/raw` 并生成 `papers/<考试>/parsed` 解析 JSON（JSON 数据交换格式）。
- 为设置页“数据”页签的题目数据库文件夹输入框新增文件夹按钮，调用后端本机文件夹选择器辅助填写目录。

新增/修改/生成的文件清单与用途说明：
- 修改 `backend/langdrill_agent/api.py`：新增 `/api/files/extract-text`、`/api/past-papers/import-file` 和 `/api/data-paths/select-folder` 接口，并添加原始文件上传临时保存逻辑。
- 修改 `backend/langdrill_agent/paper_assets.py`：扩展文本抽取支持 CSV（逗号分隔值）、图片和复杂文档；图片/PPTX（PowerPoint 文档格式）/XLSX（Excel 工作簿格式）依赖可选 MinerU CLI（MinerU 命令行工具）。
- 修改 `backend/langdrill_agent/data_paths.py`、`backend/langdrill_agent/models.py`：新增本机文件夹选择服务和请求模型。
- 新增 `frontend/src/fileImport.ts`：封装文件文本抽取、文本追加和真题文件上传。
- 修改 `frontend/src/App.tsx`、`frontend/src/components/RightWorkbench.tsx`、`frontend/src/api.ts`、`frontend/src/styles.css`：接入主聊天、截图导入、手动导入试卷和数据路径选择按钮的前端交互与样式。
- 修改 `try/test_past_papers.py`：新增文件文本抽取接口和拖拽试卷上传导入接口测试。
- 修改 `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步拖拽导入、文件抽取和数据目录选择规则。
- 修改 `doc/进展记录/2026-7-1.md`：记录本阶段完成内容、验证和回退方案。

错误汇报：
- 问题：普通 Web（网页）浏览器拖入本地文件时不能可靠获得真实绝对路径，不能只把路径填入“本地试卷文件路径”。
- 原因：浏览器安全模型不会向网页暴露用户本机绝对路径。
- 解决方案：新增后端原始文件上传接口；前端拖入文件后直接上传二进制内容，由后端保存到真题资产目录或抽取文本，再回填现有输入框。

验证结果：
- `py -m pytest try -q` 通过，58 个用例通过、1 个跳过；保留 FastAPI（Web API 框架）`on_event` 既有弃用警告。
- `py -m ruff check backend try` 通过。
- `cd frontend && npm run build` 通过，确认 TypeScript（类型化 JavaScript）与 Vite（前端构建工具）生产构建正常。
- `git diff --check` 通过，仅提示 Windows 换行签出提醒。
- `http://127.0.0.1:5173` 与 `http://127.0.0.1:8000/docs` 均返回 200。

风险与回退：
- 本阶段为输入入口、文件上传接口和文档更新，无数据库迁移、批量删除或结构性重构。
- 回退方式：使用 Git（版本控制）回退本阶段提交即可移除拖拽导入、文件抽取接口和文件夹选择按钮。

## 2026-07-01 16:00 ~ 2026-07-01 16:19

本阶段完成内容：
- 修复官方考纲识别：CET 官网同一页面包含法语四级 2023 版、日语 2024 版和英语 2016 修订版，手动检查现在按语种与级别匹配，不再把法语 2023 版当成英语四/六级最新版。
- 新增内置考试 `cft4`（法语四级），默认考纲为“全国大学法语四级考试大纲（2023版）”，并补齐近 3 年真题索引和法语题型配置。
- 修复考纲状态展示：若用户已选择某个历史考纲，`status` 返回当前选中来源，而不是始终取年份最大的来源；手动检查发现新版本时优先复用同年份来源并切换选中。
- 修复真实官网页面标题粘连问题：若页面标题“考试大纲”与条目文本被抽到同一行，会清洗为真实条目标题再保存。
- 同步前端默认考试选项、自动测试、README（项目说明文件）、项目地图、AGENTS（项目规则）和验收标准。

新增/修改/生成的文件清单与用途说明：
- 修改 `backend/langdrill_agent/services.py`：新增官方考纲页面解析、语种/级别匹配、法语四级考纲来源、法语四级真题题型配置，并修正选中考纲状态口径。
- 修改 `backend/langdrill_agent/paper_assets.py`：把 `cft4` 加入内置真题目录初始化列表。
- 修改 `frontend/src/App.tsx`：把法语四级加入前端 fallback（回退）考试选项。
- 修改 `try/test_syllabus_service.py`：新增官方页面解析、英语 2016 不被法语 2023 污染、法语四级识别 2023 的测试。
- 修改 `try/test_data_paths.py`：默认参考数据计数更新为 `syllabus_sources=8`、`exam_assets=27`。
- 修改 `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：记录内置考试范围、官方考纲语种匹配规则和新验收项。

错误汇报：
- 问题：设置页显示英语考纲仍只认 2016，用户指出没有正确识别到官网页面中的 2023 版考纲。
- 原因：官网 2023 版条目实际是“全国大学法语四级考试大纲（2023版）”；旧逻辑只依赖内置默认年份，且没有把同一官网页面按语种和级别解析匹配。
- 解决方案：新增官方页面候选条目解析和 `cet4`/`cet6`/`cft4`/`cjt4`/`cjt6` 匹配器；英语四/六级只匹配“英语”考纲，法语四级单独匹配 2023 版。

验证结果：
- `py -m pytest try/test_syllabus_service.py try/test_data_paths.py try/test_past_papers.py -q` 通过，12 个用例通过。
- `py -m pytest try -q` 通过，60 个用例通过、1 个跳过。
- `py -m ruff check backend try` 通过。
- `cd frontend && npm run build` 通过。
- 临时数据库真实官网抓取验证通过：`cet4` 返回 2016 年《全国大学英语四、六级考试大纲（2016年修订版）》，`cft4` 返回 2023 年“全国大学法语四级考试大纲（2023版）”。
- `git diff --check` 通过，仅提示 Windows 换行签出提醒。

风险与回退：
- 本阶段为考纲识别、内置考试配置、前端选项和文档测试更新，无数据迁移、批量删除或结构性重构。
- 回退方式：使用 Git（版本控制）回退本阶段提交即可恢复到仅支持英语/日语等旧内置考试和旧考纲检查逻辑。

## 2026-07-01 16:27 ~ 2026-07-01 16:47

本阶段完成内容：
- 修复拖拽图片文件读取失败：`/api/files/extract-text` 现在会把空文件、超大文件和解析失败转成明确 HTTP（超文本传输协议）错误，不再让异常穿透成 `Request failed: 500`。
- 图片 OCR（文字识别）增加兜底链路：优先使用 MinerU CLI（MinerU 命令行工具），遇到 EOF/timeout（超时）等瞬时错误会重试；MinerU 仍失败时自动回退 RapidOCR（本地文字识别）。
- 将 `rapidocr-onnxruntime` 加入 `paper-parsing` 可选依赖，并安装到项目 `.venv` 供当前开发服务使用。
- 同步 README（项目说明文件）、AGENTS（项目规则）、项目地图和验收标准，记录图片 OCR 的 MinerU + RapidOCR 规则。

新增/修改/生成的文件清单与用途说明：
- 修改 `backend/langdrill_agent/api.py`：上传空文件返回 400，超限返回 413，解析失败返回 422 和具体错误。
- 修改 `backend/langdrill_agent/paper_assets.py`：新增 MinerU 重试、RapidOCR 图片 fallback（兜底）和图片 OCR 汇总错误。
- 修改 `pyproject.toml`：`paper-parsing` 可选依赖新增 `rapidocr-onnxruntime>=1.2.3`。
- 修改 `try/test_past_papers.py`：新增空上传、解析失败非 500、MinerU EOF 重试、图片回退 RapidOCR 测试。
- 修改 `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步 OCR 依赖和验收记录。
- 修改 `.gitignore`：忽略本地 `展示截图/` 目录，避免人工截图误提交。
- 工作区外运行环境行为：在项目虚拟环境 `.venv` 安装 `rapidocr-onnxruntime==1.2.3` 及其依赖，用于本地开发服务图片 OCR 验收。

错误汇报：
- 问题：拖拽截图后前端显示“文件读取失败：Request failed: 500”。
- 原因：MinerU CLI 成功发起 flash-extract（快速抽取）后，从远程 CDN 下载 `full.md` 时返回 EOF；异常未在通用抽取接口内转成业务错误，且运行中的 `.venv` 起初没有 RapidOCR 兜底依赖。
- 解决方案：通用抽取接口捕获解析异常并返回 422；MinerU 对 EOF 等瞬时错误重试；图片 OCR 失败后回退 RapidOCR；当前 `.venv` 安装 RapidOCR 并重启服务。

验证结果：
- `py -m pytest try/test_past_papers.py -q` 通过，10 个用例通过。
- `py -m pytest try -q` 通过，64 个用例通过、1 个跳过。
- `py -m ruff check backend try` 通过。
- `cd frontend && npm run build` 通过。
- 真实截图接口验证：同一 PNG 上传 `/api/files/extract-text` 返回 200，`parser=rapidocr-onnxruntime`，OCR 文本包含“全国大学法语四级考试大纲(2023版)”。
- Browser（浏览器）验收通过：右侧截图导入区接收 1 个 PNG 文件 drop（拖放），文本框回填 217 个字符，页面无 `Request failed: 500`，控制台无 error/warn（错误/警告）。

风险与回退：
- 本阶段为错误处理、可选 OCR 依赖和文档测试更新，无数据库迁移、批量删除或结构性重构。
- 回退方式：使用 Git（版本控制）回退本阶段提交；若需要还原运行环境，可在 `.venv` 中卸载 `rapidocr-onnxruntime` 及仅由其引入的 OCR 依赖。

## 2026-07-01 16:48 ~ 2026-07-01 17:08

本阶段完成内容：
- 新增 MinerU token 配置：设置页“模型”中提供官方 token 获取地址和 API 文档地址，可保存/清除 `MINERU_TOKEN`；后端只返回是否已配置和脱敏预览，不返回 token 明文。
- 扩展文件解析链路：截图、试卷和通用文件抽取会读取 `MINERU_TOKEN`，有 token 时使用 `mineru-open-api extract` 精准解析，无 token 时继续使用 `flash-extract` 轻量解析；图片失败后仍回退 RapidOCR（本地文字识别）。
- 新增模型视觉能力配置：内置 OpenAI/GPT 和 Claude 模型默认支持图片输入，DeepSeek（深度求索）、MiMo（小米米魔）和自定义模型默认关闭；设置页可手动切换并保存。
- 新增聊天栏图片分流：当前模型支持视觉时，拖入图片会作为附件发送给模型；当前模型不支持视觉时，拖入图片会调用 MinerU/RapidOCR 抽取文本并追加到输入框。
- 扩展 OpenAI-compatible（OpenAI 兼容）和 Anthropic Messages（Anthropic 消息接口）模型调用 payload，支持图片附件且不把图片 base64 写入模型调用日志。

新增/修改/生成的文件清单与用途说明：
- 修改 `backend/langdrill_agent/models.py`：新增聊天图片附件、MinerU 配置请求和模型视觉能力字段。
- 修改 `backend/langdrill_agent/providers.py`：新增 OpenAI-compatible `image_url` 和 Anthropic base64 image source（图片源）payload 组装。
- 修改 `backend/langdrill_agent/services.py`：新增 `MinerUConfigService`，扩展 `ModelConfigService` 保存/返回模型视觉能力和 `MINERU_TOKEN` 环境变量账本。
- 修改 `backend/langdrill_agent/api.py`：新增 `/api/mineru-config`，bootstrap（初始化加载）返回 MinerU 状态，聊天接口接收图片附件并校验视觉能力，文件抽取接口传入 MinerU token。
- 修改 `backend/langdrill_agent/paper_assets.py`：按 token 选择 MinerU 精准/轻量解析模式。
- 修改 `frontend/src/App.tsx`、`frontend/src/fileImport.ts`、`frontend/src/types.ts`、`frontend/src/styles.css`：新增设置页 MinerU 配置、模型视觉开关、聊天附件队列、图片分流和样式。
- 修改 `.env.example`：新增 `MINERU_TOKEN` 占位和官方地址说明。
- 修改 `try/test_model_config.py`、`try/test_provider_reasoning.py`、`try/test_past_papers.py`：覆盖 MinerU token 安全状态、模型视觉能力保存、多模态 payload 和 MinerU token 模式。
- 修改 `AGENTS.md`、`README.md`、`doc/项目地图.md`、`doc/验收标准.md`：同步 MinerU token、视觉分流和解析验收规则。
- 修改 `doc/进展记录/2026-7-1.md`：记录本阶段完成内容、验证和回退方案。

错误汇报：
- 本阶段未出现实现阻断错误。针对用户提出的“需要 MinerU token 配置”和“聊天图片按模型视觉能力分流”，已通过配置服务、前端分流和 provider 多模态 payload 解决。

验证结果：
- `py -m pytest try/test_model_config.py try/test_provider_reasoning.py try/test_past_papers.py -q` 通过，33 个用例通过。
- `py -m pytest try -q` 通过，70 个用例通过、1 个跳过。
- `py -m ruff check backend try` 通过。
- `cd frontend && npm run build` 通过。
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\dev\start-dev.ps1 -NoBrowser -SkipInstall` 返回 0。
- Browser（浏览器）验收通过：`http://127.0.0.1:5173` 首页加载，聊天栏显示“图片：MinerU解析”；设置页显示“当前模型支持图片输入”开关、MinerU token 输入框、`https://mineru.net/apiManage/token` 官方 token 链接和 `https://mineru.net/apiManage/docs` 文档链接；点击视觉开关后说明变为“聊天栏图片直传当前模型；试卷、截图和文件抽取仍可走 MinerU。”；点击“取消”后未保存测试状态；控制台无 error/warn（错误/警告）。

风险与回退：
- 本阶段为配置、解析链路、聊天附件和文档测试更新，无数据库迁移、批量删除或工作区外真实数据写入；Browser（浏览器）验收未保存测试 token 或模型配置。
- 回退方式：使用 Git（版本控制）回退本阶段提交即可移除 MinerU token 设置、视觉能力字段、多模态图片附件和相关前端入口。
