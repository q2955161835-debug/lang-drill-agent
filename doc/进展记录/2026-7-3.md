# 2026-07-03

## 2026-07-03 00:18 ~ 2026-07-03 00:22

### 本阶段完成内容

- 排查左侧“当日学习”面板截图词表 11 个词显示为 22 个词的问题。
- 确认真实数据库中 2026-07-02 截图词表为 11 条 `knowledge_items` 和 11 道题；当前工作区源码直接计算 `daily_panel.knowledge_total=11`，但运行中的后端 API 仍返回 `knowledge_total=22`、`knowledge_done=4`。
- 根因：8000 端口后端进程是在去重修复前启动的旧 Python 进程，未重新加载已修改的 `SessionService._knowledge_progress()`。
- 处理：运行 `scripts/dev/start-dev.ps1 -NoBrowser -SkipInstall` 重启本地前后端服务；重启后 `/api/sessions/ses_6dccc6795411` 返回 `knowledge_total=11`、`knowledge_done=3`，与当前源码口径一致。

### 新增/修改/生成的文件清单与用途说明

- `doc/进展记录/2026-7-3.md`：记录本次运行态旧进程导致修复未生效的排查、处理和验证结果。

### 工作区外文件/行为

- 只读检查 `C:\Users\29551\.langdrill-agent\data\langdrill_agent.db`，用于核对真实题目、词条和会话面板统计；未修改数据库内容。
- 重启本地运行服务：后端 `http://127.0.0.1:8000`，前端 `http://127.0.0.1:5173`。

### 验证结果

- `GET http://127.0.0.1:8000/api/sessions/ses_6dccc6795411`：`questions_total=11`、`questions_done=3`、`knowledge_total=11`、`knowledge_done=3`。
- `GET http://127.0.0.1:8000/docs`：200。
- `GET http://127.0.0.1:5173`：200。
- `py -m pytest try/test_daily_scope.py try/test_screenshot_import_workflow.py -q`：8 passed，2 warnings（既有 FastAPI `on_event` 弃用警告）。

### 错误汇报

- 问题：用户页面显示 11 个截图词汇被统计成 22 个当日词汇。
- 原因：运行中的后端进程未重启，仍使用去重修复前的旧函数字节码；旧逻辑把题目标签 `vocabulary:<term>` 和导入词条 `<term>` 分别计数。
- 解决方案：重启前后端服务，让后端加载当前源码；API 回查已恢复为 11 个去重词汇。
