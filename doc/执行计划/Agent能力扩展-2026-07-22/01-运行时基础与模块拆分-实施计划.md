# 运行时基础与模块拆分 Implementation Plan

状态：已完成；2026-07-22 验收通过，可进入下游计划。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 建立版本化数据库迁移、统一 Agent Run/Trace、可靠调用、错误码、流式事件和前端测试基线，同时保持现有学习路径行为不变。

**Architecture:** 在现有扁平后端旁新增 `runtime/` 与 `routers/`，先提供稳定接口，再逐步把相关代码从巨型文件迁出。数据库迁移按文件顺序幂等执行；长任务通过数据库事件 + SSE 公开。

**Tech Stack:** Python 3.11、FastAPI、Pydantic 2、SQLite、pytest、React 19、TypeScript、Vitest。

## Global Constraints

- 不重写现有学习业务；只提取本阶段必需边界。
- `001_initial.sql` 保持可用于新库；增量变化进入新迁移文件。
- API 错误返回 `code` 和 `params`，保留 `detail` 兼容现有前端。
- 每个任务结束必须运行相关测试并提交。

---

### Task 1: 版本化数据库迁移器

**Files:**
- Create: `tests/test_migration_runner.py`
- Create: `backend/langdrill_agent/migrations/002_runtime_foundation.sql`
- Modify: `backend/langdrill_agent/db.py`
- Modify: `pyproject.toml`

**Interfaces:**
- Produces: `iter_migration_files() -> list[Path]`
- Produces: `apply_migrations(conn: sqlite3.Connection) -> list[str]`
- Existing `init_db()` continues returning `Path`.

- [x] **Step 1: Write the failing migration tests**

```python
from pathlib import Path

from langdrill_agent.db import apply_migrations, connect, init_db, iter_migration_files


def test_init_db_records_all_migrations(tmp_path: Path) -> None:
    db_path = tmp_path / "runtime.db"
    init_db(db_path)
    with connect(db_path) as conn:
        versions = [row[0] for row in conn.execute(
            "SELECT version FROM schema_migrations ORDER BY version"
        )]
    assert versions == [path.stem for path in iter_migration_files()]


def test_apply_migrations_is_idempotent(tmp_path: Path) -> None:
    db_path = tmp_path / "runtime.db"
    init_db(db_path)
    with connect(db_path) as conn:
        assert apply_migrations(conn) == []
```

- [x] **Step 2: Run the tests and verify failure**

Run: `py -m pytest tests/test_migration_runner.py -v`
Expected: FAIL because `apply_migrations` and `schema_migrations` do not exist.

- [x] **Step 3: Add migration metadata and runtime tables**

```sql
CREATE TABLE IF NOT EXISTS schema_migrations (
  version TEXT PRIMARY KEY,
  applied_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS agent_runs (
  id TEXT PRIMARY KEY,
  session_id TEXT,
  task_type TEXT NOT NULL,
  status TEXT NOT NULL,
  goal TEXT NOT NULL DEFAULT '',
  completion_criteria_json TEXT NOT NULL DEFAULT '[]',
  error_code TEXT NOT NULL DEFAULT '',
  created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS agent_run_events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  run_id TEXT NOT NULL,
  event_type TEXT NOT NULL,
  payload_json TEXT NOT NULL DEFAULT '{}',
  created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY(run_id) REFERENCES agent_runs(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_agent_run_events_run_id
ON agent_run_events(run_id, id);
```

- [x] **Step 4: Implement ordered migration execution**

```python
MIGRATIONS_DIR = Path(__file__).resolve().parent / "migrations"


def iter_migration_files() -> list[Path]:
    return sorted(MIGRATIONS_DIR.glob("[0-9][0-9][0-9]_*.sql"))


def apply_migrations(conn: sqlite3.Connection) -> list[str]:
    conn.execute(
        "CREATE TABLE IF NOT EXISTS schema_migrations "
        "(version TEXT PRIMARY KEY, applied_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)"
    )
    applied = {row[0] for row in conn.execute("SELECT version FROM schema_migrations")}
    completed: list[str] = []
    for path in iter_migration_files():
        version = path.stem
        if version in applied:
            continue
        conn.executescript(path.read_text(encoding="utf-8"))
        conn.execute("INSERT INTO schema_migrations(version) VALUES (?)", (version,))
        completed.append(version)
    return completed
```

Update `init_db()` to call `apply_migrations(conn)` instead of reading only `SCHEMA_PATH`.

Change pytest discovery to `testpaths = ["tests"]`; `try/` remains reserved for ignored one-off experiments and debugging artifacts.

- [x] **Step 5: Run migration tests and the existing backend tests**

Run: `py -m pytest tests/test_migration_runner.py -v`
Expected: migration tests pass.
Run: `py -m pytest -v`
Expected: all existing tests pass.

- [x] **Step 6: Commit**

```powershell
git add backend/langdrill_agent/db.py backend/langdrill_agent/migrations/002_runtime_foundation.sql pyproject.toml tests/test_migration_runner.py
git commit -m "feat: add versioned database migrations"
```

### Task 2: Run 类型、仓库和稳定错误码

**Files:**
- Create: `backend/langdrill_agent/runtime/__init__.py`
- Create: `backend/langdrill_agent/runtime/models.py`
- Create: `backend/langdrill_agent/runtime/repository.py`
- Create: `backend/langdrill_agent/runtime/errors.py`
- Create: `tests/test_runtime_repository.py`

**Interfaces:**
- Produces: `RunStatus`, `RuntimeEvent`, `AgentRunRecord`
- Produces: `AgentRunRepository.create/get/set_status/append_event/events_after`
- Produces: `RuntimeErrorPayload(code, detail, params)`.

- [x] **Step 1: Write failing repository tests**

```python
from langdrill_agent.db import connect, init_db
from langdrill_agent.runtime.repository import AgentRunRepository


def test_run_repository_round_trip(tmp_path):
    path = tmp_path / "runs.db"
    init_db(path)
    with connect(path) as conn:
        repo = AgentRunRepository(conn)
        run = repo.create(session_id="s1", task_type="knowledge_index", goal="index file")
        repo.append_event(run.id, "progress", {"percent": 25})
        repo.set_status(run.id, "completed")
        assert repo.get(run.id).status == "completed"
        assert repo.events_after(run.id, 0)[0].payload == {"percent": 25}
```

- [x] **Step 2: Verify failure**

Run: `py -m pytest tests/test_runtime_repository.py -v`
Expected: FAIL because `runtime.repository` does not exist.

- [x] **Step 3: Add exact runtime models**

```python
class RunStatus(str, Enum):
    queued = "queued"
    running = "running"
    paused = "paused"
    completed = "completed"
    failed = "failed"
    cancelled = "cancelled"


class AgentRunRecord(BaseModel):
    id: str
    session_id: str | None
    task_type: str
    status: RunStatus
    goal: str
    completion_criteria: list[str] = Field(default_factory=list)
    error_code: str = ""


class RuntimeEvent(BaseModel):
    id: int
    run_id: str
    event_type: str
    payload: dict[str, Any]
    created_at: str
```

- [x] **Step 4: Implement repository with JSON serialization and UUID IDs**

Implement `create`, `get`, `set_status`, `append_event`, and `events_after`; use existing `dumps`, `loads`, and `new_id` helpers where available. Raise `KeyError(run_id)` when `get` cannot find a record.

- [x] **Step 5: Add stable error payload**

```python
class RuntimeErrorPayload(BaseModel):
    code: str
    detail: str
    params: dict[str, Any] = Field(default_factory=dict)


class RuntimeServiceError(RuntimeError):
    def __init__(self, code: str, detail: str, **params: Any):
        super().__init__(detail)
        self.payload = RuntimeErrorPayload(code=code, detail=detail, params=params)
```

- [x] **Step 6: Run tests and commit**

Run: `py -m pytest tests/test_runtime_repository.py -v`
Expected: PASS.
Run: `py -m ruff check backend/langdrill_agent/runtime tests/test_runtime_repository.py`
Expected: no violations.

```powershell
git add backend/langdrill_agent/runtime tests/test_runtime_repository.py
git commit -m "feat: add agent run repository and error contract"
```

### Task 3: 可靠调用策略与能力意图分类器

**Files:**
- Create: `backend/langdrill_agent/runtime/retry.py`
- Create: `backend/langdrill_agent/runtime/intent.py`
- Create: `tests/test_runtime_retry.py`
- Create: `tests/test_capability_intent.py`
- Modify: `backend/langdrill_agent/models.py`

**Interfaces:**
- Produces: `RetryPolicy(max_attempts=3, base_delay_seconds=0.25, max_delay_seconds=4.0)`
- Produces: `run_with_retry(operation, policy, retryable) -> T`
- Produces: `CapabilityIntentClassifier.classify(text) -> CapabilityIntent`
- Adds `TaskType.agentic_task` without changing existing TaskRouter routes.

- [x] **Step 1: Write retry and intent tests**

```python
def test_retry_succeeds_on_second_attempt():
    calls = 0
    def operation():
        nonlocal calls
        calls += 1
        if calls == 1:
            raise TimeoutError("temporary")
        return "ok"
    assert run_with_retry(operation, RetryPolicy(max_attempts=2), lambda exc: True) == "ok"


@pytest.mark.parametrize("text", [
    "帮我给这个项目增加导出功能",
    "安装并配置这个工具",
    "整理这个目录并生成报告",
])
def test_classifier_detects_agentic_actions(text):
    assert CapabilityIntentClassifier().classify(text).requires_runtime is True


def test_classifier_does_not_capture_learning_request():
    assert CapabilityIntentClassifier().classify("给我出五道四级阅读题").requires_runtime is False
```

- [x] **Step 2: Verify tests fail**

Run: `py -m pytest tests/test_runtime_retry.py tests/test_capability_intent.py -v`
Expected: FAIL because modules do not exist.

- [x] **Step 3: Implement deterministic retry policy**

Use injected `sleep: Callable[[float], None]` in tests; never retry validation, permission, or non-idempotent errors unless the caller supplies an idempotency key.

- [x] **Step 4: Implement conservative capability intent**

```python
class CapabilityIntent(BaseModel):
    requires_runtime: bool
    confidence: float
    reason: str


class CapabilityIntentClassifier:
    ACTION = re.compile(r"(?:帮我|请|需要).{0,18}(?:增加|实现|安装|配置|处理|整理|修改|构建|运行)|(?:install|build|edit|run)\b", re.I)
    LEARNING = re.compile(r"(?:出题|刷题|练习|讲解|复盘|单词|阅读题)")

    def classify(self, text: str) -> CapabilityIntent:
        if self.LEARNING.search(text):
            return CapabilityIntent(requires_runtime=False, confidence=0.9, reason="learning_flow")
        matched = bool(self.ACTION.search(text))
        return CapabilityIntent(
            requires_runtime=matched,
            confidence=0.75 if matched else 0.2,
            reason="explicit_action" if matched else "chat",
        )
```

- [x] **Step 5: Run tests and existing TaskRouter regression**

Run: `py -m pytest tests/test_runtime_retry.py tests/test_capability_intent.py -v`
Expected: PASS.
Run: `py -m pytest -k task_router -v`
Expected: existing routing tests pass.

- [x] **Step 6: Commit**

```powershell
git add backend/langdrill_agent/runtime backend/langdrill_agent/models.py tests/test_runtime_retry.py tests/test_capability_intent.py
git commit -m "feat: add runtime retry and action intent detection"
```

### Task 4: Agent Run API 与 SSE 事件

**Files:**
- Create: `backend/langdrill_agent/routers/__init__.py`
- Create: `backend/langdrill_agent/routers/agent_runs.py`
- Create: `tests/test_agent_runs_api.py`
- Modify: `backend/langdrill_agent/api.py`

**Interfaces:**
- Produces: `GET /api/agent-runs/{run_id}`
- Produces: `GET /api/agent-runs/{run_id}/events?after=<id>` as `text/event-stream`
- Produces: `POST /api/agent-runs/{run_id}/cancel`

- [x] **Step 1: Write failing API tests**

```python
def test_agent_run_status_and_cancel(client, seeded_run_id):
    status = client.get(f"/api/agent-runs/{seeded_run_id}")
    assert status.status_code == 200
    cancelled = client.post(f"/api/agent-runs/{seeded_run_id}/cancel")
    assert cancelled.json()["run"]["status"] == "cancelled"


def test_missing_run_returns_stable_error(client):
    response = client.get("/api/agent-runs/missing")
    assert response.status_code == 404
    assert response.json()["detail"]["code"] == "AGENT_RUN_NOT_FOUND"
```

- [x] **Step 2: Verify failure**

Run: `py -m pytest tests/test_agent_runs_api.py -v`
Expected: 404 routes or missing fixtures.

- [x] **Step 3: Implement router and stable error helper**

```python
router = APIRouter(prefix="/api/agent-runs", tags=["agent-runs"])


@router.get("/{run_id}")
def get_run(run_id: str) -> dict[str, Any]:
    with transaction() as conn:
        try:
            run = AgentRunRepository(conn).get(run_id)
        except KeyError:
            raise HTTPException(404, detail={"code": "AGENT_RUN_NOT_FOUND", "params": {"run_id": run_id}})
        return {"run": run.model_dump(mode="json")}
```

Implement SSE frames as `id: <event_id>\nevent: <event_type>\ndata: <json>\n\n`; stop polling after terminal state and no pending events.

- [x] **Step 4: Register router without moving existing endpoints**

Add `app.include_router(agent_runs.router)` after `FastAPI()` construction. Do not modify existing route behavior in this task.

- [x] **Step 5: Run API tests and commit**

Run: `py -m pytest tests/test_agent_runs_api.py -v`
Expected: PASS.

```powershell
git add backend/langdrill_agent/routers backend/langdrill_agent/api.py tests/test_agent_runs_api.py
git commit -m "feat: expose agent run status and event stream"
```

### Task 5: 前端测试基线和 Agent Run 客户端

**Files:**
- Modify: `frontend/package.json`
- Modify: `frontend/package-lock.json`
- Create: `frontend/src/features/agentRuns/types.ts`
- Create: `frontend/src/features/agentRuns/client.ts`
- Create: `frontend/src/features/agentRuns/client.test.ts`
- Modify: `frontend/src/types.ts`

**Interfaces:**
- Produces: `AgentRun`, `AgentRunEvent`
- Produces: `getAgentRun(id)` and `subscribeAgentRun(id, onEvent)`.

- [x] **Step 1: Add Vitest dependencies and scripts**

```json
"scripts": {
  "test": "vitest run",
  "test:watch": "vitest"
},
"devDependencies": {
  "@testing-library/react": "^16.0.0",
  "jsdom": "^26.0.0",
  "vitest": "^3.0.0"
}
```

Run: `npm --prefix frontend install`
Expected: lockfile updates without audit-blocking install errors.

- [x] **Step 2: Write failing client tests**

```ts
import { describe, expect, it, vi } from "vitest";
import { getAgentRun } from "./client";

it("loads the stable run envelope", async () => {
  vi.stubGlobal("fetch", vi.fn(async () => ({
    ok: true,
    json: async () => ({ run: { id: "r1", status: "running", task_type: "index" } })
  })));
  await expect(getAgentRun("r1")).resolves.toMatchObject({ id: "r1", status: "running" });
});
```

- [x] **Step 3: Verify failure**

Run: `npm --prefix frontend test -- src/features/agentRuns/client.test.ts`
Expected: FAIL because client module does not exist.

- [x] **Step 4: Implement typed client**

```ts
export type AgentRun = {
  id: string;
  session_id?: string | null;
  task_type: string;
  status: "queued" | "running" | "paused" | "completed" | "failed" | "cancelled";
  goal: string;
  completion_criteria: string[];
  error_code: string;
};

export async function getAgentRun(id: string): Promise<AgentRun> {
  const response = await apiGet<{ run: AgentRun }>(`/api/agent-runs/${encodeURIComponent(id)}`);
  return response.run;
}
```

Use native `EventSource` for subscription and return a cleanup function that closes it.

- [x] **Step 5: Run tests, TypeScript build, and commit**

Run: `npm --prefix frontend test`
Expected: PASS.
Run: `npm --prefix frontend run build`
Expected: PASS.

```powershell
git add frontend/package.json frontend/package-lock.json frontend/src/features/agentRuns frontend/src/types.ts
git commit -m "test: add frontend runtime client baseline"
```

### Task 6: 基础阶段回归与验收文档

**Files:**
- Create: `tests/test_runtime_learning_regression.py`
- Modify: `doc/验收/模块/验收-Agent运行时.md`
- Create then archive after pass: `doc/验收/任务/验收-Agent运行时基础.md`
- Modify: `doc/项目地图.md`

**Interfaces:**
- Consumes all Task 1–5 interfaces.
- Produces a passing L3 foundation gate for plans 02–07.

- [x] **Step 1: Add regression assertions**

Write tests proving greetings remain `general_chat`, explicit drills remain `daily_drill`, answer selection remains `answer_question`, and the capability classifier does not change TaskRouter results.

```python
def test_creative_capability_detection_does_not_replace_learning_route():
    router = TaskRouter()
    task = router.route("给我出五道阅读题", has_active_question=False)
    intent = CapabilityIntentClassifier().classify("给我出五道阅读题")
    assert task is TaskType.daily_drill
    assert intent.requires_runtime is False
```

- [x] **Step 2: Run complete foundation verification**

Run: `py -m pytest -v`
Expected: all passed.
Run: `py -m ruff check backend tests`
Expected: no violations.
Run: `npm --prefix frontend test`
Run: `npm --prefix frontend run build`
Expected: tests and build pass.

- [x] **Step 3: Update L3 acceptance records**

Record exact commands, results, migration rollback, known risks, and conclusion. Archive the task record under `doc/验收/归档/2026-07-22/` only when conclusion is `通过`.

- [x] **Step 4: Commit source/test changes**

`doc/` remains local per repository policy. Commit only tracked source and tests if any remain.

```powershell
git status --short
git commit --allow-empty -m "chore: complete runtime foundation gate"
```
